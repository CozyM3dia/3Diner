## Problem

<!-- What is broken or missing, and how does it reach a user? For anything
     touching untrusted input, name the entry point (e.g. the public
     POST /api/orders endpoint). -->

## Fix

<!-- What changed, and why this approach over the alternatives. -->

## Tests

<!-- Which test file covers this, and what it asserts. For bug fixes:
     confirm the test failed before the fix. -->

## Verification

- [ ] `npm run typecheck`
- [ ] `npm run test:ci`
- [ ] `npm run lint`
- [ ] `npm run build`

<!-- Note any pre-existing failures you did not introduce. -->
