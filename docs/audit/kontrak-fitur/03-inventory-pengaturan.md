# Kontrak Fitur Dashboard 3Diner — Bagian 03

## Inventory · Pengaturan · QR · Jadwal & Diskon · Pengumuman

> Bagian dari kontrak **"TIDAK ADA FITUR HILANG"** untuk rebuild total UI dashboard 3Diner.
> Repo: `C:\Kerja\3Diner\App` · Next.js 16 + Supabase · Worktree: `.worktrees/dashboard-ux`.

**Cakupan bagian 03:** 145 fitur dari 564 total.

| Area | Rute | Jumlah fitur |
| --- | --- | --- |
| Area 7 — Inventory | `/dashboard/inventory` (+ mode embedded di `/dashboard`) | 70 |
| Area 8 — Pengaturan, QR, Jadwal & Diskon, Pengumuman | `/dashboard/settings`, `/dashboard/scheduler`, `/dashboard/announcements` | 75 |
| **Total** | | **145** |

**Aturan baca tabel**

- **Lokasi** adalah baseline sebelum rebuild. Kalau file dipindah, kolom ini jadi acuan "fitur ini dulu ada di sini" — bukan perintah mempertahankan path.
- **State** adalah daftar kondisi yang wajib punya tampilan. State yang tidak ditangani = fitur hilang, walaupun jalur bahagianya jalan.
- **Cara verifikasi** ditulis supaya bisa dijalankan tanpa membuka kode. Semua langkah ini terangkum ulang di bagian **CHECKLIST VERIFIKASI MANUAL FINAL** di akhir dokumen.
- **Risiko kalau hilang** menjelaskan kenapa fitur itu gampang lenyap saat file ditulis ulang. Baca kolom ini dulu sebelum menghapus baris kode yang kelihatannya tidak berguna.

---


## Area 7 — Inventory (`/dashboard/inventory`) — 70 fitur

Rute: `/dashboard/inventory`. Komponen inti: `InventoryWorkspace`, `InventoryTable`, `InventoryItemForm`, `StockAdjustmentModal`, `RecipeEditor`. Sumber data: `src/lib/dashboard-inventory.ts`. Mutasi: `src/lib/dashboard-actions.ts`.


### Rute, penjagaan sesi & skeleton — fitur 1–5

| # | Fitur | Lokasi | Apa yang terjadi | Interaksi | State | Cara verifikasi | Risiko kalau hilang |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | **Guard autentikasi halaman inventory** | `src/app/dashboard/inventory/page.tsx:7-8` | Halaman memanggil getDashboardCafeContext() lalu redirect("/login") kalau userId null. Halaman ini async server component, jadi redirect terjadi sebelum HTML apa pun terkirim. | • Buka /dashboard/inventory tanpa sesi login -> dilempar ke /login<br>• Buka dengan sesi valid -> render normal | `belum login (redirect)` · `login valid` · `sesi expired di tengah jalan (redirect saat refresh)` | Hapus cookie sesi Supabase di devtools, muat ulang /dashboard/inventory. Harus mendarat di /login, bukan halaman kosong atau error. | Saat file page.tsx ditulis ulang, orang mudah lupa baris redirect karena UI-nya tidak kelihatan. Halamannya tetap render tapi kosong tanpa proteksi. |
| 2 | **Dedupe lookup kafe per request lewat React cache()** | `src/lib/dashboard-context.ts:29 (getDashboardCafeContext), dipakai di src/app/dashboard/inventory/page.tsx:7` | getDashboardCafeContext dibungkus cache() sehingga auth.getUser + getOwnerCafeSlug + getCafeBySlug hanya jalan sekali walau layout dan page sama-sama memanggilnya dalam satu render tree. | • Tidak ada interaksi langsung; efek terasa sebagai waktu muat halaman | `cache hit (layout sudah memanggil duluan)` · `cache miss (panggilan pertama)` · `user null -> EMPTY` · `slug null -> hanya userId terisi` | Tambah console.log sementara di dalam getDashboardCafeContext, muat /dashboard/inventory sekali. Log harus muncul satu kali, bukan dua kali (layout + page). | Kalau rebuild mengganti pemanggilan jadi supabase.auth.getUser() langsung di page, halaman tetap benar tapi round-trip auth berlipat dan halaman terasa lebih lambat. Regresi ini tak terlihat di UI. |
| 3 | **Skeleton route-level saat halaman inventory dimuat** | `src/app/dashboard/inventory/loading.tsx:1-23` | File loading.tsx Next.js menampilkan kerangka: judul + subjudul, 4 kartu metrik (grid-cols-2 -> lg:grid-cols-4), dan 2 panel (tabel + sidebar) dengan layout lg:grid-cols-[minmax(0,1fr)_320px] yang sama persis dengan layout asli. | • Navigasi dari halaman dashboard lain ke Inventory -> skeleton muncul sampai data siap | `loading (aria-busy=true)` · `selesai (diganti page.tsx)` | Throttle jaringan ke Slow 3G, klik menu Inventory dari sidebar. Skeleton harus muncul dengan bentuk yang sama dengan layout final (4 kartu + 2 panel), bukan spinner polos atau layar putih. | loading.tsx adalah file terpisah yang tidak diimpor siapa pun. Rebuild yang fokus ke komponen sering meninggalkan loading.tsx dengan layout lama, sehingga skeleton tidak lagi cocok dengan UI baru dan halaman terasa 'melompat'. |
| 4 | **Aksesibilitas skeleton loading** | `src/app/dashboard/inventory/loading.tsx:3` | Container skeleton memakai aria-busy="true" dan aria-label="Memuat inventory" supaya screen reader mengumumkan status memuat, bukan membaca kotak kosong. | • Screen reader membacakan 'Memuat inventory' saat transisi rute | `loading` | Inspect element root skeleton saat loading; harus ada aria-busy="true" dan aria-label="Memuat inventory". | Atribut ARIA adalah hal pertama yang hilang saat markup ditulis ulang karena tidak memengaruhi tampilan sama sekali. |
| 5 | **Animasi shimmer skeleton** | `src/app/globals.css:490-499 (.dash-skel, @keyframes dashShimmer), dipakai loading.tsx:5,6,9,13,14,17,18` | Kelas .dash-skel memberi gradient bergerak 1.5s linear infinite dengan background-size 720px, memberi kesan konten sedang dimuat, bukan blok mati. | • Pasif, terlihat selama loading | `loading` · `prefers-reduced-motion (animasi dipangkas ke 0.01ms oleh globals.css:514-520)` | Muat halaman dengan throttle; blok abu-abu harus berkilau bergerak. Lalu aktifkan prefers-reduced-motion di devtools; kilau harus berhenti. | Kalau rebuild pakai skeleton bawaan komponen library, kelas .dash-skel jadi yatim dan animasi hilang tanpa error. |

### Pengambilan & pengolahan data inventory — fitur 6–13

| # | Fitur | Lokasi | Apa yang terjadi | Interaksi | State | Cara verifikasi | Risiko kalau hilang |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 6 | **Pengambilan data items dan movements secara paralel** | `src/lib/dashboard-inventory.ts:58-70` | Query Inventory_Items dan Inventory_Movements dijalankan bersamaan lewat Promise.all, bukan berurutan, sehingga waktu muat halaman = query terlama, bukan jumlah keduanya. | • Pasif, memengaruhi kecepatan halaman | `keduanya sukses` · `salah satu gagal` · `keduanya gagal` | Lihat waterfall jaringan/log Supabase saat memuat halaman: dua query harus start hampir bersamaan, bukan berurutan. | Refactor yang memecah fetch ke dua komponen server terpisah atau memakai await berurutan akan menggandakan latency tanpa ada yang menyadarinya. |
| 7 | **Urutan daftar bahan alfabetis A-Z** | `src/lib/dashboard-inventory.ts:63 (.order("name", { ascending: true }))` | Item inventory selalu diurutkan berdasarkan nama menaik di sisi database. Tabel di UI tidak melakukan sort sendiri, jadi urutan ini satu-satunya sumber kebenaran. | • Pasif; user melihat daftar terurut nama | `daftar kosong` · `daftar berisi` | Tambah bahan bernama 'Zucchini' lalu 'Apel'. Setelah refresh, Apel harus muncul di atas Zucchini. | Karena sortir terjadi di lib bukan di komponen tabel, rebuild UI tabel mudah menghapus asumsi ini atau menambah sort client-side yang bentrok. Tidak ada UI kontrol sort, jadi hilangnya urutan tidak memunculkan error apa pun. |
| 8 | **Riwayat mutasi 12 terakhir dengan join nama dan satuan bahan** | `src/lib/dashboard-inventory.ts:64-69` | Query mutasi memakai select("*, inventory_item:Inventory_Items(name, unit)"), diurut created_at menurun, dan dibatasi tepat 12 baris. Angka 12 ini juga ditulis literal di UI sebagai '12 aktivitas terakhir'. | • Pasif; panel Mutasi Terbaru menampilkan hasilnya | `0 mutasi` · `1-11 mutasi` · `tepat 12 mutasi` · `bahan sudah dihapus (join null)` | Lakukan 13 penyesuaian stok berturut-turut, lalu refresh. Panel harus menampilkan tepat 12 entri terbaru, yang paling atas adalah yang terakhir dilakukan. | Angka 12 muncul di dua tempat berbeda (query dan teks UI di InventoryTable.tsx:230). Rebuild UI bisa mengganti teksnya jadi '10 aktivitas terakhir' tanpa mengubah query, atau sebaliknya. |
| 9 | **Pelacakan kegagalan muat parsial (failedLoads)** | `src/lib/dashboard-inventory.ts:74-77, dan penanganan slug/cafe null di :47-56` | Kegagalan dicatat granular sebagai daftar string berbahasa Indonesia: "profil kafe" (slug null atau cafe tidak ditemukan), "data bahan" (query items error), "riwayat mutasi" (query movements error). Daftar ini yang dipakai UI untuk menyusun pesan error. | • Pasif; memicu tampilan error | `failedLoads kosong (normal)` · `hanya 'data bahan'` · `hanya 'riwayat mutasi'` · `keduanya` · `'profil kafe'` | Ubah sementara nama tabel di query movements jadi salah, muat halaman. Harus muncul pesan 'Terjadi kendala saat memuat riwayat mutasi.' bukan crash atau halaman kosong. | Sangat mudah disederhanakan jadi satu boolean hasError saat rebuild, sehingga pesan spesifik hilang dan user tidak tahu bagian mana yang bermasalah. |
| 10 | **Tampilan error muat inventory dengan pesan gabungan** | `src/components/dashboard/InventoryWorkspace.tsx:24-26, 98-118 (khususnya baris 113 failedLoads.join(" dan "))` | Kalau failedLoads tidak kosong, seluruh workspace diganti kartu error: ikon CircleAlert dalam kotak merah, judul 'Inventory belum dapat dimuat', dan kalimat 'Terjadi kendala saat memuat X dan Y. Coba muat ulang halaman.' Penggabungan pakai kata ' dan ', bukan koma. | • Pasif; user membaca dan diminta muat ulang halaman manual | `error satu sumber` · `error dua sumber` · `mode embedded (tanpa header)` · `mode standalone (dengan header Inventory)` | Paksa dua query gagal. Pesan harus berbunyi persis 'Terjadi kendala saat memuat data bahan dan riwayat mutasi.' | Ini adalah early-return sebelum semua UI lain. Rebuild yang menyusun ulang komponen sering menaruh error state sebagai banner kecil di atas tabel, mengubah perilaku: metrik dan tabel kosong ikut terender padahal seharusnya tidak. |
| 11 | **Error state punya varian embedded dan standalone** | `src/components/dashboard/InventoryWorkspace.tsx:98-106` | InventoryLoadError menyembunyikan header 'Inventory' saat embedded=true (karena dashboard utama sudah punya headernya sendiri), dan membungkus dengan container max-w-[1180px] p-5 lg:p-8 saat standalone. | • Pasif | `embedded true` · `embedded false` | Paksa error, cek /dashboard (embedded, tanpa judul besar 'Inventory') dan /dashboard/inventory (standalone, dengan judul). | Percabangan embedded ini halus dan hanya muncul di kondisi error yang jarang terjadi, jadi hampir pasti tidak diuji manual saat rebuild. |
| 12 | **Ringkasan stok: total, menipis, habis, nilai** | `src/lib/dashboard-inventory.ts:19-26 (summarizeInventory)` | Menghitung total = jumlah item; low = item dengan current_qty > 0 DAN current_qty <= minimum_qty; empty = current_qty <= 0; value = jumlah current_qty * estimated_unit_cost. Perhatikan low dan empty saling eksklusif, dan batas memakai <= bukan <. | • Pasif; ditampilkan sebagai 4 kartu KPI | `daftar kosong (semua 0)` · `qty tepat sama dengan minimum -> dihitung menipis` · `qty negatif -> dihitung habis` | Buat bahan dengan stok 5 dan minimum 5. Kartu 'Stok Menipis' harus bertambah 1, bukan 'Aman'. Lalu set stok 0; ia harus pindah ke 'Stok Habis' dan keluar dari 'Menipis'. | Batas <= vs < sangat mudah tertukar saat logika ditulis ulang di komponen. Salah satu tanda kurang tidak memunculkan error, hanya angka yang diam-diam salah. |
| 13 | **Prioritas bahan kritis untuk panel dashboard** | `src/lib/dashboard-inventory.ts:28-37 (criticalInventoryItems)` | Menyaring item dengan current_qty <= minimum_qty, lalu mengurutkan: item habis (<=0) selalu di depan, sisanya diurut berdasarkan selisih (current_qty - minimum_qty) menaik (paling defisit di atas), lalu dipotong maksimal 4. Memakai [...items] sehingga array asli tidak termutasi. | • Pasif; hasilnya tampil sebagai kartu di panel 'Bahan perlu dicek hari ini' | `tidak ada item kritis (panel disembunyikan)` · `1-4 item kritis` · `lebih dari 4 item kritis (dipotong)` | Jalankan tests/inventory-dashboard.test.ts kasus 'prioritizes empty and below-minimum items' yang mengharapkan urutan [empty, low-far, low-limited-out]. | Copy tanpa spread ([...items]) akan memutasi urutan array items yang dipakai tabel utama, membuat tabel tidak lagi urut A-Z. Bug ini muncul jauh dari tempat perubahan. |

### Kartu KPI & panel mode embedded — fitur 14–21

| # | Fitur | Lokasi | Apa yang terjadi | Interaksi | State | Cara verifikasi | Risiko kalau hilang |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 14 | **Empat kartu KPI inventory dengan warna aksen berbeda** | `src/components/dashboard/InventoryWorkspace.tsx:60-65` | Kartu: Total Bahan (ikon Boxes, oranye #FD5002), Stok Menipis (AlertTriangle, amber #F59E0B), Stok Habis (PackageX, merah #EF4444), Nilai Stok (Wallet, hijau #22D3A6 dengan prefix 'Rp '). Setiap ikon ukuran 15 strokeWidth 2, dan setiap warna punya pasangan background rgba 0.12. | • Hover kartu -> border menyala dan naik 2px (globals.css:464-466, khusus perangkat hover) | `nilai 0` · `nilai besar` · `daftar kosong` | Bandingkan hex warna dan ikon satu per satu dengan tampilan lama. Kartu 'Nilai Stok' harus menampilkan 'Rp' kecil sebelum angka. | Pemetaan warna-ke-makna (amber = menipis, merah = habis) ikut dipakai badge status tabel dan panel kritis. Kalau rebuild memakai token warna generik, konsistensi semantik lintas komponen pecah diam-diam. |
| 15 | **Animasi count-up angka KPI saat kartu masuk viewport** | `src/components/dashboard/system/DashboardMetric.tsx:18-51` | Setiap kartu memasang IntersectionObserver dengan threshold 0.4; begitu 40% kartu terlihat, angka menghitung naik dari 0 ke nilai target selama 900ms memakai easing ease-out quart (1 - (1-p)^4) lewat requestAnimationFrame, lalu observer di-disconnect saat unmount. | • Scroll sampai kartu terlihat -> angka menghitung naik<br>• Kartu di luar viewport -> angka tetap 0 sampai discroll | `belum terlihat (menampilkan 0)` · `sedang menghitung` · `selesai` · `prefers-reduced-motion (CSS tidak mematikan ini, animasi JS tetap jalan)` | Muat /dashboard/inventory, perhatikan keempat angka KPI menghitung naik dari 0, bukan langsung muncul. Scroll ke bawah lalu naik lagi tidak memicu ulang. | Efek ini murni JS dan mudah dianggap 'dekorasi'. Kalau DashboardMetric diganti kartu statis, angka langsung muncul dan halaman kehilangan karakter khasnya tanpa satu pun error. |
| 16 | **Format angka KPI locale id-ID dengan tabular-nums** | `src/components/dashboard/system/DashboardMetric.tsx:70-74` | Nilai dirender dengan n.toLocaleString("id-ID") sehingga 1250000 tampil 1.250.000 (titik sebagai pemisah ribuan, gaya Indonesia), ukuran 26px bold, kelas tabular-nums supaya lebar digit tetap saat angka berubah selama animasi count-up. | • Pasif | `angka kecil` · `angka jutaan` · `dengan prefix Rp` | Isi inventory sehingga Nilai Stok di atas satu juta. Harus tampil 'Rp 1.250.000' dengan titik, bukan '1,250,000' atau '1250000'. | Locale default runtime bisa en-US. Kalau toLocaleString dipanggil tanpa argumen locale di kode baru, hasilnya berubah jadi koma di sebagian environment saja, jadi lolos dari review lokal. |
| 17 | **Mode embedded workspace inventory di halaman dashboard utama** | `src/components/dashboard/InventoryWorkspace.tsx:31-32,37-43 dan pemakaiannya di src/app/dashboard/page.tsx:283-289` | Komponen yang sama dipakai dua kali. Saat embedded: dibungkus kelas animasi 'dash-reveal dash-d5', header memakai layout flex lg:flex-row justify-between, judul mengecil jadi text-xl. Saat standalone: header mb-7 dengan 'dash-reveal' dan judul text-2xl. | • Buka /dashboard -> versi embedded<br>• Buka /dashboard/inventory -> versi standalone | `embedded` · `standalone` | Buka kedua halaman berdampingan. Ukuran judul, jarak bawah header, dan keberadaan tombol 'Buka fokus inventory' harus berbeda persis seperti sekarang. | Rebuild yang hanya menguji /dashboard/inventory akan merusak tampilan embedded di halaman utama dashboard, dan sebaliknya. Satu komponen, dua kontrak visual. |
| 18 | **Tombol 'Buka fokus inventory' hanya di mode embedded** | `src/components/dashboard/InventoryWorkspace.tsx:49-57` | Link Next.js ke /dashboard/inventory yang hanya dirender saat embedded=true, dengan style latar #132136 dan border tipis. Sebagai Link, Next.js melakukan prefetch rute inventory saat link masuk viewport. | • Klik -> navigasi client-side ke halaman inventory penuh<br>• Hover -> efek dash-btn brightness 1.08<br>• Tekan -> scale 0.97 | `hanya ada saat embedded` | Di /dashboard, tombol harus ada di kanan header inventory dan mengarah ke /dashboard/inventory. Di /dashboard/inventory, tombol harus tidak ada. | Ini satu-satunya jalur navigasi kontekstual dari dashboard ke inventory. Kalau hilang, user harus lewat sidebar dan tidak akan melaporkannya sebagai bug. |
| 19 | **Panel 'Bahan perlu dicek hari ini' (khusus embedded)** | `src/components/dashboard/InventoryWorkspace.tsx:67-91` | Hanya muncul kalau embedded=true DAN ada item kritis. Kartu dengan latar gradient 135deg dari rgba(253,80,2,0.14) ke rgba(13,24,41,0.92) dan border oranye, berisi ikon AlertTriangle, judul, lalu grid kartu kecil (md:2 kolom, xl:4 kolom) berisi nama bahan dan sisa stok. | • Hover nama bahan -> tooltip native title berisi nama lengkap (untuk nama yang ter-truncate) | `tidak ada item kritis -> panel disembunyikan total` · `ada 1-4 item kritis` · `standalone -> tidak pernah muncul` | Set satu bahan ke stok 0, buka /dashboard. Panel gradient oranye harus muncul di bawah kartu KPI. Set semua bahan aman, panel harus lenyap sepenuhnya (bukan panel kosong). | Kondisi ganda (embedded && critical.length > 0) mudah disederhanakan jadi satu kondisi saja, membuat panel bocor ke halaman inventory penuh atau muncul kosong. |
| 20 | **Pewarnaan teks sisa stok pada kartu kritis** | `src/components/dashboard/InventoryWorkspace.tsx:84-86` | Baris '{stok} tersisa, minimum {minimum}' diwarnai merah muda #FCA5A5 jika current_qty <= 0, dan amber #F59E0B jika hanya menipis. Memakai tabular-nums dan formatQty untuk kedua angka. | • Pasif | `habis (merah)` · `menipis (amber)` | Siapkan satu bahan stok 0 dan satu bahan stok 1 minimum 5, buka /dashboard. Warna teks kedua kartu harus berbeda. | Perbedaan warna ini satu-satunya pembeda visual antara habis dan menipis di panel ini (tidak ada label teks). Kalau warnanya diseragamkan, informasi hilang tanpa jejak. |
| 21 | **Animasi reveal bertahap panel inventory** | `src/app/globals.css:502-511 (.dash-reveal, .dash-d1, .dash-d2, .dash-d5), dipakai di InventoryWorkspace.tsx:31,32,102 dan InventoryTable.tsx:134,227` | Elemen naik 10px sambil fade-in selama 0.28s. Delay berjenjang: tabel bahan .dash-d1 (0.03s), panel mutasi .dash-d2 (0.06s), workspace embedded .dash-d5 (0.15s), menciptakan urutan masuk yang berurutan bukan serentak. | • Pasif; terlihat saat halaman pertama kali render | `normal` · `prefers-reduced-motion -> animasi dimatikan total dan opacity dipaksa 1 (globals.css:521)` | Muat ulang halaman dan perhatikan tabel muncul sedikit lebih dulu dari panel mutasi. Lalu aktifkan reduced motion; keduanya harus langsung terlihat tanpa gerakan dan tanpa elemen yang tetap transparan. | Kalau kelas dash-reveal diganti animasi library lain, aturan prefers-reduced-motion di globals.css:521 tidak lagi mengenainya dan aksesibilitas gerakan hilang. Lebih buruk: kalau kelas dipertahankan tapi elemen tidak pernah dianimasikan, opacity bisa tersangkut 0 dan panel tak terlihat. |

### Umpan balik aksi, manajemen fokus & modal — fitur 22–30

| # | Fitur | Lokasi | Apa yang terjadi | Interaksi | State | Cara verifikasi | Risiko kalau hilang |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 22 | **Toast konfirmasi aksi inventory** | `src/components/dashboard/InventoryTable.tsx:112-120` | Setelah aksi berhasil, muncul kotak fixed di kanan atas (right-4 top-4, z-50, max-w-sm) dengan ikon CheckCircle2 hijau, teks pesan, dan tombol X untuk menutup. Border hijau rgba(34,211,166,0.35). Toast tidak hilang otomatis; harus ditutup manual atau tergantikan pesan berikutnya. | • Simpan bahan/atur stok -> toast muncul<br>• Klik tombol X -> toast hilang (setNotice(""))<br>• Hover tombol X -> title tooltip 'Tutup' | `tidak ada notice (tersembunyi)` · `notice aktif` · `notice tergantikan pesan baru` | Tambah bahan baru. Toast hijau harus muncul di kanan atas dan tetap di sana sampai diklik X. Pastikan tidak ada auto-dismiss timer di implementasi baru kecuali memang diinginkan. | Mengganti dengan library toast (sonner) mengubah perilaku: toast jadi hilang otomatis, posisinya bisa di luar dash-portal-root sehingga token warna dashboard tidak terbawa (lihat system/portal.ts:1-4). |
| 23 | **Live region sr-only untuk pengumuman aksi** | `src/components/dashboard/InventoryTable.tsx:108-110` | Div terpisah dengan aria-live="polite" aria-atomic="true" className="sr-only" yang selalu ada di DOM dan berisi teks notice. Ini terpisah dari toast visual, sehingga screen reader membaca perubahan tanpa bergantung pada elemen visual muncul/hilang. | • Screen reader membacakan pesan setelah aksi berhasil | `kosong` · `berisi pesan` | Inspect DOM: div sr-only dengan aria-live harus selalu ada (bukan dirender kondisional), dan isinya berubah setelah simpan. | Sangat mudah dianggap duplikat toast dan dihapus. Padahal kalau live region dirender kondisional bersama toast, screen reader sering tidak mengumumkannya karena elemen baru dimasukkan bersamaan dengan isinya. |
| 24 | **Pesan spesifik per jenis aksi inventory** | `src/components/dashboard/InventoryTable.tsx:68-72 (inventoryActionMessage)` | Tiga kalimat berbeda: create -> '{nama} berhasil ditambahkan ke inventory.', edit -> '{nama} berhasil diperbarui.', adjust -> 'Stok {nama} berhasil disesuaikan.' Nama bahan selalu disisipkan. | • Pasif; hasil dari setiap aksi | `create` · `edit` · `adjust` | Jalankan tests/inventory-dashboard.test.ts blok 'inventory action feedback' yang menguji ketiga string persis. Fungsi ini diekspor dan diuji, jadi harus tetap diekspor dengan nama sama. | Fungsi ini diekspor dari file komponen semata-mata untuk diuji. Rebuild yang meng-inline string ke dalam JSX akan mematikan test suite, dan kalau test ikut dihapus, perbedaan kalimat lolos begitu saja. |
| 25 | **Refresh data server setelah aksi berhasil** | `src/components/dashboard/InventoryTable.tsx:95-99 (finishAction memanggil router.refresh())` | Setelah modal ditutup dan notice diset, router.refresh() memicu re-render server component sehingga tabel, kartu KPI, dan panel mutasi ikut terbarui tanpa reload halaman penuh. Tidak ada optimistic update; UI menunggu data server. | • Simpan perubahan -> beberapa saat kemudian angka KPI dan baris tabel ikut berubah | `refresh berjalan` · `refresh selesai` · `refresh gagal (data lama tetap tampil)` | Tambah bahan baru. Tanpa menekan F5, baris baru harus muncul di tabel dan angka 'Total Bahan' bertambah (kartu KPI akan menghitung ulang). | Kombinasi revalidatePath di server action + router.refresh() di klien saling melengkapi. Kalau salah satu dihilangkan saat rebuild, data terlihat 'tidak berubah setelah simpan' hanya di sebagian kasus, sangat sulit direproduksi. |
| 26 | **Pengembalian fokus ke tombol pemicu setelah modal ditutup** | `src/components/dashboard/InventoryTable.tsx:83-104 (returnFocusRef, restoreFocus, openModal, closeModal)` | openModal menyimpan element tombol pemicu (event.currentTarget) ke ref. Saat modal ditutup, restoreFocus memanggil requestAnimationFrame lalu .focus() pada element itu. Radix onCloseAutoFocus sengaja di-preventDefault (baris 292) supaya tidak bentrok dengan mekanisme manual ini. | • Klik ikon 'Atur stok' pada baris ke-5, tutup modal -> fokus kembali ke ikon baris ke-5<br>• Tekan Escape untuk menutup -> fokus tetap kembali ke tombol asal<br>• Simpan lalu modal tertutup otomatis -> fokus kembali | `modal ditutup via X` · `via Escape` · `via klik overlay` · `via tombol Batal` · `via simpan berhasil` | Navigasi hanya dengan keyboard: Tab ke tombol Ubah pada baris tertentu, Enter, lalu Escape. Fokus harus kembali persis ke tombol Ubah baris itu, bukan lompat ke awal halaman. | Ada tiga bagian yang harus tetap sinkron: penyimpanan ref, preventDefault pada onCloseAutoFocus, dan requestAnimationFrame. Menghapus salah satunya membuat fokus lompat ke <body>, dan pengguna keyboard kehilangan posisi tanpa gejala visual bagi pengguna mouse. |
| 27 | **Autofokus kustom ke field pertama modal** | `src/components/dashboard/InventoryTable.tsx:288-291 (input[name='name']) dan src/components/dashboard/StockAdjustmentModal.tsx:56-59 (input[name='quantity'])` | onOpenAutoFocus di-preventDefault, lalu querySelector mencari field spesifik dan memanggil .focus(). Modal bahan fokus ke input nama; modal stok fokus ke input jumlah (melewati select mode). Form bahan juga punya autoFocus pada input name sebagai lapis kedua (InventoryItemForm.tsx:46). | • Buka modal Tambah Bahan -> kursor langsung di kolom Nama Bahan<br>• Buka modal Atur Stok -> kursor langsung di kolom Jumlah, bukan di dropdown Jenis Penyesuaian | `modal create` · `modal edit` · `modal adjust` | Buka modal Atur Stok lalu langsung ketik angka tanpa klik apa pun. Angka harus masuk ke field Jumlah. | Kalau preventDefault dilepas, Radix memfokuskan elemen fokusable pertama, yang di modal stok adalah tombol X atau select mode. User harus klik dulu sebelum mengetik, kerugian kecepatan yang tidak terlihat sebagai bug. |
| 28 | **Modal dirender ke portal dashboard** | `src/components/dashboard/InventoryTable.tsx:284 dan StockAdjustmentModal.tsx:53 (container={getDashPortal() ?? undefined}), definisi di src/components/dashboard/system/portal.ts:5-10` | Kedua dialog dipasang ke elemen #dash-portal-root, bukan document.body, supaya token CSS .dash-root (warna, font dashboard) ikut terbawa ke dalam modal. Ada fallback ?? undefined kalau elemen belum ada. | • Buka modal apa pun -> tampil dengan warna gelap dashboard, bukan tema default | `portal ada` · `portal belum ter-mount (fallback ke body)` | Buka modal, inspect DOM: konten dialog harus berada di dalam #dash-portal-root. Warna latar modal harus mengikuti var(--dash-raised), bukan putih. | Ini aturan arsitektur lintas komponen yang tidak terlihat dari kode komponen inventory saja. Modal baru yang lupa container akan tampil dengan tema salah, dan mungkin baru ketahuan setelah rilis. |
| 29 | **Tombol tutup kustom pada modal (showCloseButton=false)** | `src/components/dashboard/InventoryTable.tsx:283,296-298 dan StockAdjustmentModal.tsx:54,71-73` | Tombol X bawaan DialogContent dimatikan (showCloseButton={false}), diganti tombol sendiri di dalam header dengan kelas dash-icon-btn, aria-label spesifik ('Tutup formulir bahan' / 'Tutup pengaturan stok') dan title='Tutup'. | • Klik X -> modal tertutup dan fokus dikembalikan<br>• Hover X -> tooltip 'Tutup' + latar menyala (globals.css:485-486) | `modal bahan` · `modal stok` | Buka kedua modal, arahkan screen reader ke tombol X. Label harus berbeda antar modal ('formulir bahan' vs 'pengaturan stok'), bukan sekadar 'Close'. | Mengembalikan tombol bawaan terlihat lebih rapi tapi kehilangan aria-label berbahasa Indonesia yang spesifik, dan posisinya absolute top-right yang bisa menabrak judul panjang. |
| 30 | **Penutupan modal lewat Escape dan klik overlay** | `src/components/dashboard/InventoryTable.tsx:281 dan StockAdjustmentModal.tsx:50 (onOpenChange), overlay di src/components/ui/dialog.tsx:34-48` | Radix Dialog menyediakan Escape-to-close, klik overlay untuk menutup, focus trap di dalam modal, dan penguncian scroll body. Overlay memakai bg-black/10 dengan backdrop-blur-xs (kalau didukung) dan animasi fade 100ms; konten memakai zoom-in-95/zoom-out-95. | • Tekan Escape -> modal tertutup<br>• Klik area gelap di luar modal -> tertutup<br>• Tab di dalam modal -> fokus berputar di dalam, tidak bocor ke halaman belakang<br>• Scroll saat modal terbuka -> halaman di belakang terkunci | `terbuka` · `menutup (animasi keluar)` · `tertutup` | Buka modal, tekan Tab berulang kali sampai lebih dari jumlah field. Fokus tidak boleh keluar ke tabel di belakang. Lalu tekan Escape; modal harus tertutup dan halaman bisa discroll lagi. | Kalau modal dibangun ulang dengan div kustom, semua perilaku ini (focus trap, scroll lock, Escape, animasi keluar) hilang sekaligus. Ini kelihatan 'sama saja' di screenshot tapi rusak total untuk pengguna keyboard. |

### Tabel bahan & panel riwayat mutasi — fitur 31–46

| # | Fitur | Lokasi | Apa yang terjadi | Interaksi | State | Cara verifikasi | Risiko kalau hilang |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 31 | **Header daftar bahan dengan penghitung item** | `src/components/dashboard/InventoryTable.tsx:135-144` | Header panel berisi judul 'Daftar Bahan' (id inventory-list-title, dirujuk aria-labelledby di section) dan subteks dinamis '{items.length} bahan terdaftar', ditambah tombol oranye 'Tambah Bahan' dengan ikon Plus yang shrink-0 dan whitespace-nowrap supaya tidak terpotong di layar sempit. | • Klik 'Tambah Bahan' -> modal create terbuka, tombol ini disimpan sebagai target restore fokus | `0 bahan` · `banyak bahan` · `judul panjang (min-w-0 mencegah tombol terdorong keluar)` | Perkecil lebar jendela sampai mobile. Tombol 'Tambah Bahan' harus tetap utuh satu baris dan tidak menyusut, sementara teks judul yang mengalah. | Kombinasi min-w-0 pada teks + shrink-0 pada tombol adalah perbaikan layout spesifik. Tanpa itu, tombol pecah dua baris di layar kecil, yang hanya terlihat kalau diuji responsif. |
| 32 | **Empty state daftar bahan dengan CTA kedua** | `src/components/dashboard/InventoryTable.tsx:146-157` | Saat items kosong, area tabel diganti blok min-h-80 berisi ikon PackageSearch (23px, strokeWidth 1.5) dalam kotak #132136, teks 'Belum ada bahan', penjelasan 'Tambahkan bahan pertama untuk mulai memantau stok kafe.', dan tombol 'Tambah Bahan' kedua di tengah (ukuran lebih besar: px-4 py-2.5). | • Klik tombol CTA di tengah -> modal create terbuka, fokus dikembalikan ke tombol ini setelah ditutup | `items kosong` | Pakai kafe tanpa bahan sama sekali. Harus ada dua tombol 'Tambah Bahan' (satu di header, satu di tengah empty state), dan keduanya berfungsi. | Empty state jarang dilihat developer yang datanya sudah terisi. Rebuild sering hanya menyisakan tabel kosong dengan header kolom, kehilangan onboarding untuk kafe baru. |
| 33 | **Hint dan navigasi keyboard scroll horizontal tabel** | `src/components/dashboard/InventoryTable.tsx:160-176 dan fungsi tableHorizontalScrollDelta di :62-66` | Baris hint dengan ikon ArrowLeftRight bertuliskan 'Geser tabel atau gunakan tombol panah untuk melihat kolom lain.' (id inventory-table-scroll-hint). Container scroll punya role="region", tabIndex=0, aria-label="Tabel inventaris", aria-describedby merujuk hint tersebut, dan handler onKeyDown: ArrowLeft menggeser -240px, ArrowRight +240px, tombol lain diabaikan (delta 0, tanpa preventDefault sehingga scroll vertikal normal tetap jalan). behavior: "auto" (bukan smooth). | • Tab ke tabel -> muncul ring fokus oranye di dalam (focus-visible:ring-2 ring-inset ring-[#FD5002])<br>• Tekan panah kiri/kanan -> tabel bergeser 240px<br>• Tekan panah atas/bawah -> halaman scroll normal, tidak dicegat<br>• Geser dengan trackpad/sentuhan -> overflow-x-auto biasa | `tabel muat penuh (tidak perlu scroll)` · `tabel lebih lebar dari layar` · `fokus keyboard aktif` | Jalankan tests/inventory-dashboard.test.ts blok 'inventory table keyboard scrolling' (-240 / +240 / 0). Lalu manual: perkecil jendela, Tab ke tabel, tekan panah kanan; tabel harus bergeser dan halaman tidak ikut scroll. | Ini fitur aksesibilitas yang hampir tidak pernah dipakai dengan mouse, jadi hilangnya tidak akan dilaporkan. Fungsi tableHorizontalScrollDelta juga diekspor khusus untuk diuji; meng-inline-nya mematikan test. |
| 34 | **Struktur tabel bahan enam kolom dengan lebar minimum** | `src/components/dashboard/InventoryTable.tsx:177-186` | Tabel w-full min-w-[760px] (memaksa scroll horizontal di layar sempit). Header: Bahan, Stok, Minimum, Harga / Unit, Status, Aksi. Kolom ke-6 (index 5) rata kanan, sisanya rata kiri. Gaya header: 11px, uppercase, tracking-wider, warna #5A7898. | • Pasif; header tidak bisa diklik (tidak ada sort kolom) | `ada data` · `hanya satu baris` | Hitung dan cocokkan keenam judul kolom persis, termasuk spasi pada 'Harga / Unit'. Pastikan min-w-[760px] masih ada sehingga hint scroll punya alasan. | Kalau min-w dihapus demi 'responsif', kolom akan gepeng dan hint scroll jadi bohong. Kalau ditambah sort kolom, urutan A-Z dari server bisa bentrok. |
| 35 | **Sel nama bahan dengan truncate dan tooltip catatan** | `src/components/dashboard/InventoryTable.tsx:194-197` | Sel pertama dibatasi max-w-[230px]. Nama dipotong dengan truncate dan diberi atribut title berisi nama lengkap. Kalau item punya notes, baris kedua kecil (11px, #5A7898) juga di-truncate dengan title-nya sendiri. Kalau notes kosong, baris kedua tidak dirender sama sekali (bukan baris kosong). | • Hover nama panjang -> tooltip native menampilkan teks penuh<br>• Hover catatan panjang -> tooltip catatan penuh | `tanpa catatan` · `dengan catatan pendek` · `dengan catatan sangat panjang` · `nama sangat panjang` | Buat bahan dengan nama 60 karakter dan catatan 100 karakter. Keduanya harus terpotong dengan elipsis, dan hover masing-masing memunculkan tooltip berisi teks penuh. | Atribut title adalah satu-satunya cara membaca teks penuh di tabel ini. Rebuild yang mengganti truncate dengan line-clamp atau melebarkan kolom biasanya lupa menyalin title, dan informasi jadi tidak bisa diakses sama sekali. |
| 36 | **Format kuantitas dengan satuan (formatQty)** | `src/lib/inventory.ts:36-41, dipakai di InventoryTable.tsx:198,199,242`<br>`InventoryWorkspace.tsx:85`<br>`StockAdjustmentModal.tsx:68`<br>`RecipeEditor.tsx:173,175` | Number(value).toLocaleString("en-US", { maximumFractionDigits: 3 }) lalu ditambah spasi dan satuan. Perhatikan locale-nya en-US (bukan id-ID), jadi 1500 gram tampil '1,500 gram' dengan koma, dan desimal memakai titik: 1.5 kg. Trailing zero dibuang: 2.250 jadi '2.25'. | • Pasif; muncul di 6 tempat berbeda | `bilangan bulat` · `desimal 1-3 angka` · `lebih dari 3 desimal (dibulatkan tampilan)` · `nilai 0` · `ribuan` | Jalankan tests/inventory.test.ts blok formatQty ('200 ml', '1.5 kg', '2.25 liter'). Lalu manual: buat bahan 1500 gram; tabel harus menampilkan '1,500 gram' dengan koma. | Locale en-US di sini tidak konsisten dengan formatRupiah dan KPI yang memakai id-ID. Siapa pun yang 'merapikan' locale saat rebuild akan mengubah tampilan seluruh angka stok, dan test inventory.test.ts akan gagal. Perbedaan ini harus disengaja dipertahankan atau diubah secara sadar di semua enam pemanggil sekaligus. |
| 37 | **Format harga per unit Rupiah** | `src/components/dashboard/InventoryTable.tsx:200, implementasi di src/lib/format.ts:14-21` | formatRupiah memakai Intl.NumberFormat id-ID style currency IDR dengan minimum dan maximumFractionDigits 0, menghasilkan 'Rp 5.000' tanpa desimal. Sel memakai tabular-nums dan font-semibold. | • Pasif | `harga 0` · `harga besar` · `harga desimal dari DB (dibulatkan tampilan)` | Isi harga 5000 pada sebuah bahan. Kolom Harga / Unit harus menampilkan 'Rp 5.000' (titik ribuan, tanpa ',00'). | Mengganti dengan template string manual atau locale berbeda menghasilkan 'IDR 5,000.00'. Terlihat sepele tapi merusak konsistensi seluruh dashboard. |
| 38 | **Badge status stok tiga varian** | `src/components/dashboard/InventoryTable.tsx:36-40 (konstanta STATUS) dan :201-206, logika di src/lib/inventory.ts:30-34` | inventoryStatus mengembalikan 'empty' jika current_qty <= 0, 'low' jika current_qty <= minimum_qty, selain itu 'safe'. Badge pill menampilkan ikon + label: Aman (CheckCircle2, #22D3A6), Menipis (TriangleAlert, #F59E0B), Habis (XCircle, #EF4444), masing-masing dengan background rgba 0.12 warna yang sama. Ikon ukuran 13 dan aria-hidden. | • Pasif | `safe` · `low` · `empty` · `qty tepat = minimum -> low` · `qty negatif -> empty` | Jalankan tests/inventory.test.ts blok inventoryStatus. Manual: bahan dengan stok 5 minimum 5 harus berbadge 'Menipis' (bukan 'Aman'). | Ikon dan warna badge harus konsisten dengan kartu KPI dan RecipeEditor yang memakai palet sama tapi didefinisikan terpisah (RecipeEditor.tsx:165-169 memakai #FCA5A5 untuk empty, bukan #EF4444). Perbedaan halus ini nyata di kode dan harus dipertahankan atau disatukan secara sadar. |
| 39 | **Tombol aksi per baris dengan label kontekstual** | `src/components/dashboard/InventoryTable.tsx:207-216` | Dua tombol ikon di kolom Aksi: PackagePlus untuk 'Atur stok {nama}' (title 'Atur stok') dan Pencil untuk 'Ubah bahan {nama}' (title 'Ubah bahan'). Keduanya latar #132136, warna #9FB6D1, kelas dash-icon-btn dash-press, ukuran ikon 15. aria-label menyertakan nama bahan sehingga screen reader bisa membedakan antar baris. | • Klik PackagePlus -> modal Atur Stok untuk baris itu<br>• Klik Pencil -> modal Ubah Bahan terisi data baris itu<br>• Hover -> latar menyala + tooltip<br>• Tekan -> scale 0.96 (dash-press) | `setiap baris punya sepasang tombol` · `tidak ada tombol hapus bahan di UI ini` | Pakai screen reader atau inspect: aria-label tombol pada baris 'Tomat' harus berbunyi 'Atur stok Tomat', bukan hanya 'Atur stok'. | aria-label generik adalah default yang paling sering dipakai saat rebuild. Dengan 30 baris, screen reader membacakan 30 tombol identik dan tabel jadi tidak bisa dipakai. Catatan tambahan: tidak ada aksi hapus bahan di UI ini, jadi jangan menambahkannya tanpa diminta. |
| 40 | **Highlight baris tabel saat hover (khusus perangkat berpenunjuk)** | `src/components/dashboard/InventoryTable.tsx:193 (kelas dash-row) dan src/app/globals.css:469-470` | Kelas .dash-row memberi transisi background 0.15s, dan aturan hover dibungkus @media (hover: hover) sehingga perangkat sentuh tidak mengalami hover yang tersangkut setelah tap. Border bawah dirender kondisional: baris terakhir tidak diberi border (index < items.length - 1). | • Hover baris di desktop -> latar rgba(255,255,255,0.035)<br>• Tap baris di ponsel -> tidak ada hover yang menempel | `baris tengah (ada border)` · `baris terakhir (tanpa border)` · `perangkat sentuh` | Hover baris di desktop harus menyala. Emulasikan perangkat sentuh di devtools, tap baris; latar tidak boleh tetap menyala setelah jari diangkat. Cek juga baris terakhir tidak punya garis bawah ganda dengan tepi kartu. | Pembungkus @media (hover: hover) mudah hilang saat CSS ditulis ulang dengan utility hover: Tailwind polos, memunculkan bug hover-menempel di ponsel yang jarang diuji. |
| 41 | **Panel Mutasi Terbaru dengan empty state** | `src/components/dashboard/InventoryTable.tsx:227-238` | Aside lebar tetap 320px pada layar lg (grid lg:grid-cols-[minmax(0,1fr)_320px]) dengan judul 'Mutasi Terbaru' (id movement-title, aria-labelledby) dan subteks '12 aktivitas terakhir'. Kalau kosong: blok min-h-48 dengan ikon PackageSearch, teks 'Belum ada mutasi' dan 'Penyesuaian stok akan tercatat di sini.' | • Pasif; panel hanya menampilkan riwayat | `0 mutasi (empty state)` · `ada mutasi` · `layar kecil (panel turun ke bawah tabel)` | Pada kafe baru tanpa penyesuaian, panel kanan harus menampilkan empty state, bukan kotak kosong. Perkecil jendela di bawah breakpoint lg; panel harus pindah ke bawah tabel dengan lebar penuh. | Lebar tetap 320px dan grid minmax(0,1fr) mencegah tabel melebar tak terkendali. Grid yang ditulis ulang tanpa minmax(0,1fr) membuat tabel min-w-760px mendorong layout dan halaman bisa scroll horizontal seluruhnya. |
| 42 | **Ikon dan label per jenis mutasi** | `src/components/dashboard/InventoryTable.tsx:49-60 (movementTypeLabel), :74-78 (movementIcon), dipakai di :241,254` | Empat jenis mutasi diterjemahkan: manual_add -> 'Tambah stok' (ikon ArrowUpFromLine), manual_subtract -> 'Kurangi stok' (ArrowDownToLine), manual_set -> 'Set stok' (Equal), order_deduction -> 'Pengurangan pesanan' (ArrowDownToLine sebagai fallback). Ikon dalam kotak 7x7 rounded #132136. | • Pasif | `keempat jenis mutasi` · `order_deduction datang otomatis dari pesanan pelanggan, bukan dari UI ini` | Jalankan tests/inventory-dashboard.test.ts blok 'inventory dashboard movement labels' yang menguji keempat string. Manual: lakukan satu tambah, satu kurang, satu set; ketiganya harus berbeda label dan ikon. | Jenis 'order_deduction' hanya muncul kalau ada pesanan yang memotong stok lewat resep, jadi hampir tidak pernah terlihat saat pengembangan lokal. Sangat mungkin terlewat dan menampilkan label kosong atau undefined. |
| 43 | **Tanda dan warna delta kuantitas mutasi** | `src/components/dashboard/InventoryTable.tsx:242, 251` | Nilai delta diformat dengan tanda eksplisit: '+' jika delta_qty > 0, '-' jika < 0, tanpa tanda jika tepat 0 (kasus manual_set yang tidak mengubah jumlah). Nilai absolutnya diformat lewat formatQty dengan satuan dari record mutasi (bukan dari item, sehingga riwayat tetap benar walau satuan bahan diubah kemudian). Warna: merah #FCA5A5 untuk negatif, hijau #22D3A6 untuk nol dan positif. Memakai tabular-nums dan shrink-0. | • Pasif | `delta positif` · `delta negatif` · `delta tepat 0` · `satuan bahan berubah setelah mutasi tercatat` | Lakukan 'Set jumlah persis' ke nilai yang sama dengan stok sekarang. Entri mutasi harus muncul tanpa tanda + atau -, berwarna hijau. | Kasus delta 0 (tanpa tanda) adalah cabang ketiga yang mudah hilang kalau ditulis sebagai ternary dua cabang, menghasilkan '+0 gram'. Selain itu memakai movement.unit alih-alih item.unit adalah keputusan sengaja agar riwayat historis akurat. |
| 44 | **Format tanggal mutasi dan elemen time semantik** | `src/components/dashboard/InventoryTable.tsx:42-47 (Intl.DateTimeFormat) dan :253-255` | Formatter dibuat sekali di module scope (bukan per render) dengan locale id-ID, day 2-digit, month short, hour 2-digit, minute 2-digit, menghasilkan format seperti '19 Jul 14.05'. Dibungkus <time dateTime={movement.created_at}> sehingga nilai ISO asli tetap tersedia di DOM. Tidak ada timeZone eksplisit, jadi mengikuti zona waktu browser pengguna. | • Pasif; hover tidak memunculkan tanggal lengkap | `hari ini` · `bulan lalu` · `tahun berbeda (tahun tidak ditampilkan)` | Inspect elemen <time>; atribut dateTime harus berisi timestamp ISO penuh sementara teksnya format pendek Indonesia. Pastikan bulan tampil singkat berbahasa Indonesia (Jul, Agu, Des), bukan Jul/Aug/Dec. | Dua hal mudah hilang: pembuatan formatter di module scope (kalau dipindah ke dalam komponen, objek Intl dibuat ulang setiap render untuk 12 baris) dan elemen <time dateTime>. Juga: tahun tidak ditampilkan, sehingga mutasi tahun lalu terlihat sama dengan tahun ini. Ini perilaku existing yang harus dipertahankan kecuali diminta berubah. |
| 45 | **Catatan mutasi dengan pembatasan dua baris** | `src/components/dashboard/InventoryTable.tsx:256` | Kalau movement.note ada, ditampilkan dengan line-clamp-2 (maksimal dua baris, sisanya elipsis) ukuran 11px warna #9FB6D1. Kalau null, baris ini tidak dirender. | • Pasif; tidak ada tombol 'lihat selengkapnya' dan tidak ada tooltip | `tanpa catatan` · `catatan pendek` · `catatan panjang (terpotong 2 baris)` | Lakukan penyesuaian stok dengan catatan sangat panjang. Di panel mutasi catatan harus terpotong tepat di baris kedua, tidak memanjangkan kartu. | line-clamp bergantung pada plugin/utility Tailwind yang aktif. Kalau hilang, satu catatan panjang bisa membuat kartu mutasi setinggi setengah layar dan merusak panel. |
| 46 | **Fallback nama bahan yang sudah dihapus di riwayat mutasi** | `src/components/dashboard/InventoryTable.tsx:250 (movement.inventory_item?.name ?? "Bahan dihapus")` | Kalau join ke Inventory_Items mengembalikan null (bahan sudah dihapus dari database), riwayat tetap ditampilkan dengan teks 'Bahan dihapus' alih-alih kosong atau crash. Atribut title juga memakai nilai opsional yang sama. | • Pasif | `bahan masih ada` · `bahan sudah dihapus` | Hapus sebuah Inventory_Items langsung di Supabase yang punya riwayat mutasi, lalu muat halaman. Entri mutasi harus menampilkan 'Bahan dihapus', bukan baris kosong atau error render. | Optional chaining plus nullish fallback ini hanya penting di kasus langka. Rebuild yang menulis movement.inventory_item.name akan meledak dengan runtime error yang membuat seluruh halaman inventory blank, dan baru ketahuan di produksi. |

### Form bahan & modal Atur Stok — fitur 47–54

| # | Fitur | Lokasi | Apa yang terjadi | Interaksi | State | Cara verifikasi | Risiko kalau hilang |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 47 | **Form bahan: field, satuan terbatas, dan aturan input numerik** | `src/components/dashboard/InventoryItemForm.tsx:42-77, daftar satuan di src/types/index.ts:172` | Lima field: Nama Bahan (required, autoFocus, placeholder 'Sirup Lemon'), Satuan (select dari INVENTORY_UNITS = gram, kg, ml, liter, pcs, pack, botol; default 'gram'), Harga / Unit (number, min 0, step 1, inputMode numeric), Stok Saat Ini (number, min 0, step 0.001, inputMode decimal), Batas Menipis (sama), dan Catatan (textarea rows 3, resize-none). Semua memakai kelas dash-input dan dashInputStyle. | • Ketik nama -> required memblokir submit kalau kosong<br>• Pilih satuan dari 7 opsi<br>• Isi angka di ponsel -> keyboard numerik/desimal muncul karena inputMode<br>• Coba isi negatif -> min=0 memblokir di browser<br>• Fokus field -> border oranye + ring rgba(253,80,2,0.13) (globals.css:480-481) | `mode tambah (semua default: qty 0, cost 0, unit gram)` · `mode ubah (terisi nilai item)` · `catatan null -> string kosong` | Buka modal Tambah Bahan di ponsel/emulator; field Stok harus memunculkan keyboard desimal, field Harga keyboard numerik. Cek dropdown satuan berisi tepat 7 pilihan dengan urutan gram, kg, ml, liter, pcs, pack, botol. | step="0.001" pada qty vs step="1" pada harga adalah perbedaan sengaja (harga dibulatkan di server lewat Math.round). Menyeragamkan step akan membuat browser menolak input desimal stok yang valid. inputMode juga tidak terlihat sama sekali di desktop. |
| 48 | **Status pending pada form bahan** | `src/components/dashboard/InventoryItemForm.tsx:19,35,78-81` | useTransition memberi flag pending yang: mengeset aria-busy pada form, men-disable tombol simpan, mengganti ikon Save dengan Loader2 beranimasi spin, mengganti label 'Simpan Bahan' menjadi 'Menyimpan', menurunkan opacity ke 0.7, dan memberi disabled:cursor-not-allowed. | • Klik Simpan -> tombol berubah jadi spinner + 'Menyimpan' dan tidak bisa diklik ulang<br>• Klik ganda cepat -> hanya satu submit terkirim | `idle` · `pending` · `sukses (modal tertutup)` · `gagal (kembali idle dengan error)` | Throttle jaringan ke Slow 3G, klik Simpan. Tombol harus menampilkan spinner dan label 'Menyimpan', dan klik kedua tidak boleh membuat data ganda. | Ini satu-satunya penjaga double-submit di form ini. Kalau pending state disederhanakan jadi hanya spinner tanpa disabled, koneksi lambat bisa menghasilkan dua bahan duplikat. |
| 49 | **Penanganan error inline pada form bahan** | `src/components/dashboard/InventoryItemForm.tsx:18,24-28,36-41` | Error dari server action ditampilkan di atas form dalam kotak role="alert" latar rgba(239,68,68,0.1) teks #FCA5A5 dengan ikon AlertCircle. Error direset ke string kosong setiap kali submit dimulai. Saat error, onDone tidak dipanggil sehingga modal tetap terbuka dan data yang sudah diketik tidak hilang. | • Submit dengan satuan tidak valid -> pesan muncul, modal tetap terbuka, isian dipertahankan<br>• Submit ulang -> pesan lama hilang dulu | `tanpa error` · `error validasi ('Nama bahan wajib diisi.', 'Satuan bahan tidak valid.', 'Angka inventory tidak valid.')` · `error sesi ('Sesi tidak valid. Masuk ulang.')` · `error database (pesan mentah dari Supabase)` | Buka devtools, ubah value option satuan jadi nilai asing lewat DOM, submit. Kotak merah harus muncul di dalam modal dan isian form tidak boleh ter-reset. | Kalau modal ditutup pada saat error (misalnya karena finishAction dipanggil tanpa mengecek result.error), user kehilangan semua ketikan dan tidak pernah tahu kenapa gagal. Urutan pengecekan di baris 24-31 harus dipertahankan. |
| 50 | **Penurunan nama bahan untuk pesan toast** | `src/components/dashboard/InventoryItemForm.tsx:29-30` | Nama untuk toast diambil berlapis: String(fd.get("name") ?? item?.name ?? "Bahan").trim() \|\| "Bahan". Artinya: pakai nilai form, kalau tidak ada pakai nama item lama, kalau tidak ada juga pakai literal 'Bahan'; dan kalau hasil trim kosong tetap 'Bahan'. | • Pasif; menentukan isi toast setelah simpan | `nama normal` · `nama dengan spasi berlebih` · `nama kosong (secara teori)` | Simpan bahan bernama '  Kopi  ' (dengan spasi). Toast harus berbunyi 'Kopi berhasil ditambahkan ke inventory.' tanpa spasi menggantung. | Rantai fallback empat lapis ini terlihat berlebihan dan sering dipangkas jadi satu baris, yang bisa menghasilkan toast berbunyi 'undefined berhasil diperbarui.' |
| 51 | **Modal Atur Stok: tiga mode penyesuaian** | `src/components/dashboard/StockAdjustmentModal.tsx:17,33,86-97` | Select terkontrol (value + onChange, bukan defaultValue) dengan tiga opsi: 'Tambah stok' (add), 'Kurangi stok' (subtract), 'Set jumlah persis' (set). Nilai awal 'add'. State mode juga mengendalikan atribut min pada field jumlah. | • Ganti mode di dropdown -> atribut min field Jumlah ikut berubah secara langsung | `add (default)` · `subtract` · `set` | Buka modal, pilih 'Set jumlah persis', ketik 0, submit. Harus diterima. Lalu pilih 'Tambah stok', ketik 0, submit. Browser harus menolak karena min=0.001. | Kalau select diubah jadi uncontrolled (defaultValue), state mode tidak pernah berubah dan aturan min jumlah membeku di 0.001, membuat 'Set jumlah persis ke 0' mustahil dilakukan lewat UI. Bug ini hanya muncul pada satu kombinasi input. |
| 52 | **Batas minimum jumlah bergantung mode** | `src/components/dashboard/StockAdjustmentModal.tsx:19-21 (quantityMinForMode) dan :100` | Fungsi murni yang mengembalikan '0' untuk mode set (mengizinkan pengosongan stok ke nol) dan '0.001' untuk add/subtract (memaksa jumlah positif). Nilai ini dipasang ke atribut min input. Aturan yang sama ditegakkan lagi di server (dashboard-actions.ts:372-374) sehingga bypass DOM tetap tertolak. | • Ganti mode -> batas input berubah<br>• Isi 0 pada mode add -> browser memblokir, dan kalau dipaksa lewat DOM, server menolak dengan 'Jumlah penyesuaian harus lebih dari 0.' | `mode set (min 0)` · `mode add/subtract (min 0.001)` | Jalankan tests/inventory-dashboard.test.ts blok 'stock adjustment quantity rules' dan tests/dashboard-inventory-actions.test.ts 'rejects zero add and subtract adjustments'. Fungsi ini diekspor khusus untuk diuji. | Validasi ini ada berlapis di klien dan server. Menghapus lapis klien saja membuat pengalaman lebih buruk tanpa error; menghapus lapis server membuka celah data. Keduanya harus tetap ada. |
| 53 | **Konteks stok saat ini di deskripsi modal Atur Stok** | `src/components/dashboard/StockAdjustmentModal.tsx:67-69 dan placeholder di :100,104` | DialogDescription menampilkan '{nama bahan}. Stok sekarang {formatQty(current_qty, unit)}.' sehingga pengguna tahu titik awalnya sebelum memilih mode. Placeholder field jumlah dinamis: `Jumlah (${item.unit})` sehingga satuan terlihat. Placeholder catatan: 'Catatan penyesuaian'. | • Pasif; deskripsi dibaca screen reader sebagai aria-describedby dialog | `stok positif` · `stok 0` · `satuan apa pun` | Buka Atur Stok untuk bahan bersatuan liter dengan stok 2.5. Deskripsi harus berbunyi 'Nama. Stok sekarang 2.5 liter.' dan placeholder field jumlah 'Jumlah (liter)'. | Tanpa konteks ini pengguna harus mengingat stok dari tabel di belakang modal. Placeholder dinamis juga mudah diganti teks statis 'Jumlah', menghilangkan petunjuk satuan yang mencegah salah input (gram vs kg). |
| 54 | **Tombol Batal terpisah pada modal Atur Stok** | `src/components/dashboard/StockAdjustmentModal.tsx:106-114` | Footer berisi dua tombol rata kanan: 'Batal' (type=button, outline transparan border rgba(255,255,255,0.1), memanggil onClose langsung) dan 'Simpan' (submit, oranye, dengan state pending: spinner Loader2, label 'Menyimpan', opacity 0.7, disabled). Modal form bahan tidak punya tombol Batal, hanya X, jadi pola kedua modal memang berbeda. | • Klik Batal -> modal tertutup tanpa menyimpan, fokus kembali ke tombol pemicu<br>• Klik Simpan -> submit dengan state pending | `idle` · `pending (Simpan disabled, Batal tetap aktif)` · `error` | Bandingkan kedua modal: modal Atur Stok harus punya baris tombol Batal + Simpan di bawah; modal Tambah/Ubah Bahan hanya punya satu tombol Simpan Bahan rata kiri. | Rebuild cenderung menyeragamkan kedua modal. Perbedaan ini existing dan menghilangkan tombol Batal mengurangi jalan keluar bagi pengguna yang tidak tahu Escape. |

### Server action, validasi & revalidasi — fitur 55–58

| # | Fitur | Lokasi | Apa yang terjadi | Interaksi | State | Cara verifikasi | Risiko kalau hilang |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 55 | **Validasi server dan pembersihan payload bahan** | `src/lib/dashboard-actions.ts:19-24 (cleanInventoryUnit), :289-321 (inventoryNumber, inventoryPayload), :323-361` | Server memvalidasi ulang semuanya: satuan dicocokkan ke whitelist 7 nilai (kalau tidak cocok jadi string kosong lalu ditolak 'Satuan bahan tidak valid.'); angka kosong dianggap 0, angka non-finite atau negatif ditolak 'Angka inventory tidak valid.'; nama wajib ada; estimated_unit_cost dibulatkan dengan Math.round; notes yang kosong disimpan sebagai null (bukan string kosong). | • Pasif; hanya terasa saat validasi klien di-bypass | `payload valid` · `satuan asing` · `angka negatif/NaN/Infinity` · `nama kosong` · `field angka dikosongkan -> jadi 0` | Jalankan tests/dashboard-inventory-actions.test.ts, khususnya kasus it.each untuk -1, NaN, -0.001, Infinity, -20, 'not-a-number'. Semua harus ditolak sebelum insert dipanggil. | Perilaku 'field kosong = 0' (bukan error) adalah keputusan sengaja di baris 291. Kalau diubah jadi error, pengguna yang mengosongkan Batas Menipis akan diblokir padahal sekarang tidak. Math.round pada harga juga mudah hilang, membiarkan harga desimal masuk database. |
| 56 | **Penjagaan kepemilikan kafe pada semua aksi inventory** | `src/lib/dashboard-actions.ts:33-47 (getAuthCafeId), dipanggil di :324, :343, :364` | Setiap server action inventory memulai dengan getAuthCafeId() yang memverifikasi user login lalu memetakan ke id_cafe miliknya; kalau null, aksi berhenti dengan 'Sesi tidak valid. Masuk ulang.' Update juga di-scope ganda dengan .eq("id_inventory_item", id).eq("cafe_id", cafeId) sehingga item milik kafe lain tidak bisa diubah walau ID-nya ditebak. | • Pasif; terlihat sebagai pesan error kalau sesi habis di tengah pengisian form | `sesi valid` · `sesi habis` · `user tanpa kafe` · `ID item milik kafe lain` | Panggil updateInventoryItem dengan ID milik kafe lain (lewat test). Query update harus tetap ter-scope cafe_id sehingga tidak ada baris yang berubah. | Filter .eq("cafe_id", cafeId) pada update (baris 354) terlihat redundan karena ID sudah unik, jadi mudah dihapus saat 'merapikan'. Padahal itu satu-satunya pencegah modifikasi lintas kafe pada jalur ini. |
| 57 | **Revalidasi cache lintas halaman setelah aksi inventory** | `src/lib/dashboard-actions.ts:336-338, :357-359, :390-391` | createInventoryItem dan updateInventoryItem me-revalidate tiga rute: /dashboard, /dashboard/inventory, dan /dashboard/menu (karena RecipeEditor di halaman menu menampilkan daftar dan status stok bahan). adjustInventoryStock hanya me-revalidate /dashboard dan /dashboard/inventory (bukan /dashboard/menu). | • Pasif; efeknya terasa saat berpindah halaman setelah mengubah bahan | `setelah create` · `setelah update` · `setelah adjust` | Tambah bahan baru di /dashboard/inventory, lalu navigasi ke /dashboard/menu dan buka form menu. Bahan baru harus sudah tersedia di dropdown RecipeEditor tanpa hard refresh. | Daftar path ini adalah kaitan tak terlihat antar area dashboard. Kalau /dashboard/menu terlewat, dropdown resep menampilkan daftar bahan basi dan pengguna mengira bahan yang baru dibuat tidak tersimpan. Perbedaan sengaja (adjust tidak me-revalidate menu) juga harus dipertahankan atau diubah secara sadar. |
| 58 | **Penyesuaian stok atomik lewat RPC dengan pemetaan error** | `src/lib/dashboard-actions.ts:363-393` | Penyesuaian tidak dilakukan dengan baca-lalu-tulis, melainkan satu panggilan supabaseAdmin.rpc("adjust_inventory_stock", { p_cafe_id, p_inventory_item_id, p_mode, p_quantity, p_note }) sehingga aman dari race condition dengan pemotongan stok dari pesanan. Kode error RPC diterjemahkan: inventory_not_found -> 'Bahan tidak ditemukan.', negative_stock -> 'Stok tidak boleh kurang dari 0.', invalid_adjustment -> 'Jumlah penyesuaian tidak valid.' Mode divalidasi terhadap daftar ['add','subtract','set'] sebelum RPC dipanggil. | • Kurangi stok melebihi yang tersedia -> muncul 'Stok tidak boleh kurang dari 0.' di dalam modal | `sukses` · `stok akan negatif` · `bahan tidak ditemukan/lintas kafe` · `mode tidak valid` · `error koneksi Supabase (pesan mentah)` | Jalankan tests/dashboard-inventory-actions.test.ts 'uses the atomic stock adjustment RPC instead of separate inventory writes' yang secara eksplisit memastikan tidak ada .update() dan tidak ada insert ke Inventory_Movements dari kode aplikasi. | Sangat berbahaya: rebuild yang menulis ulang logika ini sebagai select lalu update lalu insert movement akan lolos semua uji manual tapi menghasilkan stok salah saat pesanan masuk bersamaan. Test yang ada sengaja menegakkan larangan itu, jadi test tersebut harus ikut dipertahankan. |

### RecipeEditor — jembatan Menu ↔ Inventory — fitur 59–67

| # | Fitur | Lokasi | Apa yang terjadi | Interaksi | State | Cara verifikasi | Risiko kalau hilang |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 59 | **RecipeEditor: penambahan baris otomatis tanpa duplikat** | `src/components/dashboard/RecipeEditor.tsx:11-18 (nextRecipeRow), :44-47 (addRow), :68-76` | Tombol 'Tambah Bahan' mencari bahan pertama yang belum dipakai di baris resep dan menambahkannya dengan qty_per_menu default 1. Kalau semua bahan sudah terpakai, fungsi mengembalikan undefined dan tombol otomatis disabled (opacity 0.5, cursor-not-allowed). Tombol juga disabled saat prop disabled=true (form menu sedang menyimpan). | • Klik Tambah Bahan -> baris baru langsung terisi bahan berikutnya, tidak kosong<br>• Tambah terus sampai semua bahan terpakai -> tombol mati sendiri<br>• Saat menu sedang disimpan -> tombol mati | `masih ada bahan tersedia` · `semua bahan sudah dipakai (tombol disabled)` · `tidak ada bahan sama sekali (tombol disabled)` · `form disabled` | Jalankan tests/recipe-editor.test.ts blok 'recipe editor row selection'. Manual: dengan 2 bahan di inventory, klik Tambah Bahan tiga kali; tombol harus mati setelah klik kedua. | Pemanggilan nextRecipeRow di atribut disabled (baris 71) dieksekusi setiap render dan mudah diganti kondisi lain seperti rows.length >= items.length. Kalau salah, tombol bisa menambah baris duplikat yang lalu ditolak server dengan pesan 'Satu bahan tidak boleh muncul dua kali'. |
| 60 | **Opsi bahan yang sudah dipakai dinonaktifkan di dropdown resep** | `src/components/dashboard/RecipeEditor.tsx:111-122` | Setiap <option> dinonaktifkan kalau bahan itu sudah dipakai baris lain (kecuali baris yang sedang diedit, supaya nilai terpilihnya sendiri tetap valid). Ini mencegah duplikat sebelum sampai ke validasi server. | • Buka dropdown pada baris kedua -> bahan yang sudah dipakai baris pertama tampil abu-abu dan tidak bisa dipilih | `belum ada baris lain` · `beberapa bahan terpakai` · `semua bahan terpakai` | Tambah dua baris resep dengan bahan berbeda, lalu buka dropdown baris pertama. Bahan milik baris kedua harus disabled, sementara bahan baris pertama sendiri tetap bisa dipilih. | Kondisi ganda (bukan bahan saya DAN sudah dipakai orang lain) mudah disalin salah jadi hanya 'sudah dipakai', yang membuat select menonaktifkan nilainya sendiri dan dropdown tampak kosong atau reset. |
| 61 | **Input jumlah resep dengan tampilan kosong saat nol** | `src/components/dashboard/RecipeEditor.tsx:124-140` | Input terkontrol memakai value={row.qty_per_menu \|\| ""} sehingga nilai 0 ditampilkan sebagai field kosong (bukan '0' yang harus dihapus dulu). onChange memakai Number(event.target.value) \|\| 0 sehingga ketikan tidak valid jatuh ke 0. Atribut: type number, min 0.001, step 0.001, inputMode decimal. Label 'Jumlah bahan {n}' disembunyikan dengan sr-only. | • Hapus isi field -> menjadi kosong, bukan '0'<br>• Ketik angka -> tersimpan ke state induk (MenuForm)<br>• Screen reader membaca 'Jumlah bahan 1' meski tidak ada label visual | `nilai 0 (kosong)` · `nilai desimal` · `nilai tidak valid -> 0` · `disabled saat menyimpan` | Kosongkan field jumlah pada satu baris resep lalu coba simpan menu. Harus muncul pesan 'Jumlah setiap bahan harus lebih dari 0.' dan penyimpanan dibatalkan. | Trik `\|\| ""` terlihat seperti bug dan sering 'diperbaiki' jadi value={row.qty_per_menu}, yang membuat field menampilkan 0 permanen dan pengguna harus menghapus nol sebelum mengetik. Label sr-only juga mudah hilang karena tidak terlihat. |
| 62 | **Satuan bahan ditampilkan di dalam field jumlah resep** | `src/components/dashboard/RecipeEditor.tsx:126,134,137-139` | Span absolut di kanan dalam field (right-3, translate-y-1/2, 11px, warna #5A7898) menampilkan satuan bahan terpilih, dengan pointer-events-none supaya klik tetap tembus ke input. Input diberi pr-9 agar teks tidak menabrak satuan. | • Ganti bahan di dropdown -> satuan di dalam field ikut berubah<br>• Klik tepat di area satuan -> tetap memfokuskan input, bukan mengabaikan klik | `bahan ditemukan (satuan tampil)` · `bahan tidak ditemukan di map (satuan kosong)` | Pilih bahan bersatuan 'liter' pada baris resep. Teks 'liter' harus muncul di dalam kanan field jumlah. Klik tepat pada tulisan itu; kursor harus masuk ke input. | pointer-events-none dan padding pr-9 adalah dua detail kecil yang saling bergantung. Tanpa pointer-events-none, area kanan field jadi mati klik; tanpa pr-9, angka panjang tertimpa teks satuan. |
| 63 | **Hapus baris resep** | `src/components/dashboard/RecipeEditor.tsx:141-151` | Tombol ikon Trash2 (h-11 w-10) berlatar merah rgba(239,68,68,0.1) dengan aria-label dinamis `Hapus bahan ${item?.name ?? index + 1}` dan title 'Hapus bahan'. Menghapus langsung tanpa konfirmasi. Ikut disabled saat form menyimpan. | • Klik ikon tempat sampah -> baris langsung hilang dari state (belum tersimpan sampai menu disimpan)<br>• Hover -> efek dash-press<br>• Screen reader membaca nama bahan yang akan dihapus | `item ditemukan (label pakai nama)` · `item tidak ditemukan (label pakai nomor baris)` · `disabled` | Tambah dua baris, hapus yang pertama. Baris kedua harus tetap ada dengan nilai jumlahnya utuh (key memakai kombinasi id dan index). | Tidak ada konfirmasi dan tidak ada undo; ini perilaku existing yang disengaja karena perubahan baru permanen setelah menu disimpan. Menambah dialog konfirmasi akan mengubah alur. Sebaliknya, key baris `${id}-${index}` penting agar React tidak salah mencocokkan baris setelah penghapusan. |
| 64 | **Konteks stok per bahan di dalam editor resep** | `src/components/dashboard/RecipeEditor.tsx:153,163-180` | Di bawah setiap baris resep ditampilkan 'Stok {qty} {unit} \| Minimum {min} {unit} {Label status}' dengan label berwarna: Aman #22D3A6, Menipis #F59E0B, Habis #FCA5A5. Pemisah '\|' diberi aria-hidden. Hanya dirender kalau item ketemu di map. | • Pasif; membantu memutuskan jumlah per menu tanpa pindah halaman | `safe` · `low` · `empty` · `bahan tidak ditemukan (blok tidak dirender)` | Jalankan tests/recipe-editor.test.ts 'shows each ingredient current stock, minimum, unit, and empty status' yang mengecek string 'Stok 0 gram', 'Minimum 500 gram', dan 'Habis' ada di markup. | Fitur ini menghubungkan area Menu ke area Inventory. Rebuild yang fokus pada satu area saja mudah membuang blok ini karena terlihat seperti hiasan, padahal ia mencegah pemilik kafe membuat resep dari bahan yang habis. |
| 65 | **Tautan kontekstual ke halaman Inventory dari editor resep** | `src/components/dashboard/RecipeEditor.tsx:177 dan :182-193 (komponen InventoryLink)` | Dua varian link ke /dashboard/inventory. Varian compact ('Perbarui stok' + ikon ArrowUpRight 12px, ml-auto, tanpa background) hanya muncul pada baris yang bahannya berstatus habis. Varian penuh ('Buka Inventory' + ikon 14px, latar rgba(253,80,2,0.12), rounded) muncul di blok saat inventory benar-benar kosong. | • Klik 'Perbarui stok' pada bahan habis -> pindah ke halaman inventory<br>• Klik 'Buka Inventory' saat belum ada bahan sama sekali -> pindah ke halaman inventory | `ada bahan habis (link compact)` · `inventory kosong (link penuh)` · `semua aman (tidak ada link)` | Jalankan tests/recipe-editor.test.ts yang mengecek href="/dashboard/inventory" dan teks 'Buka Inventory'. Manual: buat satu bahan berstatus habis lalu buka form menu; link 'Perbarui stok' harus muncul hanya pada baris bahan itu. | Ada dua varian dengan kondisi tampil berbeda. Rebuild sering menyisakan satu saja, memutus jalur perbaikan cepat dari form menu ke inventory. |
| 66 | **Status kosong dan validasi editor resep** | `src/components/dashboard/RecipeEditor.tsx:20-24 (recipeRowsValidationError), :42,79-96` | Tiga kondisi tampilan bertingkat: (1) inventoryItems kosong -> blok abu-abu 'Belum ada bahan inventory. Tambahkan bahan sebelum membuat resep.' dengan link; (2) rows kosong -> teks 'Menu ini belum memakai stok inventory.'; (3) ada baris -> daftar baris. Di atasnya, banner merah role="alert" 'Jumlah setiap bahan harus lebih dari 0.' muncul realtime saat ada baris dengan qty <= 0 atau non-finite. Validasi yang sama juga menghalangi submit di MenuForm.tsx:168-173. | • Kosongkan jumlah sebuah baris -> banner merah langsung muncul tanpa menekan tombol apa pun<br>• Coba simpan menu dengan banner aktif -> submit dibatalkan dan pesan sama muncul di area error MenuForm | `inventory kosong` · `resep kosong (valid, artinya menu tidak memotong stok)` · `ada baris valid` · `ada baris tidak valid` | Jalankan tests/recipe-editor.test.ts blok 'recipe editor quantity validation' termasuk kasus resep kosong yang harus dianggap valid (undefined). | Resep kosong SAH (menu tanpa pemotongan stok) sementara baris dengan jumlah 0 TIDAK sah. Perbedaan ini mudah tertukar saat validasi ditulis ulang, dan akibatnya semua menu tanpa resep jadi tidak bisa disimpan. Fungsi recipeRowsValidationError juga diekspor dan dipakai MenuForm, jadi harus tetap diekspor dengan nama sama. |
| 67 | **Propagasi status disabled dari form menu ke editor resep** | `src/components/dashboard/RecipeEditor.tsx:35,71,106,129,144 dan pemanggilan di src/components/dashboard/MenuForm.tsx:403-408 (disabled={saving})` | Saat MenuForm sedang menyimpan, prop disabled=true diteruskan ke seluruh kontrol RecipeEditor: tombol Tambah Bahan, setiap select bahan, setiap input jumlah, dan setiap tombol hapus. Semua memakai disabled:opacity-50 dan disabled:cursor-not-allowed pada tombol. | • Klik Simpan di form menu -> seluruh editor resep membeku sampai proses selesai | `aktif` · `disabled saat menyimpan` | Throttle jaringan, klik Simpan pada form menu. Selama spinner berjalan, tidak satu pun kontrol di blok Resep Inventory boleh bisa diubah. | Empat kontrol berbeda harus sama-sama menerima prop disabled. Melewatkan satu saja (misalnya select bahan) memungkinkan pengguna mengubah resep saat request sudah terkirim, sehingga data yang tersimpan berbeda dengan yang terlihat di layar. |

### Gaya, motion & aksesibilitas — fitur 68–70

| # | Fitur | Lokasi | Apa yang terjadi | Interaksi | State | Cara verifikasi | Risiko kalau hilang |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 68 | **Umpan balik tekan dan hover pada seluruh tombol inventory** | `src/app/globals.css:467-468 (.dash-press), :482-486 (.dash-btn, .dash-icon-btn)`<br>`dipakai di InventoryTable.tsx:116,140,153,209,212,296`<br>`StockAdjustmentModal.tsx:71,107,110`<br>`RecipeEditor.tsx:70,145,186` | Tiga kelas interaksi berbeda: .dash-press (transform scale 0.96 saat ditekan), .dash-btn (brightness 1.08 saat hover di perangkat berpenunjuk, scale 0.97 saat ditekan), .dash-icon-btn (background rgba(255,255,255,0.06) dan warna terang saat hover). Semua efek hover dibungkus @media (hover: hover). | • Tekan tombol -> mengecil sedikit<br>• Hover tombol utama -> lebih terang<br>• Hover tombol ikon -> muncul latar lembut<br>• Perangkat sentuh -> tanpa hover menempel | `idle` · `hover` · `active` · `disabled (opacity dikelola inline/utility)` | Klik dan tahan tombol 'Tambah Bahan'; harus mengecil. Hover ikon Ubah pada baris tabel; latar harus muncul. Emulasikan perangkat sentuh; hover tidak boleh tersangkut setelah tap. | Kelas-kelas ini adalah bahasa interaksi khas dashboard yang dipakai di 12 titik pada area inventory saja. Menggantinya dengan komponen Button generik menghilangkan karakter tekan/hover yang konsisten, dan tidak akan terdeteksi test mana pun. |
| 69 | **Gaya fokus input dashboard** | `src/app/globals.css:480-481 (.dash-input) dan src/components/dashboard/system/fields.tsx:33-38 (dashInputClass, dashInputStyle)` | Semua input inventory memakai kelas dash-input dan gaya dashInputStyle (background var(--dash-raised), border rgba(255,255,255,0.1), color var(--dash-text)). Saat fokus, border menjadi oranye rgba(253,80,2,0.6) dan muncul ring box-shadow 3px rgba(253,80,2,0.13), dengan outline dimatikan. Perhatikan InventoryItemForm memakai kelas ditulis manual sedangkan StockAdjustmentModal memakai konstanta dashInputClass. | • Tab atau klik ke field mana pun -> border oranye + halo lembut | `normal` · `fokus` · `disabled` | Tab melewati semua field di modal Tambah Bahan. Setiap field (termasuk select Satuan dan textarea Catatan) harus menampilkan border oranye saat fokus, bukan outline biru default browser. | outline:none pada .dash-input berarti indikator fokus SEPENUHNYA bergantung pada border dan box-shadow oranye. Kalau kelas dash-input hilang saat rebuild, field kehilangan outline default sekaligus penggantinya dan menjadi tidak bisa dinavigasi keyboard secara visual. Ini kegagalan aksesibilitas serius yang tidak kelihatan pakai mouse. |
| 70 | **Struktur landmark dan penamaan seksi** | `InventoryWorkspace.tsx:31 (aria-labelledby=inventory-workspace-title), :38`<br>`InventoryTable.tsx:134,137 (inventory-list-title), :227,229 (movement-title)`<br>`RecipeEditor.tsx:54,61 (recipe-editor-title)`<br>`InventoryWorkspace.tsx:100,111 (inventory-load-error-title)` | Setiap seksi besar adalah elemen <section>/<aside> dengan aria-labelledby yang menunjuk id judulnya masing-masing: workspace, daftar bahan, mutasi terbaru, editor resep, dan kartu error. Panel mutasi juga memakai <ol> semantik untuk daftar berurutan, bukan div. | • Navigasi landmark dengan screen reader -> tiap area punya nama sendiri | `semua state (struktur selalu ada)` | Pakai ekstensi accessibility tree atau screen reader untuk melihat daftar landmark. Harus muncul region bernama 'Inventory', 'Daftar Bahan', 'Mutasi Terbaru', bukan tiga region tanpa nama. | Pasangan id dan aria-labelledby akan putus diam-diam kalau salah satu sisi diubah saat rebuild. Tidak ada error, tidak ada perubahan visual, tapi struktur navigasi screen reader runtuh jadi satu blok tanpa nama. |

---

## Area 8 — Pengaturan, QR, Jadwal & Diskon, Pengumuman — 75 fitur

Rute: `/dashboard/settings`, `/dashboard/scheduler`, `/dashboard/announcements`. Komponen inti: `SettingsForm`, `QrSmartMenu`, `FileUpload`, `PhoneMockup`, `SchedulerClient`, `AnnouncementForm`. Helper: `src/lib/site-url.ts`, `src/lib/qr-render.ts`, `src/lib/menu-availability.ts`, `src/lib/contrast.ts`.


### Rute, penjagaan sesi, skeleton & motion — fitur 1–7

| # | Fitur | Lokasi | Apa yang terjadi | Interaksi | State | Cara verifikasi | Risiko kalau hilang |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | **Penjagaan sesi + redirect ke /login di tiga halaman** | `src/app/dashboard/settings/page.tsx:10-11`<br>`src/app/dashboard/scheduler/page.tsx:9-10`<br>`src/app/dashboard/announcements/page.tsx:9-10` | Setiap halaman memanggil getDashboardCafeContext() lalu redirect("/login") bila userId null. Data kafe (slug/cafeId) diambil dari konteks yang sama, bukan query terpisah. | • Buka /dashboard/settings, /dashboard/scheduler, /dashboard/announcements tanpa sesi -> otomatis dilempar ke /login | `izin` · `kosong` | Logout lalu akses ketiga URL langsung; harus mendarat di /login, bukan halaman kosong atau error 500. | Saat rebuild UI, page.tsx sering diganti jadi client component atau guard dipindah ke layout; redirect hilang diam-diam dan halaman jadi bocor/blank. |
| 2 | **Dedupe query auth+kafe per request lewat React cache()** | `src/lib/dashboard-context.ts:28-47`<br>`src/lib/analytics.ts:80-88 (getOwnerCafeSlug juga cache())` | getDashboardCafeContext dibungkus cache() sehingga layout + page + komponen nested dalam satu render tree hanya melakukan satu auth.getUser() dan satu lookup Cafes. | — | `loading` | Nyalakan log query Supabase (atau tambahkan console.log sementara di getDashboardCafeContext), muat /dashboard/settings sekali, pastikan hanya satu pemanggilan meski layout dan page sama-sama memakainya. | Rebuild yang memanggil supabase langsung di tiap komponen akan melipatgandakan query dan memperlambat halaman tanpa gejala visual apa pun. |
| 3 | **Query khusus Settings: select("*") seluruh kolom Cafes** | `src/app/dashboard/settings/page.tsx:13-16` | Berbeda dengan halaman lain yang cukup memakai konteks ringkas, Settings sengaja mengambil seluruh kolom tabel Cafes berdasarkan slug_url karena form butuh greeting, logo_url, cover_url, google_maps_review_url, alamat_cafe. | — | `kosong` · `error` | Isi greeting + URL ulasan Google, reload halaman; nilai harus muncul kembali di input. Bila kolom hilang dari select, field jadi kosong tiap reload. | Rebuild sering mengganti ke select kolom terbatas demi 'optimasi'; field yang tidak ikut di-select berubah jadi kosong permanen dan tersimpan null saat user menekan Simpan. |
| 4 | **State 'Kafe tidak ditemukan' pada Settings** | `src/app/dashboard/settings/page.tsx:18-24` | Bila slug ada tapi baris Cafes tidak ditemukan, halaman merender blok terpusat min-h-[60vh] dengan teks 'Kafe tidak ditemukan' berwarna #E9EEF6, bukan crash. | — | `kosong` · `error` | Sementara ubah slug di query ke nilai acak; halaman harus menampilkan pesan itu, bukan exception. | Cabang null biasanya terhapus saat komponen dipecah; halaman jadi throw pada typedCafe.nama_cafe. |
| 5 | **Skeleton loading per rute (3 file terpisah, meniru layout asli)** | `src/app/dashboard/settings/loading.tsx:1-50`<br>`src/app/dashboard/scheduler/loading.tsx:1-28`<br>`src/app/dashboard/announcements/loading.tsx:1-27` | Setiap rute punya loading.tsx sendiri dengan kotak dash-skel yang ukurannya meniru layout final (Settings: panel QR 280px + grid 2 kolom + preview 420px; Scheduler: 4 kartu; Announcements: editor + preview 340px). | • Navigasi antar-menu dashboard memicu skeleton sebelum data tiba | `loading` | Throttle jaringan ke Slow 3G, klik menu Pengaturan/Jadwal/Pengumuman dari sidebar; skeleton harus muncul dengan proporsi mirip halaman jadi (tidak ada layout shift besar). | File loading.tsx tidak diimpor siapa pun sehingga mudah terlupakan saat rebuild; hilangnya menyebabkan layar beku tanpa umpan balik. |
| 6 | **Animasi shimmer skeleton + reveal masuk konten** | `src/app/globals.css:495-509 (.dash-skel, .dash-reveal, .dash-d1..d5)`<br>`src/components/dashboard/SettingsForm.tsx:53,70,99`<br>`src/components/dashboard/SchedulerClient.tsx:132-135` | Skeleton memakai gradient bergerak 1.5s linear. Konten asli masuk dengan animasi dashReveal 0.28s translateY(10px)->0; panel Settings di-stagger via dash-d1/d2/d3, baris Scheduler di-stagger via inline animationDelay index*0.055s dengan batas maksimum indeks 7. | • Muat halaman -> panel muncul berurutan | `loading` · `sukses` | Rekam layar pemuatan Scheduler dengan >8 menu; kartu ke-1..8 muncul bertahap, kartu ke-9 dst muncul bersamaan (delay dibatasi 7*0.055s). | Kelas dash-reveal/dash-d* mudah tertinggal saat markup ditulis ulang; halaman jadi muncul sekaligus dan terasa 'patah'. |
| 7 | **Penghormatan prefers-reduced-motion** | `src/app/globals.css:518-527` | Pada mode reduced motion semua animasi dipaksa 0.01ms dan .dash-reveal khusus di-nolkan (animation:none, opacity:1, transform:none) supaya konten tidak tertahan pada opacity 0. | • Aktifkan Reduce Motion di OS | `sukses` | Aktifkan Reduce Motion, muat Scheduler; semua kartu langsung terlihat penuh tanpa fade dan tanpa ada yang tetap transparan. | Jika rebuild memakai library animasi (Framer Motion) tanpa guard reduced-motion, aturan CSS ini tidak lagi berlaku dan aksesibilitas hilang tanpa terlihat. |

### QR Smart Menu — pembentukan URL, render & kustomisasi — fitur 8–17

| # | Fitur | Lokasi | Apa yang terjadi | Interaksi | State | Cara verifikasi | Risiko kalau hilang |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 8 | **Pembentukan URL menu publik kanonik (prioritas env + normalisasi)** | `src/lib/site-url.ts:3-33`<br>`dipakai di src/app/dashboard/settings/page.tsx:27` | canonicalOrigin() memilih NEXT_PUBLIC_SITE_URL, lalu VERCEL_PROJECT_PRODUCTION_URL, lalu http://localhost:3000. normalizeOrigin membuang protokol ganda ('https://https://host'), menambahkan https:// bila tidak ada, mempertahankan http eksplisit untuk lokal, dan membuang trailing slash. menuUrlFor meng-encodeURIComponent slug. | — | `kosong` · `error` | Jalankan tests/qr-smart-menu.test.ts (blok 'site-url helpers'); dan cek nilai input Tautan Smart Menu di produksi tidak mengandung slash ganda atau protokol dobel. | Sangat mudah 'disederhanakan' jadi template string; hasilnya QR tercetak menunjuk ke localhost atau URL rusak — kesalahan yang baru ketahuan setelah stiker dicetak. |
| 9 | **Generator matriks QR dengan level koreksi error adaptif** | `src/components/dashboard/QrSmartMenu.tsx:49-57` | QRCode.create dijalankan di client dengan errorCorrectionLevel 'H' saat logo diaktifkan dan 'M' saat tidak; kegagalan pembuatan ditangkap try/catch dan mengembalikan null (bukan crash). | • Centang 'Logo 3Diner di tengah QR' -> matriks dibuat ulang dengan EC H | `error` · `kosong` | Centang logo dan perhatikan jumlah modul QR bertambah (matriks lebih rapat); scan hasil unduhan PNG dengan logo memakai ponsel — harus tetap terbaca. | Bila logo dipertahankan tapi EC dikembalikan ke M, QR tetap terlihat normal di layar namun gagal discan di dunia nyata — kegagalan senyap paling mahal di area ini. |
| 10 | **Pratinjau QR sebagai SVG vektor yang di-memo** | `src/components/dashboard/QrSmartMenu.tsx:59-77, 279-293` | svgOptions dan previewSvg dibungkus useMemo dengan dependensi tepat (menuUrl, cafeName, color, style, logoOn, showName, showScan); hasil string SVG disuntikkan lewat dangerouslySetInnerHTML ke dalam kotak var(--dash-raised) dengan box-shadow, max-width 280px, dan aria-hidden saat SVG tidak ada. | • Ubah style/warna/checkbox -> pratinjau berubah seketika tanpa request jaringan | `kosong` · `sukses` | Ubah setiap kontrol kustomisasi dan pastikan pratinjau ikut berubah instan; buka DevTools Network — tidak boleh ada request saat mengubah opsi. | Rebuild sering mengganti ke <img src=dataURL> atau library QR canvas; hasil cetak berubah dari vektor jadi raster dan opsi bingkai/caption ikut hilang. |
| 11 | **Kustomisasi QR: dua style (Bersih / Bingkai 3Diner)** | `src/components/dashboard/QrSmartMenu.tsx:417-423`<br>`efek di src/lib/qr-render.ts:70,95-99,139-144` | Mode 'bingkai' menambah padding 2 modul, garis tepi navy #022C60 stroke 0.7, dan baris teks oranye '3DINER SMART MENU' dengan letter-spacing 0.12 di bawah caption. | • Klik chip Bersih<br>• Klik chip Bingkai 3Diner | `sukses` | Pilih Bingkai, unduh PNG dan SVG; kedua berkas harus memuat garis tepi navy dan teks '3DINER SMART MENU' pada posisi yang sama. | Cabang frame ada di tiga tempat (computeLayout, buildQrSvg, downloadPng canvas); rebuild yang hanya memindahkan tombol bisa memutus salah satu sehingga PNG dan SVG jadi berbeda. |
| 12 | **Kustomisasi QR: dua warna modul dengan swatch** | `src/components/dashboard/QrSmartMenu.tsx:25-26,431-438` | Pilihan Navy 3Diner (#022C60) dan Gelap Pekat (#10151D); tiap tombol menampilkan lingkaran warna 12px dengan border putih transparan dan aria-pressed. | • Klik Navy 3Diner<br>• Klik Gelap Pekat | `sukses` | Pilih Gelap Pekat, unduh SVG, buka berkas dan pastikan atribut fill path bernilai #10151D. | Nilai hex konstanta mudah tergeser saat token warna dashboard di-refactor; QR yang terlalu terang gagal discan. |
| 13 | **Kustomisasi QR: tiga checkbox elemen dengan hint** | `src/components/dashboard/QrSmartMenu.tsx:442-451, 461-486` | CheckRow untuk logo (dengan hint 'Koreksi error otomatis naik ke level H'), tampilkan nama kafe, dan tampilkan teks 'Scan untuk melihat menu'. Setiap baris adalah <label> dengan minHeight 44px, checkbox accent-[#FD5002]. | • Centang/hapus centang logo<br>• Centang/hapus centang nama kafe<br>• Centang/hapus centang teks scan | `sukses` | Hapus centang nama dan teks scan; tinggi SVG harus menyusut (computeLayout mengurangi captionLines) dan caption hilang dari PNG juga. | Hint tentang EC level H adalah satu-satunya penjelasan ke pemilik kafe; hilangnya membuat perubahan perilaku jadi tak terjelaskan. Target sentuh 44px juga diuji otomatis. |
| 14 | **Disclosure 'Sesuaikan Tampilan QR' (Radix Collapsible) + rotasi chevron** | `src/components/dashboard/QrSmartMenu.tsx:390-409, 452`<br>`src/components/ui/collapsible.tsx:1-33` | Panel kustomisasi tersembunyi di balik Collapsible Radix (state customOpen), pemicu punya minHeight 44px, dan ikon ChevronDown berputar 180deg dengan transisi 180ms cubic-bezier(0.22,1,0.36,1). Ada garis pemisah borderTop di atas pemicu. | • Klik 'Sesuaikan Tampilan QR' untuk buka/tutup<br>• Enter/Space pada pemicu (Radix menangani keyboard) | `sukses` | Tab ke pemicu, tekan Enter; panel terbuka, chevron berputar, dan atribut aria-expanded berubah. | Diganti dengan <details> atau state manual akan menghilangkan semantik aria-expanded/aria-controls dari Radix. |
| 15 | **Input tautan read-only dengan select-on-focus** | `src/components/dashboard/QrSmartMenu.tsx:297-316` | Input id=qr-menu-url readOnly berisi menuUrl, tinggi 44px, truncate, dan onFocus otomatis men-select seluruh teks agar bisa langsung Ctrl+C. Label terhubung lewat htmlFor. | • Klik/tab ke input -> seluruh URL tersorot<br>• Ctrl+C menyalin | `disabled` · `sukses` | Klik satu kali di input; seluruh teks harus tersorot otomatis. | onFocus select() adalah detail satu baris yang hampir pasti hilang saat input diganti komponen Input shadcn. |
| 16 | **Tombol salin tautan: umpan balik 2,2 detik + fallback error + aria-live** | `src/components/dashboard/QrSmartMenu.tsx:79-88, 317-331, 349-351` | navigator.clipboard.writeText; sukses -> ikon Copy berubah jadi Check hijau #22D3A6 dengan transisi warna 180ms, title dan aria-label berubah jadi 'Tautan disalin', dan teks 'Tautan disalin' muncul di region aria-live=polite (min-h 18px agar tidak menggeser layout). Setelah 2200ms kembali normal. Bila clipboard gagal, pesan error diarahkan ke exportMsg: 'Gagal menyalin tautan. Salin manual dari kolom di atas.' | • Klik tombol salin | `sukses` · `error` | Klik salin di halaman http (bukan https) atau blokir izin clipboard; harus muncul pesan fallback, bukan diam saja. Uji otomatis: tests/qr-smart-menu.test.ts:142-153. | Blok catch clipboard dan region aria-live sering dibuang; pengguna yang gagal menyalin tidak mendapat sinyal apa pun. |
| 17 | **Tautan 'Buka Menu' ke halaman publik** | `src/components/dashboard/QrSmartMenu.tsx:333-347` | Anchor ke menuUrl dengan target=_blank dan rel="noopener noreferrer", tinggi 44px, ikon ExternalLink aria-hidden. | • Klik 'Buka Menu' -> tab baru ke menu pelanggan | `sukses` | Klik dan pastikan tab baru terbuka ke URL yang sama persis dengan isi input. | rel=noopener mudah hilang; ini masalah keamanan yang tidak menimbulkan gejala visual. |

### QR Smart Menu — ekspor PNG/SVG & keamanan render — fitur 18–30

| # | Fitur | Lokasi | Apa yang terjadi | Interaksi | State | Cara verifikasi | Risiko kalau hilang |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 18 | **Unduh PNG 2048px siap cetak (rasterisasi canvas manual)** | `src/components/dashboard/QrSmartMenu.tsx:139-236` | Menghitung scale = ceil(2048 / layout.width), menggambar latar, bingkai (strokeRect 0.7*scale), setiap modul QR sebagai fillRect integer agar tepi tajam untuk cetak, logo dengan backing roundRect padding 0.8*scale, lalu tiga baris caption memakai posisi vertikal yang persis meniru computeLayout (2.4/1.9/1.8 + tambahan 1.7 untuk baris bingkai), font Poppins dengan bobot 600/400/500/600 dan warna #022C60 / #51698F / #FD5002. | • Klik 'Unduh PNG' | `loading` · `error` · `disabled` · `sukses` | Unduh PNG dengan semua opsi aktif; buka berkas, ukur lebar >= ~2048px, dan bandingkan posisi caption dengan SVG hasil unduhan — harus identik. | Ini kode paling panjang di area ini dan tidak punya pembanding visual di UI; rebuild yang menukarnya ke rasterisasi SVG->canvas akan menghasilkan teks buram, font berbeda, atau canvas tainted. |
| 19 | **Unduh SVG dengan logo di-embed sebagai data URI** | `src/components/dashboard/QrSmartMenu.tsx:101-137` | Sebelum menulis berkas, logo di-fetch dan dikonversi ke data URI lewat FileReader supaya SVG berdiri sendiri saat dibuka offline; bila fetch gagal, opsi logo dihapus dan ekspor tetap berjalan tanpa logo. | • Klik 'Unduh SVG' dengan logo aktif | `loading` · `error` · `sukses` | Unduh SVG dengan logo, buka berkas di editor teks: atribut href harus diawali 'data:image/png;base64,' bukan '/brand/...'. Buka berkas offline — logo harus tetap tampil. | Konversi data URI terlihat seperti langkah opsional dan sering dihapus; hasilnya SVG cetak yang dikirim ke percetakan kehilangan logo. |
| 20 | **Mekanisme unduh: anchor sementara + revoke tertunda 2 detik** | `src/components/dashboard/QrSmartMenu.tsx:90-99` | Membuat <a download>, klik programatik, hapus elemen, lalu URL.revokeObjectURL dijadwalkan 2000ms kemudian karena revoke sinkron dapat membatalkan unduhan di sebagian browser. | • Klik Unduh PNG/SVG | `sukses` | Unduh berkas di Safari/iOS dan Chrome; berkas harus tersimpan utuh, tidak 0 byte. | setTimeout revoke tampak seperti kode berlebihan; menghapusnya menyebabkan unduhan gagal hanya di sebagian browser. |
| 21 | **Nama berkas QR tersanitasi** | `src/lib/qr-render.ts:39-47` | qrFileName menghasilkan '3diner-qr-menu-[slug].[ext]' dengan slug dilowercase, karakter non [a-z0-9-] diganti '-', tanda hubung ganda diciutkan, hubung di ujung dibuang, dan fallback 'kafe' bila hasilnya kosong. | • Unduh berkas -> nama otomatis | `sukses` | Jalankan tests/qr-smart-menu.test.ts:64-68; atau uji slug berisi spasi/tanda kurung dan periksa nama berkas hasil unduhan. | Diganti dengan `${slug}.png` akan menghasilkan nama berkas ilegal di Windows untuk slug tertentu. |
| 22 | **Quiet zone 4 modul dan tata letak caption bersama PNG/SVG** | `src/lib/qr-render.ts:28, 61-80` | computeLayout adalah satu-satunya sumber kebenaran ukuran: quiet zone 4 modul (spek QR) + padding bingkai 2, tinggi tambahan caption 3.4/2.6/2.4/2.4 unit plus 1.5 unit jarak. Dipakai oleh SVG dan oleh rasterisasi PNG. | — | `sukses` | tests/qr-smart-menu.test.ts:85-87 memeriksa qrOffset==QUIET_ZONE dan width==size+2*QUIET_ZONE. | Quiet zone yang menyusut membuat QR gagal discan di permukaan berpola; kesalahan ini tak terlihat di layar. |
| 23 | **Escaping XML pada seluruh teks SVG** | `src/lib/qr-render.ts:30-37, 91-92, 102, 110-113, 121, 128, 135` | escapeXml diterapkan pada title, desc, warna, href logo, dan semua caption sehingga nama kafe berisi <, >, &, kutip tidak merusak atau menyuntik markup. | — | `error` · `sukses` | tests/qr-smart-menu.test.ts:103-114 memverifikasi nama kafe berisi <script> keluar sebagai &lt;script&gt;. | Preview memakai dangerouslySetInnerHTML, jadi hilangnya escaping langsung menjadi celah XSS di dashboard. |
| 24 | **Metadata aksesibel pada SVG QR** | `src/lib/qr-render.ts:88-92`<br>`judul disusun di src/components/dashboard/QrSmartMenu.tsx:69-70` | SVG memiliki role="img", aria-labelledby="qr-title qr-desc", <title> 'QR menu digital [nama]' dan <desc> 'Scan untuk membuka smart menu [nama] di [url]' sehingga tujuan QR terbaca pembaca layar dan tersimpan di berkas ekspor. | • Pembaca layar membaca pratinjau QR | `sukses` | tests/qr-smart-menu.test.ts:139 mencari role img dengan nama /QR menu digital Senja Kopi/. | Metadata di dalam string SVG buatan sendiri tidak akan muncul di linting a11y; hilang tanpa peringatan. |
| 25 | **Backing putih logo dan ukuran logo 20% matriks** | `src/lib/qr-render.ts:105-115`<br>`padanan canvas di QrSmartMenu.tsx:183-192` | Kotak logo = round(size*0.2) modul, diberi rect latar (warna background) berpadding 0.8 unit radius 1.2 agar kontras terjaga; komentar menegaskan EC level H mengompensasi modul tertutup (<= ~4% area). | • Aktifkan logo | `sukses` | Aktifkan logo, unduh PNG, scan dengan ponsel dari jarak 30cm; harus terbaca. | Memperbesar logo demi estetika akan merusak scan-ability; angka 0.2 harus dipertahankan apa adanya. |
| 26 | **State kosong QR saat slug/menuUrl belum ada** | `src/components/dashboard/QrSmartMenu.tsx:245-259`<br>`teks kedua di 287-291` | Bila menuUrl atau slug null, panel tetap tampil dengan heading QR dan dua paragraf penjelas ('Tautan Smart Menu belum tersedia.' + arahan melengkapi profil kafe), tanpa tombol unduh. | — | `kosong` | tests/qr-smart-menu.test.ts:120-123; atau render dengan slug null. | Cabang awal-return mudah tergabung ke render utama saat refactor, menyebabkan tombol unduh muncul dalam keadaan tidak berfungsi. |
| 27 | **Kunci ekspor ganda: kedua tombol unduh nonaktif saat proses berjalan** | `src/components/dashboard/QrSmartMenu.tsx:46, 261, 354-378` | State exporting bernilai 'png' \| 'svg' \| null; busy menonaktifkan kedua tombol (disabled:opacity-60) dan tombol yang aktif menampilkan Loader2 animate-spin sebagai ganti ikon Download. Tombol juga nonaktif bila previewSvg null. | • Klik Unduh PNG lalu segera coba klik Unduh SVG | `loading` · `disabled` | tests/qr-smart-menu.test.ts:178-191; secara manual, klik cepat dua kali tidak boleh menghasilkan dua berkas. | Tanpa penguncian ini, klik ganda pada koneksi lambat menghasilkan dua unduhan dan pesan status bertabrakan. |
| 28 | **Pesan hasil ekspor aria-live dua warna** | `src/components/dashboard/QrSmartMenu.tsx:381-383`<br>`sumber pesan 131,133,230,232,86` | Satu baris aria-live=polite dengan min-height 18px menampilkan 'PNG terunduh.' / 'SVG terunduh.' (hijau #22D3A6) atau 'Gagal mengunduh QR. Coba lagi.' (merah #FCA5A5). Pesan direset ke null setiap kali ekspor baru dimulai. | • Unduh berhasil<br>• Unduh gagal | `sukses` · `error` | Blokir canvas (atau jalankan di jsdom) untuk memicu jalur gagal; pesan merah harus muncul dan tidak menggeser tata letak. | min-height 18px mencegah layout shift; pesan sering diganti toast global sehingga konteks 'gagal ekspor mana' hilang. |
| 29 | **Petunjuk cetak QR** | `src/components/dashboard/QrSmartMenu.tsx:385-387` | Teks statis 'PNG 2048px siap cetak. Cetak minimal 3x3 cm agar mudah discan dari meja.' — satu-satunya panduan ukuran cetak fisik di aplikasi. | — | `sukses` | Cari string '3x3 cm' di UI setelah rebuild. | Teks kecil abu-abu yang mudah dianggap dekorasi dan dibuang; padahal ini mencegah pemilik kafe mencetak QR terlalu kecil. |
| 30 | **Target sentuh minimum 44px pada seluruh kontrol QR** | `src/components/dashboard/QrSmartMenu.tsx:262-267 (chip()), 315, 322-325, 342, 359, 373, 398, 473` | Input, tombol salin, tautan buka menu, dua tombol unduh, pemicu disclosure, chip style/warna, dan baris checkbox semuanya dipaksa 44px tinggi/minHeight. | • Sentuh kontrol di perangkat mobile | `sukses` | tests/qr-smart-menu.test.ts:155-176 membaca style.minHeight setiap kontrol; jalankan tes tersebut setelah rebuild. | Nilai inline 44px terlihat seperti gaya sisa dan sering dibuang saat pindah ke utilitas Tailwind; ada tes yang akan gagal, jadi tes ini harus dipertahankan. |

### Form Pengaturan, pratinjau langsung & unggah berkas — fitur 31–43

| # | Fitur | Lokasi | Apa yang terjadi | Interaksi | State | Cara verifikasi | Risiko kalau hilang |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 31 | **Form Pengaturan: state terkontrol yang menggerakkan pratinjau langsung** | `src/components/dashboard/SettingsForm.tsx:17-23, 119-191` | Enam state (nama, alamat, greeting, logoUrl, coverUrl, reviewUrl) diinisialisasi dari baris Cafes dan setiap perubahan input langsung memperbarui pratinjau ponsel di sisi kanan, termasuk perubahan yang datang dari FileUpload lewat callback onChange. | • Ketik nama kafe -> judul di pratinjau berubah<br>• Ketik alamat -> baris MapPin muncul<br>• Ketik greeting -> baris uppercase muncul di atas nama<br>• Unggah logo/sampul -> gambar pratinjau berganti<br>• Isi URL ulasan -> kartu 'Beri Ulasan di Google' muncul | `kosong` · `sukses` | Kosongkan field greeting; baris greeting harus hilang dari pratinjau. Kosongkan nama; pratinjau menampilkan teks fallback 'Nama Kafe'. | Jika input diubah ke uncontrolled/defaultValue demi menyederhanakan form, pratinjau langsung mati diam-diam sementara form tetap tersimpan. |
| 32 | **Aturan tampil/sembunyi elemen di pratinjau kafe** | `src/components/dashboard/SettingsForm.tsx:122-126 (fallback gradien sampul), 136-140 (fallback logo 3Diner), 146-148, 149, 150-155, 157-167` | Sampul kosong diganti gradien 135deg #0A3A78->#022C60->#002355; logo kosong diganti /brand/logo-3diner-mark.svg 30x30; greeting/alamat/kartu ulasan hanya dirender bila terisi; nama kosong menampilkan 'Nama Kafe'. Ada pula scrim gradien di atas sampul dan badge 'MENU 3D · AR' di pojok kanan atas. | • Hapus isi tiap field satu per satu | `kosong` · `sukses` | Hapus logo lewat tombol X pada FileUpload; pratinjau harus jatuh ke mark 3Diner, bukan kotak putih kosong. | Fallback ini hanya tampak saat data kosong (kafe baru) sehingga tidak pernah teruji saat rebuild dengan akun demo yang datanya lengkap. |
| 33 | **Transisi halus pada gambar dan judul pratinjau** | `src/components/dashboard/SettingsForm.tsx:123, 149` | Elemen Image sampul dan judul nama kafe memakai transition-all duration-300 sehingga perubahan hasil unggahan/ketikan tidak berkedip kasar. | • Ganti sampul<br>• Ketik nama | `sukses` | Ganti foto sampul dan perhatikan pergantian gambar melembut selama ~300ms. | Kelas transisi pada elemen next/image sering hilang ketika markup pratinjau ditulis ulang. |
| 34 | **Urutan responsif: pratinjau di atas form pada layar kecil** | `src/components/dashboard/SettingsForm.tsx:41`<br>`pola sama di src/components/dashboard/AnnouncementForm.tsx:83` | Container memakai flex flex-col-reverse pada mobile (pratinjau muncul lebih dulu) lalu berubah jadi grid dua kolom [minmax(0,1fr)_350px] dengan items-start pada lg. | • Perkecil jendela di bawah breakpoint lg | `sukses` | Buka DevTools lebar 390px: mockup ponsel harus berada di atas form; pada 1440px berada di kanan. | flex-col-reverse adalah trik satu kelas; rebuild yang memakai grid saja akan menaruh pratinjau di bawah tombol simpan sehingga tak terlihat di mobile. |
| 35 | **Submit Pengaturan: server action + siklus status tombol** | `src/components/dashboard/SettingsForm.tsx:25-38, 107-115`<br>`src/lib/dashboard-actions.ts:271-287` | onSubmit mencegah default, mereset error dan saved, memanggil updateCafeSettings(new FormData(form)). Saat saving tombol beropasitas 0.7 dengan Loader2 berputar; sukses mengubah latar tombol jadi hijau #22D3A6 dengan ikon Check dan label 'Tersimpan' selama 2500ms lalu kembali ke oranye 'Simpan Perubahan' (transisi background 200ms). | • Klik Simpan Perubahan<br>• Tekan Enter di dalam input (submit form native) | `loading` · `sukses` · `error` · `disabled` | Simpan sekali dan hitung: label 'Tersimpan' harus kembali jadi 'Simpan Perubahan' setelah ~2,5 detik. Tekan Enter di field nama juga harus menyimpan. | Timer 2500ms dan pergantian warna sering diganti toast; padahal tombol ini adalah satu-satunya konfirmasi visual di dekat titik aksi. |
| 36 | **Banner error form Pengaturan** | `src/components/dashboard/SettingsForm.tsx:43-47` | Blok merah rgba(239,68,68,0.1) dengan ikon AlertCircle dan teks #FCA5A5 di paling atas form, diisi pesan dari server action. | — | `error` | Kosongkan nama kafe (hapus atribut required sementara lewat DevTools) lalu simpan; harus muncul 'Nama kafe wajib diisi.' | Ditaruh di atas form, mudah terlewat saat panel dipindah; tanpa itu kegagalan simpan jadi sunyi total. |
| 37 | **Validasi nama kafe: wajib di klien dan di server** | `src/components/dashboard/SettingsForm.tsx:56-58 (required)`<br>`src/lib/dashboard-actions.ts:275,282` | Input punya atribut required; server juga menolak dengan 'Nama kafe wajib diisi.' setelah trim (str() mengembalikan null untuk string kosong). | • Coba simpan dengan nama kosong | `error` | Isi nama dengan spasi saja lalu simpan; server harus menolak walau atribut required lolos. | Validasi ganda mudah dianggap redundan; menghapus sisi server membuka jalan menyimpan nama kosong lewat spasi. |
| 38 | **Normalisasi nilai form ke null di server + revalidatePath** | `src/lib/dashboard-actions.ts:49-54 (str), 274-286` | str() melakukan trim dan mengubah string kosong jadi null, sehingga greeting/URL ulasan/logo/cover yang dikosongkan tersimpan sebagai NULL (bukan string kosong). Setelah update sukses dipanggil revalidatePath("/dashboard/settings"). | • Kosongkan URL ulasan lalu simpan | `sukses` | Kosongkan URL ulasan, simpan, reload halaman; field harus tetap kosong dan tombol ulasan hilang dari menu pelanggan. | revalidatePath tak terlihat di UI; hilangnya membuat halaman menampilkan data lama setelah simpan+navigasi. |
| 39 | **Penjagaan kepemilikan kafe pada setiap server action** | `src/lib/dashboard-actions.ts:33-47 (getAuthCafeId), dipakai di 272, 173, 494` | Setiap aksi (updateCafeSettings, setMenuAvailability, saveAnnouncement) memanggil getAuthCafeId() yang menurunkan cafe_id dari user login; bila null mengembalikan 'Sesi tidak valid. Masuk ulang.' Update Menus/Announcements selalu memakai .eq("cafe_id", cafeId) sebagai pagar tambahan. | • Sesi kedaluwarsa lalu menekan Simpan | `izin` · `error` | Hapus cookie sesi di tab lain, lalu tekan Simpan; harus muncul pesan 'Sesi tidak valid. Masuk ulang.' bukan sukses palsu. | Ini pengaman lintas-kafe; rebuild UI yang memanggil Supabase langsung dari client bisa melewatinya sepenuhnya. |
| 40 | **Unggah logo & sampul: URL bertanda tangan langsung ke Supabase** | `src/components/dashboard/SettingsForm.tsx:73-92`<br>`src/components/dashboard/FileUpload.tsx:50-78`<br>`src/lib/dashboard-actions.ts:523-536` | FileUpload meminta createMediaUploadUrl (path `${cafeId}/${kind}/${Date.now()}-${namaAman}`) lalu meng-upload langsung browser->Supabase Storage tanpa melewati serverless. Logo menerima png/jpeg/webp/avif/svg+xml, sampul tanpa svg; hint menyebut rasio 1:1 / 16:9 dan batas 30MB. | • Klik area unggah lalu pilih berkas<br>• Seret berkas ke area unggah | `loading` · `error` · `sukses` | Unggah logo dan pantau tab Network: request PUT harus menuju domain Supabase, bukan /api milik aplikasi. | Bila diubah kembali ke uploadMenuMedia (jalur server lama, dashboard-actions.ts:539), unggahan besar jadi lambat dan bisa kena batas ukuran Vercel — tanpa perubahan tampilan apa pun. |
| 41 | **FileUpload: drag-and-drop, ganti, hapus, dan reset input** | `src/components/dashboard/FileUpload.tsx:80-85, 96-106, 108-136, 138-169` | Area unggah bereaksi pada dragOver/dragLeave/drop dengan latar oranye transparan dan border putus-putus oranye; setelah terisi menampilkan thumbnail 48px, nama berkas hasil fileName() yang membuang prefiks timestamp, tombol 'Ganti' dan tombol X 'Hapus' (mengosongkan URL dan memberi tahu pratinjau). Nilai input file direset ke '' setelah dipilih agar berkas yang sama bisa dipilih ulang. | • Seret berkas<br>• Klik area kosong<br>• Klik Ganti<br>• Klik X Hapus<br>• Pilih berkas yang sama dua kali berturut-turut | `kosong` · `loading` · `error` · `sukses` | Pilih satu berkas, hapus dengan X, lalu pilih berkas yang sama lagi — unggahan harus tetap berjalan (bukti reset e.target.value bekerja). | Reset e.target.value satu baris; tanpanya memilih berkas yang sama kedua kali tidak melakukan apa-apa dan terlihat seperti aplikasi hang. |
| 42 | **FileUpload: penanda 'Terunggah' sementara 2 detik** | `src/components/dashboard/FileUpload.tsx:76-77, 124-126` | Setelah unggah sukses, baris status berubah hijau dengan ikon CheckCircle2 dan teks 'Terunggah' selama 2000ms lalu kembali ke 'Tersimpan' abu-abu. | • Unggah berkas | `sukses` | Unggah berkas dan perhatikan teks hijau 'Terunggah' berubah jadi 'Tersimpan' setelah ~2 detik. | Perbedaan halus antara 'Terunggah' (baru saja) dan 'Tersimpan' (sudah ada sebelumnya) mudah diseragamkan jadi satu teks, menghilangkan konfirmasi aksi. |
| 43 | **Kerangka mockup ponsel untuk pratinjau (dipakai Settings & Pengumuman)** | `src/components/dashboard/PhoneMockup.tsx:15-73` | Frame 320px max-width dengan bezel radius 36px, shadow besar, dynamic island 80x20, status bar berisi jam statis '19:30' bergaya tabular-nums plus ikon sinyal/wifi/baterai, area layar scroll max-h 560px dengan scrollbar disembunyikan melalui <style> yang disuntikkan (webkit width 0 + scrollbar-width none), home indicator 110x4, label default 'Pratinjau Langsung', posisi lg:sticky lg:top-6, dan tombol opsional 'Segarkan Pratinjau' yang hanya muncul bila prop onRefresh diberikan. | • Scroll di dalam layar ponsel<br>• Scroll halaman -> mockup tetap menempel di layar besar | `sukses` | Isi konten panjang lalu scroll di dalam mockup: tidak boleh ada scrollbar terlihat. Scroll halaman Pengaturan ke bawah pada layar lebar: mockup harus tetap terlihat (sticky). | Sticky, tinggi 560px, dan penyembunyian scrollbar adalah tiga detail terpisah; kehilangannya membuat pratinjau melompat atau memunculkan scrollbar ganda yang jelek. |

### Jadwal & Diskon (/dashboard/scheduler) — fitur 44–57

| # | Fitur | Lokasi | Apa yang terjadi | Interaksi | State | Cara verifikasi | Risiko kalau hilang |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 44 | **Jadwal: query menu terurut alfabet + header halaman** | `src/app/dashboard/scheduler/page.tsx:12-23` | Menus difilter cafe_id dan diurut nama_menu ascending; header memakai DashboardPageHeader dengan subjudul yang menjelaskan aturan 'Menu di luar jadwal otomatis tersembunyi'. Container max-w-3xl (lebih sempit dari halaman lain). | — | `kosong` · `sukses` | Tambah menu bernama 'Aaa' dan 'Zzz'; urutan daftar harus alfabetis, bukan urutan pembuatan. | Urutan alfabet dan lebar max-w-3xl mudah berubah saat halaman disamakan dengan halaman lain, membuat pemilik kafe kehilangan menu di daftar panjang. |
| 45 | **Jadwal: state kosong dengan CTA tambah menu** | `src/components/dashboard/SchedulerClient.tsx:420-440`<br>`src/components/dashboard/system/DashboardStates.tsx:9-18` | Tanpa menu, tampil DashboardEmptyState dengan ikon CalendarDays 36px strokeWidth 1.4, judul 'Belum ada menu', hint 'Tambah menu dulu untuk mengatur jadwal & diskon', dan tombol Link ke /dashboard/menu/new berlatar var(--orange). | • Klik 'Tambah Menu' -> navigasi ke form menu baru | `kosong` | Uji dengan akun kafe yang belum punya menu; harus ada CTA, bukan halaman kosong. | Empty state hanya terlihat pada akun baru sehingga hampir tak pernah dibuka saat pengembangan. |
| 46 | **Jadwal: inferensi mode awal dari data tersimpan** | `src/components/dashboard/SchedulerClient.tsx:48-64` | Mode baris ditentukan 'scheduled' bila schedule_days berisi entri atau schedule_start/schedule_end terisi, selain itu 'always'. Hari dipecah dari string koma dengan trim dan buang entri kosong; is_active dianggap true kecuali persis false. | — | `kosong` · `sukses` | Simpan menu dengan hanya jam mulai (tanpa hari), reload halaman; baris harus kembali dalam mode 'Jadwal', bukan 'Selalu'. | Logika inferensi ini yang menjaga data lama tetap terbaca; jika rebuild menyimpan mode ke kolom baru, semua jadwal existing tampak kembali ke 'Selalu' dan tersimpan null saat user menekan Simpan. |
| 47 | **Jadwal: sakelar aktif/nonaktif menu dengan knop beranimasi** | `src/components/dashboard/SchedulerClient.tsx:138-175` | Sakelar 36x20 di slot ikon panel: latar hijau transparan saat aktif, putih transparan saat nonaktif; knop 14px bergeser left 1px->17px dengan transisi 180ms cubic-bezier(0.22,1,0.36,1). Punya aria-pressed dan aria-label yang berubah ('Aktifkan menu ini' / 'Nonaktifkan menu ini'). | • Klik sakelar<br>• Fokus keyboard + Enter/Space | `sukses` · `disabled` | Matikan sakelar dan periksa aria-pressed=false di inspector; badge status harus berubah jadi 'Tersembunyi'. | aria-label dinamis dan aria-pressed sering hilang saat diganti komponen Switch generik yang tidak menerangkan objeknya. |
| 48 | **Jadwal: peredupan seluruh badan kartu saat menu nonaktif** | `src/components/dashboard/SchedulerClient.tsx:194-201` | Badan kartu memakai opacity 0.38 dan pointerEvents 'none' dengan transisi 200ms ketika is_active false, sehingga kontrol jadwal/diskon tidak bisa diubah selama menu dimatikan. Judul menu juga berubah warna ke var(--dash-muted). | • Matikan sakelar lalu coba klik chip hari | `disabled` | Matikan menu, coba klik chip hari dan input jam — tidak boleh ada reaksi; nyalakan lagi, kontrol kembali bisa dipakai. | pointer-events:none adalah satu-satunya penghalang interaksi (bukan atribut disabled); rebuild yang hanya menyalin opacity akan membuat kontrol tampak mati tapi tetap bisa diubah. |
| 49 | **Jadwal: kontrol segmented Selalu / Jadwal** | `src/components/dashboard/SchedulerClient.tsx:202-250` | Dua tombol dalam wadah radius 12px berlatar #0A1525; yang aktif berlatar rgba(253,80,2,0.16) dan teks oranye, dengan ikon Zap (Selalu) dan CalendarDays (Jadwal); transisi warna 150ms. Blok kontrol jadwal hanya dirender pada mode 'scheduled'. | • Klik Selalu<br>• Klik Jadwal | `sukses` | Klik Selalu lalu Simpan; periksa di database kolom schedule_days/start/end menjadi NULL (lihat SchedulerClient.tsx:100-105). | Mode 'always' bukan sekadar tampilan: saat simpan ia menulis NULL ke tiga kolom. Rebuild yang menyembunyikan kontrol tanpa mengosongkan payload akan meninggalkan jadwal lama tetap aktif di halaman pelanggan. |
| 50 | **Jadwal: 7 chip hari dengan label singkat, aria-label penuh, dan pilih semua** | `src/components/dashboard/SchedulerClient.tsx:26-34, 78-93, 256-298` | Chip Sn/Sl/Rb/Km/Jm/St/Mg (nilai ISO 1..7) masing-masing aria-label nama hari lengkap dan aria-pressed; tombol teks di kanan berubah antara 'Pilih semua' dan 'Hapus semua' beserta warnanya (oranye saat semua aktif) dan menghapus/mengisi seluruh set sekaligus. | • Klik chip hari (toggle)<br>• Klik 'Pilih semua'<br>• Klik 'Hapus semua' | `kosong` · `sukses` | Pilih semua hari, tombol harus berubah jadi 'Hapus semua'; simpan dan cek schedule_days berisi '1,2,3,4,5,6,7'. | Pemetaan nilai 1..7 harus persis ISO (1=Senin, 7=Minggu) karena dibaca menu-availability.ts:12-19; mengubah urutan chip diam-diam akan menyembunyikan menu pada hari yang salah. |
| 51 | **Jadwal: input jam mulai/selesai bertipe time** | `src/components/dashboard/SchedulerClient.tsx:300-322` | Dua input type="time" dalam grid 2 kolom, ber-id unik per menu (`sched-start-${id_menu}`) dan terhubung ke label lewat Field htmlFor. | • Isi jam mulai<br>• Isi jam selesai<br>• Kosongkan salah satu | `kosong` · `sukses` | Isi 22:00-02:00 lalu simpan; periksa perilaku lintas tengah malam pada logika ketersediaan (menu-availability.ts:31-34). | id unik per menu mencegah label saling tertukar saat ada banyak baris; id yang di-hardcode saat rebuild akan membuat klik label memindahkan fokus ke baris lain. |
| 52 | **Jadwal: pesan saat mode Jadwal dipilih tapi belum ada isinya** | `src/components/dashboard/SchedulerClient.tsx:324-328` | Bila mode 'scheduled' namun tidak ada hari dan tidak ada jam, muncul teks kecil warna #3E566E: 'Belum ada jadwal dipilih. Menu tetap tampil kapan saja.' — menjelaskan bahwa jadwal kosong sama dengan selalu tampil. | • Pilih mode Jadwal tanpa mengisi apa pun | `kosong` | Pilih Jadwal, jangan isi apa pun; teks itu harus muncul dan badge tetap 'Tampil'. | Ini satu-satunya penjelasan atas perilaku non-intuitif (jadwal kosong = selalu tampil); tanpa itu pemilik kafe mengira menu tersembunyi. |
| 53 | **Jadwal: input diskon 0-100 dengan penjepitan nilai dan umpan balik harga** | `src/components/dashboard/SchedulerClient.tsx:332-366` | Input number lebar 116px dengan min=0 max=100, dijepit lagi di handler memakai Math.min(100, Math.max(0, Number(...))), kelas tabular-nums, ikon Percent yang pointer-events none di dalam field, dan teks oranye 'Harga otomatis turun X%' yang hanya muncul bila diskon > 0. | • Ketik angka diskon<br>• Tempel nilai 999 (dijepit ke 100)<br>• Ketik -5 (dijepit ke 0) | `kosong` · `sukses` | Tempelkan '250' ke kolom diskon; nilai harus langsung berubah jadi 100 dan teks bantuan menampilkan 100%. | Penjepitan di handler (bukan hanya atribut min/max) mencegah nilai gila lolos lewat paste/keyboard; hilangnya menyebabkan harga negatif di halaman pelanggan. |
| 54 | **Jadwal: badge status 'Tampil / Tersembunyi' dihitung dari state yang belum disimpan** | `src/components/dashboard/SchedulerClient.tsx:117-128, 187-192`<br>`src/components/dashboard/system/StatusBadge.tsx:39-53` | Sebuah previewMenu dibangun dari state saat ini (termasuk perubahan yang belum disimpan) lalu dilewatkan ke isMenuAvailableNow; hasilnya jadi StatusBadge kind active/inactive dengan label khusus 'Tampil'/'Tersembunyi' plus titik warna (makna tidak hanya lewat warna). | • Ubah hari/jam/sakelar -> badge berubah seketika sebelum disimpan | `sukses` · `disabled` | Atur jadwal hanya hari ini dengan jam yang sudah lewat; badge harus langsung berubah jadi 'Tersembunyi' tanpa menyimpan. | Perhitungan pratinjau langsung ini adalah fitur andalan halaman; rebuild yang menghitung dari prop menu (data tersimpan) akan menampilkan status yang salah dan tidak ada yang menyadarinya. |
| 55 | **Jadwal: pelacakan dirty dan footer simpan yang muncul kondisional** | `src/components/dashboard/SchedulerClient.tsx:69-93, 388-413` | Setiap perubahan memanggil mark()/toggleDay() yang menyetel dirty=true, membatalkan saved, dan menghapus error. Footer simpan (dengan garis pemisah atas) hanya dirender bila dirty atau saved; tombol nonaktif saat pending atau tidak dirty, label berubah 'Simpan perubahan' -> 'Menyimpan…' -> 'Tersimpan' dengan latar hijau #1DBF94 dan reset otomatis 2500ms. | • Ubah apa pun -> footer muncul<br>• Klik Simpan perubahan<br>• Tunggu 2,5 detik -> footer hilang lagi | `disabled` · `loading` · `sukses` · `error` | Buka halaman: tidak boleh ada tombol simpan di kartu mana pun. Ubah satu chip hari: tombol muncul hanya di kartu itu. Simpan: label berganti dan footer hilang setelah ~2,5 detik. | Pola 'tombol simpan per baris yang hanya muncul saat berubah' sangat mudah diganti tombol simpan global; itu mengubah model mental dan berpotensi menyimpan seluruh menu sekaligus. |
| 56 | **Jadwal: penyimpanan lewat useTransition (tanpa memblokir UI)** | `src/components/dashboard/SchedulerClient.tsx:66, 95-115`<br>`src/lib/dashboard-actions.ts:168-183` | save() dibungkus startTransition sehingga status pending berasal dari React transition, bukan state manual; server action setMenuAvailability memperbarui hanya kolom yang dikirim dengan pagar .eq(cafe_id) lalu memanggil revalidatePath untuk /dashboard/scheduler dan /dashboard/menu. | • Klik Simpan pada beberapa kartu berbeda hampir bersamaan | `loading` · `error` · `sukses` | Simpan satu baris lalu buka /dashboard/menu; status aktif/diskon menu harus sudah ikut terbarui (bukti revalidatePath ganda). | revalidatePath ke dua rute adalah satu-satunya penghubung antara halaman Jadwal dan halaman Menu; hilangnya menyebabkan halaman Menu menampilkan data basi. |
| 57 | **Jadwal: pesan error inline per kartu** | `src/components/dashboard/SchedulerClient.tsx:368-386` | Kotak merah lembut (rgba(239,68,68,0.08) + border 0.16) dengan ikon AlertCircle #F87171 di dalam kartu yang gagal disimpan; error otomatis dibersihkan pada perubahan berikutnya. | — | `error` | Putuskan jaringan lalu tekan Simpan; error harus muncul di kartu itu saja, bukan sebagai toast global tanpa konteks. | Error per baris menjelaskan menu mana yang gagal; menggantinya dengan toast tunggal menghilangkan informasi itu saat menyimpan banyak baris. |

### Logika ketersediaan & harga diskon (dipakai lintas halaman) — fitur 58–59

| # | Fitur | Lokasi | Apa yang terjadi | Interaksi | State | Cara verifikasi | Risiko kalau hilang |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 58 | **Logika ketersediaan menu berbasis WIB (Asia/Jakarta)** | `src/lib/menu-availability.ts:8-38` | isMenuAvailableNow: (1) is_active===false -> tersembunyi; (2) filter hari memakai Intl.DateTimeFormat timeZone Asia/Jakarta weekday short lalu memetakan Sun..Sat ke 0..6 dan mengonversi Minggu jadi 7 (ISO); (3) filter jam memakai hourCycle h23 + menit WIB, dibandingkan sebagai string 'HH:MM'; jendela sehari (start<=end) memakai aturan cur<start\|\|cur>end, jendela lintas tengah malam (start>end) memakai cur<start&&cur>end; (4) jadwal kosong berarti selalu tersedia. | — | `sukses` · `kosong` | Ubah zona waktu OS ke UTC lalu buka halaman Jadwal; badge 'Tampil/Tersembunyi' harus tetap mengikuti waktu Jakarta, bukan waktu perangkat. Uji jendela 22:00-02:00 pada pukul 23:00 dan 01:00 (keduanya harus tampil). | Fungsi ini dipakai juga oleh halaman pelanggan (src/lib/supabase.ts:41). Menggantinya dengan new Date().getDay() lokal akan menyembunyikan menu pada jam yang salah untuk pengguna di luar WIB — kegagalan yang tidak terlihat dari dashboard. |
| 59 | **Perhitungan harga diskon yang dipakai lintas halaman** | `src/lib/menu-availability.ts:41-50`<br>`konsumen: src/components/MenuCard.tsx:25-26, src/components/MenuBrowser.tsx:34,190, src/components/MenuOrderPanel.tsx:29, src/app/[slug]/[menu_id]/page.tsx:102-104` | effectivePrice memberlakukan diskon hanya bila 0<pct<100 dan membulatkan ke rupiah bulat (Math.round); hasilnya dipakai kartu menu, daftar promo, panel pesan, dan halaman detail. hasDiscount memakai aturan batas yang sama. | — | `sukses` | Set diskon 100 pada satu menu: harga di halaman pelanggan harus tetap harga asli (bukan nol) karena batas pct<100. Set 33% pada harga 25000: hasil harus 16750 (bulat). | Angka diskon diedit di halaman Jadwal tapi konsekuensinya muncul di halaman pelanggan; rebuild dashboard yang menulis pct=100 atau menghitung diskon sendiri akan memutus konsistensi harga. |

### Pengumuman (/dashboard/announcements) — fitur 60–69

| # | Fitur | Lokasi | Apa yang terjadi | Interaksi | State | Cara verifikasi | Risiko kalau hilang |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 60 | **Pengumuman: ambil satu banner terbaru (maybeSingle)** | `src/app/dashboard/announcements/page.tsx:12-20` | Query Announcements difilter cafe_id, diurut updated_at descending, limit 1, maybeSingle — sehingga kafe tanpa pengumuman tidak menghasilkan error dan form tampil dalam keadaan baru. | — | `kosong` · `sukses` | Akun tanpa pengumuman harus membuka form kosong (warna default oranye, jenis Info, sakelar mati), bukan error. | single() alih-alih maybeSingle() akan melempar error untuk kafe baru; urutan updated_at desc menentukan baris mana yang diedit bila pernah ada beberapa baris. |
| 61 | **Pengumuman: pemilih jenis 4 kolom dengan ikon** | `src/components/dashboard/AnnouncementForm.tsx:96-122`<br>`src/lib/announcement-types.ts:13-22` | Grid 4 tombol: Info (Megaphone), Promo (Tag), Acara (CalendarDays), Penting (AlertTriangle). Terpilih ditandai latar rgba(253,80,2,0.1) + border oranye dan warna ikon/teks berubah; transisi 150ms. typeMeta() punya fallback ke 'info' untuk nilai tak dikenal. | • Klik salah satu jenis | `sukses` | Pilih 'Penting'; ikon segitiga peringatan harus ikut muncul di banner pratinjau (dan nanti di halaman pelanggan). | Ikon jenis dipakai bersama oleh pratinjau dashboard dan banner pelanggan (src/components/AnnouncementBanner.tsx:18-19); mengganti ikon hanya di satu tempat memutus konsistensi. |
| 62 | **Pengumuman: 5 template cepat yang mengisi tiga field sekaligus** | `src/components/dashboard/AnnouncementForm.tsx:27-33, 52-57, 124-143` | Template Live Music, Promo Hari Ini, Jam Buka, Menu Baru, dan Libur; sekali klik mengisi pesan, jenis, dan warna sekaligus serta mereset flag saved. Setiap chip menampilkan titik warna dengan halo boxShadow `${warna}33`. | • Klik satu chip template | `sukses` | Klik 'Libur': pesan, jenis 'Penting', dan warna merah #B91C1C harus berubah bersamaan, dan pratinjau ikut berubah. | Isi teks template berbahasa Indonesia yang spesifik untuk kafe; rebuild yang menyalin hanya labelnya akan kehilangan pesan siap pakai — fitur yang paling sering dipakai pemilik kafe non-teknis. |
| 63 | **Pengumuman: batas 120 karakter dengan penghitung dan peringatan warna** | `src/components/dashboard/AnnouncementForm.tsx:35, 50, 146-165` | Textarea 3 baris, resize dinonaktifkan, dijaga ganda oleh atribut maxLength={120} dan slice(0,120) di onChange (menahan paste). Penghitung 'n/120' tabular-nums melayang di pojok kanan bawah, berubah dari #41557A ke amber #F0A742 ketika sisa <= 15 karakter. | • Ketik pesan<br>• Tempel teks lebih dari 120 karakter | `kosong` · `sukses` | Tempel 300 karakter; hanya 120 pertama masuk dan penghitung berwarna amber di 105+. | Batas 120 menjaga banner satu-dua baris di ponsel; menghapus slice() membuat paste lolos dan banner pelanggan pecah (line-clamp-2 hanya memotong tampilan pratinjau). |
| 64 | **Pengumuman: 5 warna preset + pemilih warna kustom + tampilan hex** | `src/components/dashboard/AnnouncementForm.tsx:12-18, 167-207` | Preset Oranye/Navy/Teal/Merah/Ungu sebagai kotak 36px; yang aktif mendapat outline putih 2px dengan offset 2px dan tanda Check yang warnanya dihitung readableOn(preset) agar tetap terbaca di atas warna itu. Perbandingan aktif dilakukan case-insensitive (toUpperCase). Warna kustom memakai <input type="color"> transparan yang menutupi label bertanda Plus dan border putus-putus. Nilai hex ditampilkan uppercase dengan tabular-nums. | • Klik preset warna<br>• Klik kotak Plus untuk membuka color picker OS<br>• Ubah warna kustom -> hex dan pratinjau berubah | `sukses` | Pilih warna kuning terang lewat picker; tanda centang/teks pratinjau harus berubah jadi navy gelap, bukan putih (bukti readableOn bekerja). | Input color yang disembunyikan di balik label adalah trik CSS; rebuild yang menggantinya dengan tombol biasa menghilangkan kemampuan warna bebas. |
| 65 | **Kontras teks otomatis berbasis luminance WCAG** | `src/lib/contrast.ts:4-18`<br>`dipakai di AnnouncementForm.tsx:48-49,185,269,273 dan AnnouncementBanner.tsx:20-21` | readableOn menghitung luminance relatif (koefisien 0.2126/0.7152/0.0722 dengan linearisasi sRGB) dan mengembalikan navy #022C60 bila L>0.55, selain itu putih; readableSoftOn memberi versi transparan (rgba 0.62 / 0.78) untuk ikon dan tombol tutup. Hex tidak valid (panjang != 6) jatuh ke putih. | • Ganti warna latar banner | `sukses` | Set warna #FFFF00 lalu #000080; teks pratinjau harus berganti navy lalu putih secara otomatis. | Fungsi murni tanpa tampilan sendiri — mudah dilewati saat pratinjau ditulis ulang, menghasilkan teks putih di atas kuning yang tak terbaca pelanggan. |
| 66 | **Pengumuman: sakelar 'Tampilkan ke pelanggan' dengan role=switch** | `src/components/dashboard/AnnouncementForm.tsx:209-235` | Baris kartu berisi judul dan subteks yang berubah ('Banner aktif di halaman menu' / 'Banner disembunyikan saat ini'), dengan tombol role="switch" aria-checked dan knop putih 20px yang bergeser 2px->22px dengan transisi 180ms cubic-bezier(0.22,1,0.36,1); latar hijau #22D3A6 saat aktif. | • Klik sakelar<br>• Aktifkan dengan keyboard | `sukses` | Nyalakan sakelar dan periksa aria-checked=true; pratinjau harus berganti dari 'Banner nonaktif' ke banner berwarna. | Teks penjelas di bawah judul adalah satu-satunya keterangan status; sering dihapus saat komponen Switch standar dipasang. |
| 67 | **Pengumuman: pratinjau banner yang meniru tampilan pelanggan persis** | `src/components/dashboard/AnnouncementForm.tsx:252-296`<br>`pembanding src/components/AnnouncementBanner.tsx:23-45` | Di dalam PhoneMockup: sampul kafe palsu dengan overlay .grain, lalu banner asli (ikon jenis, teks line-clamp-2, ikon X berwarna soft) memakai warna dan kontras yang sama dengan komponen pelanggan, plus grid menu palsu 4 kartu berlatar #F6F8FB. Bila nonaktif, tampil pita abu #E0E7EE bertuliskan 'Banner nonaktif' dengan ikon EyeOff. Pesan kosong menampilkan placeholder 'Pesan pengumuman akan tampil di sini'. | • Ketik pesan -> banner berubah realtime<br>• Matikan sakelar -> pita nonaktif<br>• Kosongkan pesan -> placeholder | `kosong` · `disabled` · `sukses` | Bandingkan pratinjau dengan halaman /[slug] sungguhan setelah menyimpan: warna, ikon, dan tombol X harus sama. | Pratinjau dan banner pelanggan adalah dua komponen terpisah yang harus tetap sinkron; rebuild yang mengubah satu sisi menghasilkan janji palsu ('WYSIWYG' yang bohong). |
| 68 | **Pengumuman: submit membangun FormData manual (insert vs update)** | `src/components/dashboard/AnnouncementForm.tsx:59-78`<br>`src/lib/dashboard-actions.ts:493-515` | Form tidak mengandalkan atribut name; handler menyusun FormData sendiri (id hanya bila ada, message, bg_color, type, is_active sebagai string 'true'/'false'). Server menolak pesan kosong ('Pesan pengumuman wajib diisi.'), memvalidasi type terhadap daftar putih info/promo/event/warning (fallback 'info'), memberi bg_color default #FD5002, menstempel updated_at ISO, lalu update bila ada id (dengan pagar cafe_id) atau insert bila belum ada, diikuti revalidatePath('/dashboard/announcements'). | • Klik Simpan Pengumuman<br>• Tekan Enter di dalam textarea (tidak submit, karena textarea) | `loading` · `error` · `sukses` · `izin` | Simpan pertama kali (insert) lalu ubah pesan dan simpan lagi; jumlah baris di tabel Announcements harus tetap satu — bukti id ikut terkirim. | Bila id tidak lagi diikutkan saat rebuild, setiap simpan membuat baris baru; halaman pelanggan tetap terlihat benar (ambil terbaru) sehingga bug menumpuk diam-diam. |
| 69 | **Pengumuman: siklus status tombol simpan (sama dengan Settings)** | `src/components/dashboard/AnnouncementForm.tsx:237-249` | Tombol submit nonaktif saat saving dengan opacity 0.7 dan Loader2 berputar; sukses berubah hijau #22D3A6 dengan ikon Check dan label 'Tersimpan' selama 2500ms sebelum kembali ke 'Simpan Pengumuman'. Klik template juga mereset flag saved. | • Klik Simpan Pengumuman | `loading` · `sukses` · `error` · `disabled` | Simpan, lalu segera klik sebuah template: label harus langsung kembali dari 'Tersimpan' ke normal. | Reset saved di applyTemplate adalah detail kecil; tanpanya tombol menampilkan 'Tersimpan' padahal isi form sudah berubah — konfirmasi palsu. |

### Sistem desain & navigasi bersama — fitur 70–75

| # | Fitur | Lokasi | Apa yang terjadi | Interaksi | State | Cara verifikasi | Risiko kalau hilang |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 70 | **Bahasa panel dashboard: head band, body, dan slot aksi** | `src/components/dashboard/system/DashboardPanel.tsx:14-29`<br>`src/app/globals.css:431-444`<br>`pemakaian di SettingsForm.tsx:50-55,68-72,96-101, SchedulerClient.tsx:136-193, AnnouncementForm.tsx:86` | Semua panel di area ini memakai DashboardPanel: head band uppercase letter-spacing 0.06em dengan slot ikon kiri (Store oranye, Star kuning terisi, QrCode) dan slot actions kanan (StatusBadge pada Scheduler); body default padding 16px yang bisa dioverride lewat bodyClassName. Judul panel di Scheduler sengaja di-override jadi normal-case untuk menampilkan nama menu. | — | `sukses` | Bandingkan head band di Pengaturan, Jadwal, dan Pengumuman: tinggi, huruf kapital, dan padding harus identik. Judul kartu di Jadwal harus tetap huruf normal (bukan KAPITAL). | Override normal-case pada judul Scheduler mudah hilang; nama menu berubah jadi HURUF BESAR SEMUA dan tidak terbaca. |
| 71 | **Bahasa field form: label uppercase kecil + hint** | `src/components/dashboard/system/fields.tsx:3-38`<br>`pemakaian di SettingsForm.tsx:56,59,62,102, SchedulerClient.tsx:302,312,333, AnnouncementForm.tsx:97,125,146,168` | Field merender label 11px uppercase tracking-wider terhubung htmlFor, lalu children, lalu hint 11px di bawahnya. dashInputClass/dashInputStyle menetapkan padding, radius 12px, latar var(--dash-raised), border putih 0.1, dan warna teks. | • Klik label -> fokus pindah ke input terkait | `sukses` | Klik teks label 'Mulai' di halaman Jadwal; fokus harus lompat ke input jam pada baris yang sama. | Hubungan htmlFor/id sering putus saat markup ditulis ulang, terutama pada Scheduler yang id-nya dinamis per menu. |
| 72 | **Fokus input bergaya oranye (ring 3px)** | `src/app/globals.css:480-481` | .dash-input:focus mengganti border jadi rgba(253,80,2,0.6) dan menambah box-shadow ring 3px rgba(253,80,2,0.13) sambil menonaktifkan outline default, dengan transisi 150ms. | • Tab antar input di ketiga halaman | `sukses` | Tab melewati seluruh form Pengaturan tanpa menyentuh mouse; setiap input harus punya ring oranye yang jelas terlihat. | Karena outline default dinonaktifkan, hilangnya aturan ini menyebabkan tidak ada indikator fokus sama sekali — kegagalan aksesibilitas total yang tidak terlihat bagi pengguna mouse. |
| 73 | **Umpan balik tekan pada tombol (press, chip, btn, icon-btn, card hover)** | `src/app/globals.css:461-488`<br>`kelas dipakai di QrSmartMenu.tsx:322,337,358,368,397,417-435, SchedulerClient.tsx:281,397, AnnouncementForm.tsx:107,131,177,191, FileUpload.tsx:129` | .dash-press mengecil ke scale(0.96) saat ditekan, .dash-chip ke 0.95, .dash-btn ke 0.97 plus brightness 1.08 saat hover (hanya perangkat ber-hover), .dash-icon-btn menggelapkan latar saat hover, dan .dash-card mengangkat kartu 2px saat hover. | • Tekan dan tahan tombol apa pun<br>• Arahkan kursor ke kartu menu di Jadwal | `sukses` | Tekan tombol Unduh PNG dan tahan; tombol harus mengecil sedikit. Arahkan kursor ke kartu Jadwal; kartu terangkat dan border menyala. | Query @media (hover: hover) mencegah efek hover 'nyangkut' di layar sentuh; rebuild yang memakai hover Tailwind biasa menyebabkan tombol tetap menyala setelah disentuh di ponsel. |
| 74 | **Entri navigasi sidebar untuk ketiga halaman (label + deskripsi)** | `src/components/dashboard/DashboardShell.tsx:37-39` | Nav memuat 'Pengumuman' (desc 'Banner info pelanggan', ikon Megaphone), 'Jadwal & Diskon' (desc 'Jam tayang & promo otomatis', ikon CalendarClock), dan 'Pengaturan' (desc 'Profil & branding kafe', ikon Settings) — deskripsi tampil di menu mobile/command palette. | • Buka sidebar/menu mobile dan pilih salah satu | `sukses` | Buka menu navigasi di lebar mobile: setiap entri harus menampilkan label dan deskripsi pendeknya. | Deskripsi hanya terlihat di sebagian tampilan nav; mudah hilang saat sidebar dirombak, dan pengguna baru kehilangan petunjuk isi tiap halaman. |
| 75 | **Anchor #qr-menu pada panel QR** | `src/components/dashboard/QrSmartMenu.tsx:247, 270` | Section QR memiliki id="qr-menu" dan aria-label="QR Smart Menu" pada kedua cabang render (kosong dan normal), sehingga bisa dituju lewat tautan dalam (misal /dashboard/settings#qr-menu) dan dikenali sebagai landmark. | • Buka /dashboard/settings#qr-menu -> halaman menggulir ke panel QR | `kosong` · `sukses` | Muat /dashboard/settings#qr-menu; halaman harus tergulir langsung ke panel QR. | id ini bisa dirujuk dari onboarding/CTA lain; menghapusnya memutus tautan dalam tanpa error apa pun. |

---

# CHECKLIST VERIFIKASI MANUAL FINAL

Diringkas dari kolom **caraVerifikasi** seluruh **564 fitur** (8 area), disusun ulang jadi urutan yang bisa dijalankan **satu orang dalam satu sesi**. Urutannya sengaja mengikuti alur kerja, bukan urutan inventaris: siapkan dulu, uji gerbang sesi sekali untuk semua rute, lalu telusuri rute satu per satu dari kiri-atas sidebar ke bawah, terakhir sapu bersih hal-hal lintas rute.

**Aturan main**

- Kerjakan **berurutan**. Beberapa langkah menyiapkan data untuk langkah berikutnya (mis. pesanan uji di §5 dipakai lagi di §4 dan §6).
- Satu kotak yang gagal = **fitur hilang**, bukan "bug kecil". Catat nomornya, jangan lanjut tanpa mencatat.
- Estimasi realistis: **§0–§2 sekitar 40 menit**, **§3–§12 sekitar 3–4 jam**, **§13–§15 sekitar 45 menit**.
- Perangkat: satu laptop (Chrome + DevTools), satu ponsel Android/iOS untuk scan QR dan uji sentuh, satu browser kedua/incognito untuk uji realtime multi-sesi.

---

## §0. Persiapan sesi

- [ ] Jalankan `cd C:\Kerja\3Diner\App && npm run dev`; pastikan build bersih tanpa warning hydration di console.
- [ ] Siapkan **tiga akun**: (A) owner kafe berdata penuh, (B) owner **tanpa baris di tabel `Cafes`**, (C) owner kafe kedua (untuk uji kepemilikan lintas kafe).
- [ ] Pada kafe A pastikan ada: ≥8 menu (satu tanpa foto, satu tanpa kategori, satu bernama ≥60 karakter, satu bervarian, satu ber-GLB, satu tanpa GLB), ≥6 bahan inventory (satu stok 0, satu stok = minimum, satu stok aman), ≥20 pesanan dengan campuran status/metode bayar, dan event analitik minimal 14 hari ke belakang.
- [ ] Siapkan berkas uji di desktop: `menu.jpg`, `menu.pdf` (>100 item), `rusak.txt`, `besar.pdf` (>15MB), `transparan.png` (ber-alpha), `opaque.png`, `logo.png`, `sampul.jpg`, satu `.usdz`.
- [ ] Buka DevTools; siapkan panel **Network** (throttle Slow 3G), **Rendering** (Emulate `prefers-reduced-motion`, emulasi buta warna/grayscale), **Application → Local Storage**, dan **Animations**.
- [ ] Catat nilai awal **AiCreditMeter** (sisa jatah AI) — dipakai sebagai baseline uji refund di §8.
- [ ] Buka Supabase Studio di tab terpisah (untuk manipulasi baris langsung: `Orders`, `Inventory_Items`, `Cafes`, `Announcements`).

---

## §1. Gerbang sesi & routing (uji sekali, berlaku semua rute)

- [ ] Hapus cookie `sb-*` di DevTools, lalu buka langsung via address bar: `/dashboard`, `/dashboard/revenue`, `/dashboard/orders`, `/dashboard/menu`, `/dashboard/menu/new`, `/dashboard/inventory`, `/dashboard/settings`, `/dashboard/scheduler`, `/dashboard/announcements`. **Kesembilannya harus mendarat di `/login`** — bukan shell kosong, bukan blank, bukan 500.
- [ ] Pada `/dashboard/revenue` khususnya: pastikan **tidak ada request data Orders** yang sempat terkirim sebelum redirect.
- [ ] Login, lalu ketik `/login` di address bar → harus langsung dilempar balik ke `/dashboard` (middleware).
- [ ] Klik **Keluar**, tunggu sampai di `/login`, tekan **Back** browser → harus tetap di `/login`, bukan menampilkan snapshot dashboard.
- [ ] Login sebagai akun **B (tanpa kafe)** → `/dashboard` menampilkan "Belum ada kafe terhubung" (bukan crash, bukan dashboard berangka 0); sidebar menampilkan mark + teks "3Diner"; footer sidebar hanya berisi tombol Keluar tanpa tombol oranye; `/dashboard/menu` tetap render dengan empty state; `/dashboard/revenue` menampilkan empty state penuh **tapi tombol Ekspor + DateRangePicker tetap ada di header**.
- [ ] Ambil `id_menu` milik kafe C, buka `/dashboard/menu/<id>/edit` sebagai akun A → harus **404**, bukan form kosong atau 500.
- [ ] Ubah sementara slug di query Settings ke nilai acak → halaman menampilkan "Kafe tidak ditemukan", bukan exception.
- [ ] Tambahkan `console.log` sementara di `getDashboardCafeContext` / `getCafeBySlug`, lalu muat `/dashboard`, `/dashboard/revenue`, `/dashboard/orders`, `/dashboard/inventory`, `/dashboard/settings` masing-masing sekali → log harus muncul **tepat satu kali per request** meski layout dan page sama-sama memanggilnya (React `cache()`).
- [ ] Buka `/dashboard`, tab Network filter **RSC** → harus ada **8 request prefetch** untuk kedelapan rute nav segera setelah paint, tanpa hover apa pun. Lalu klik Penjualan → transisi hampir tanpa jeda.

---

## §2. Shell dashboard — sidebar, header, sheet mobile, portal, toast

- [ ] Hitung item sidebar: **tepat 8**, label Indonesia persis (termasuk **"Jadwal & Diskon"** dengan ampersand), urutan tidak berubah.
- [ ] Buka `/dashboard/menu/new` → item **Menu** tersorot (background raised + ikon oranye + font 600) **dan** item **Analitik** tidak tersorot (exact match untuk root, prefix untuk sub-rute).
- [ ] Throttle Slow 3G, klik "Penjualan" → ikon dompet berdenyut halus dan `<a>` punya `aria-busy="true"` sampai halaman berganti.
- [ ] Pindah ke kedelapan rute; `<h1>` header harus berubah sesuai label NAV. Perkecil ke lebar mobile → kotak ikon oranye hilang, judul tetap ada.
- [ ] Di 1280px: ukur sidebar = **236px**, tinggi 100dvh, sticky (scroll halaman Analitik sampai bawah, sidebar tidak ikut tergulung). Kolom utama yang scroll, bukan `<body>`; `<body>` tidak boleh punya scrollbar horizontal.
- [ ] Header aplikasi sticky dengan backdrop blur: konten samar terlihat menembusnya, tidak ada elemen halaman yang menimpanya kecuali z-index > 30.
- [ ] Viewport 375px: tap hamburger → sheet **meluncur dari KIRI**; tekan Esc → tertutup; buka lagi, klik area gelap → tertutup; buka lagi, klik "Menu" → pindah rute **dan** tertutup. Pastikan **tidak ada tombol X** di pojok sheet.
- [ ] Buka sheet, cek console → **tidak boleh ada** warning `DialogContent requires a DialogTitle`. Inspect DOM: `[data-slot="sheet-content"]` harus di dalam `#dash-portal-root`, bukan anak langsung `<body>`; tidak ada warning hydration mismatch.
- [ ] Di mobile: buka sheet, klik "Pesanan", tekan **Back** browser → sheet tetap tertutup dan tidak ada ikon nav yang masih berdenyut.
- [ ] Viewport 768px: Tab ke hamburger → tooltip muncul (delay ~300ms, tidak instan); ukur tombol ≥ **44×44**; tooltip berada di `#dash-portal-root` dan berlatar gelap, bukan putih; tooltip punya panah kecil menunjuk tombol.
- [ ] Kafe berlogo: gambar mengisi kotak penuh (`object-cover`). Ganti nama kafe jadi 40 karakter → terpotong ellipsis, tidak wrap, tinggi blok brand tetap 60px.
- [ ] Klik "Lihat Menu Kafe" → tab baru ke `/{slug}`, tab dashboard tetap hidup; atribut `rel` = `noopener noreferrer`.
- [ ] Picu satu toast (mis. dari Inventory) → muncul di **kanan atas**, berlatar gelap `var(--dash-panel)`, ikon Lucide sesuai tipe (bukan ikon default sonner), elemennya punya kelas `cn-toast` dengan radius/padding/shadow terpasang.
- [ ] Ubah OS ke light mode, picu toast lagi → toast **tetap gelap** (tidak ada `<ThemeProvider>` terpasang; `--normal-bg` mengarah ke `var(--popover)` yang di-override `.dash-root`).
- [ ] Buka modal apa pun (mis. Atur Stok di Inventory) → isinya di dalam `#dash-portal-root`, computed `--popover` = `#0D1829`, bukan oklch putih.
- [ ] Render `<Button>` polos di halaman dashboard → oranye `#FD5002` teks `#FDFDFD`. Button yang sama di halaman customer → hitam/putih default shadcn.
- [ ] Tarik halaman melewati batas atas (trackpad macOS / Safari iOS) → area overscroll **`#060E1B`**, bukan putih.
- [ ] Inspect teks di dalam sheet mobile ter-portal dan di dalam modal → computed `font-family` = **Poppins**, bukan Plus Jakarta Sans atau font sistem.
- [ ] Throttle Slow 3G lalu kunjungi kedelapan rute satu per satu; tiap skeleton berbeda bentuk dan menyerupai halamannya. Hitung kotak KPI: **6** di Analitik, **4** di Revenue dan Inventory. Cek `max-width` container: **1400px** Analitik/Revenue, **1100px** Orders/Menu, **1180px** Inventory, `max-w-5xl` Settings/Announcements, `max-w-3xl` Scheduler.
- [ ] Selama skeleton tampil: kotak benar-benar **berkilau bergerak**, bukan blok abu diam.

---

## §3. `/dashboard` — Analitik

- [ ] Judul tab persis **`Analitik · Dashboard | 3Diner`**.
- [ ] Uji empat kombinasi URL: `/dashboard`, `?start=2026-07-01`, `?end=2026-07-20`, `?start=..&end=..` → eyebrow (nama kafe + label rentang) berbeda pada keempatnya, kata per kata.
- [ ] Dengan data >1000 event: subjudul menulis **`1.234`** (titik ribuan), bukan `1,234`/`1234`; `avgPerDay` membagi dengan jumlah hari **rentang**, bukan selalu 14.
- [ ] Klik "Kelola Pesanan" → navigasi client-side tanpa reload penuh; hover memicu prefetch RSC `/dashboard/orders`.
- [ ] Kecilkan lebar browser: grid KPI berjalan **2 → 3 → 6 kolom**. Cek keenam kartu satu per satu: ikon, warna aksen, teks sub.
- [ ] Muat halaman, scroll ke bawah lalu balik ke atas → angka KPI **menghitung naik** saat pertama terlihat (IntersectionObserver), bukan langsung tampil; scroll bolak-balik tidak mereset ke 0.
- [ ] Kartu Tampilan Menu dan Lihat Model 3D **wajib** punya chip delta. Siapkan data delta negatif → chip **merah + panah turun, tanpa tanda plus**. Kartu tanpa delta (pakai `sub`) → teks abu, bukan chip.
- [ ] Zoom kartu Omzet Hari Ini: **"Rp" jelas lebih kecil dan lebih pudar** dari angkanya; sama untuk "%" di kartu Konversi. Selama count-up lebar teks **tidak melompat** (tabular-nums).
- [ ] Hover kartu KPI di desktop → naik halus 2px, border menyala. Emulasi sentuh → tap **tidak** meninggalkan hover lengket.
- [ ] Buat order uji jam 00:30 WIB dan 23:30 WIB pada hari yang sama → keduanya masuk hitungan **Omzet Hari Ini** yang sama walau server UTC.
- [ ] Ubah satu pesanan `preparing` → `ready` di Orders, kembali ke `/dashboard`, reload → **Pesanan Aktif turun 1**; cek Network: query count `head`-only, tidak mengirim body baris.
- [ ] Semua bahan di atas minimum → kartu Stok Kritis berbunyi **"semua stok aman"**. Turunkan satu bahan di bawah minimum → berubah jadi **"bahan di bawah minimum"** dan angkanya naik.
- [ ] Pilih rentang tanpa event sama sekali → Konversi tampil **`0%`**, bukan `NaN%` atau `Infinity%`.
- [ ] Kafe baru tanpa event → **insight strip hilang total** (bukan strip kosong bergaris). Kafe berdata → cek 4 tile, format jam `19.00` (titik), nama hari **lengkap** ("Senin", bukan "Sen").
- [ ] Perkecil jendela sampai 4 tile tidak muat → strip bisa digeser horizontal **tanpa batang scrollbar terlihat**.
- [ ] Panel Aktivitas Harian: garis **melengkung mulus** (Catmull-Rom, bukan zig-zag), tergambar **dari kiri ke kanan** saat masuk viewport, titik ujung kanan memudar masuk **~1 detik setelahnya**.
- [ ] Sapukan mouse melintasi grafik → crosshair (garis vertikal + lingkaran + angka) muncul untuk **setiap hari**, hilang saat mouse keluar SVG.
- [ ] Rentang 30 hari → label sumbu X **selang-seling**, hanya angka tanggal, tidak tumpang tindih. Label Y kelipatan 4 dan **bilangan bulat** (data max 7 → label teratas 8).
- [ ] Rentang **1 hari** → titik grafik berada di **tengah** plot, bukan menempel kiri.
- [ ] Render dua LineChart di satu halaman dengan warna berbeda → masing-masing punya gradient sendiri (`useId`), bukan sama-sama memakai gradient pertama.
- [ ] Corong: bandingkan angka di tahap "Lihat Model 3D" dengan hitungan manual `view_3d / click_menu * 100` (retensi dari tahap sebelumnya, **bukan** dari puncak). Tiga bar mengisi **berurutan** saat masuk viewport.
- [ ] Kafe dengan 500 buka menu tapi 1 order → bar "Mulai Pesan" tetap tampak sebagai **garis tipis berwarna** (min 2%), bukan track kosong.
- [ ] Warna angka di kalimat penjelas corong **persis sama** dengan warna bar tahapnya (teal untuk 3D, oranye untuk Mulai Pesan).
- [ ] Heatmap 24 jam: jam dengan **1 event** tetap berwarna oranye samar (beda dari kotak nol). Tepat **satu** kotak punya cincin jam-puncak; dengan data kosong tidak ada kotak yang di-outline.
- [ ] Hover setiap kolom heatmap termasuk jam kosong → tooltip format **`03:00 · 0`**. Kotak naik seperti gelombang kiri→kanan saat panel masuk viewport. Ada tepat **8 label jam** (tiap 3 jam) menyebar rata, dan keterangan di bawah heatmap ada.
- [ ] Bar per hari: hover ~1 detik → tooltip native format **`Sen: 42`**; label hari puncak berwarna oranye; tujuh bar tumbuh berurutan Senin→Minggu (stagger 45ms).
- [ ] Donut Komposisi Interaksi: tiga busur tumbuh berurutan **searah jarum jam mulai dari atas**; dengan data kosong hanya cincin abu tanpa error. Angka pusat = jumlah tiga KPI terkait (titik ribuan), label pusat berbunyi **"Interaksi"** (bukan "Total").
- [ ] Menu Terpopuler: peringkat 1 oranye, zebra striping pada baris indeks genap, bar menu teratas hampir penuh; kosongkan data → teks empty state muncul.
- [ ] Picu event baru dari menu publik, reload → baris teratas Aktivitas Terbaru berbunyi **"baru saja"**; tiga tipe event punya tiga warna titik berbeda.
- [ ] Hapus satu menu yang punya riwayat event, reload → baris Aktivitas Terbaru menampilkan **`—`**, bukan `undefined` atau baris kosong.
- [ ] Picu event jam 23:30 WIB → masuk tanggal hari itu di grafik dan kolom jam 23 di heatmap. Rentang dengan hari kosong → grafik punya titik bernilai **0**, tidak melompat.
- [ ] Pilih preset "Hari Ini" → chip delta tetap berlabel **"vs minggu lalu"** (terpisah dari rentang). Kafe baru tanpa data minggu lalu → chip **+100%**, bukan `Infinity%`/`NaN%`.
- [ ] Kafe dengan sedikit data → tile "Konversi terbaik" **tidak** menampilkan menu ber-1-klik dengan 100% (filter minimal 3 klik).
- [ ] Buka `/dashboard?start=2000-01-01&end=2026-07-27` → halaman **tetap merender** (grafik terpotong di 366 hari), bukan hang atau kehabisan memori.
- [ ] Bandingkan keenam panel: semua punya pita header abu tipis, teks kapital kecil, garis pemisah, radius **16px** konsisten. Di viewport lg, donut berada **di tengah vertikal** panelnya (bodyClassName kustom), tidak menempel atas.
- [ ] Uji layout di 375px, 1024px, 1920px → di 1920px konten terkunci **1400px**; rasio **2:1** dua baris pertama dipertahankan.
- [ ] Scroll ke bawah `/dashboard` → ada judul "Inventory" dengan 4 kartu + tabel lengkap + tombol "Buka fokus inventory". Simulasikan gagal query inventory → **panel error merah menyebut nama sumber yang gagal**, menggantikan modul, bukan pesan generik.
- [ ] Jalankan axe / navigasi landmark di NVDA/VoiceOver → section inventory punya **nama terbaca**.
- [ ] Tekan Tab dari atas halaman → setiap tombol dapat **cincin fokus oranye** jelas; klik mouse **tidak** memunculkan cincin (`focus-visible`).

### §3b. DateRangePicker (dipakai di Analitik dan Penjualan)

- [ ] Preset "Hari Ini" → label tombol menampilkan **satu tanggal saja**, bukan `tgl - tgl`. Uji juga URL hanya-`start` dan hanya-`end`.
- [ ] Scroll konten sedikit lalu buka picker → panel muncul tepat di bawah tombol dan **tidak terpotong** panel/kartu/scroll-container mana pun (portal ke body, posisi fixed terhitung).
- [ ] Buka picker → klik di dalam area kalender (**tetap terbuka**), klik di area kosong halaman (**tutup**), buka lagi tekan **Escape** (**tutup tanpa menerapkan** `tempStart`/`tempEnd`).
- [ ] Klik "7 Hari Terakhir" → rentang **7 hari inklusif** (bukan 8) dan chart menampilkan 7 batang. Klik "Semua Waktu" → **kedua** parameter hilang dari URL.
- [ ] Buka bulan yang tanggal 1-nya jatuh hari **Minggu** (mis. Februari 2026) → kolom pertama tetap **Senin** dan tanggal 1 di kolom paling **kanan**. Bandingkan tinggi panel Februari vs Agustus → identik (grid 42 sel / 6 baris). Klik tanggal abu bulan tetangga → tetap terpilih.
- [ ] Klik mundur dari Januari → pindah ke **Desember tahun sebelumnya** dengan benar.
- [ ] Set `?start=2026-03-05`, buka picker → kalender langsung menampilkan **Maret 2026**, bukan bulan berjalan.
- [ ] Klik tanggal 20 lalu tanggal 5 → `start` jadi 5 dan `end` kosong (auto-swap), bukan rentang terbalik atau alert error.
- [ ] Klik satu tanggal, gerakkan mouse 5 hari ke kanan tanpa klik → kelima sel di antaranya menyala oranye tipis, hilang saat mouse keluar. Perbesar tampilan: **tidak ada celah putus** antara sel oranye pekat dan area rentang tipis.
- [ ] Tanggal hari ini punya kotak samar berbingkai; setelah dipilih → **oranye penuh tanpa bingkai tersisa**.
- [ ] Picker fresh tanpa filter → tombol **Terapkan redup dan tidak bisa diklik**. Pilih satu tanggal lalu Terapkan → URL hanya `?start=`.
- [ ] Buka `/dashboard/revenue?foo=bar`, ganti preset → `?foo=bar` **masih ada** di URL hasil.
- [ ] Ganti rentang **tiga kali** lalu Back tiga kali → melewati setiap rentang (`router.push`, bukan `replace`). Salin URL berentang, buka di tab baru → langsung menampilkan rentang yang sama. Setelah Back, buka picker → tanggal tersorot sesuai **URL saat ini**, bukan pilihan lama.
- [ ] Di zona WIB, pilih tanggal hari ini → URL berisi tanggal **sama** dengan yang terlihat di kalender, bukan H-1.
- [ ] Viewport 375px → preset jadi baris horizontal yang bisa digeser **tanpa scrollbar terlihat**, panel tidak melebihi layar (`maxWidth: calc(100vw - 16px)`).
- [ ] Buka dropdown di atas chart → latar sedikit **buram** di belakang panel (backdrop blur) dan panel muncul dengan animasi naik singkat. Tekan-tahan tombol tanggal → mengecil sedikit lalu kembali.

---

## §4. `/dashboard/revenue` — Penjualan

- [ ] Judul tab persis **`Penjualan · Dashboard | 3Diner`**.
- [ ] Buka `?start=2026-07-01&end=2026-07-05` → KPI, chart, subtitle, label tombol tanggal, **dan ekspor** semuanya ikut rentang itu; Back mengembalikan ke rentang sebelumnya.
- [ ] Uji empat varian subtitle (tanpa param / hanya start / hanya end / keduanya), bandingkan kata per kata. Dengan >1000 pesanan → **"1.234 pesanan"**; bulan Agustus tertulis **"Agu"**, bukan "Aug".
- [ ] Urutan slot aksi header kiri→kanan: **Ekspor, lalu DateRangePicker**; di <640px tombol pindah baris baru tanpa terpotong.
- [ ] Reload dan rekam → tiga baris konten muncul berurutan jeda ~30ms (`dash-d1/d2/d3`), bukan serentak.
- [ ] Muat tanpa param → KPI Total Pendapatan punya chip "+x% vs minggu lalu". Pilih preset 7 Hari Terakhir → chip delta **hilang total**.
- [ ] Data `lastWeekRev=0` dan `thisWeekRev>0` → chip tepat **+100%**, bukan `Infinity`/`NaN`.
- [ ] Bandingkan caption KPI Jumlah Pesanan pada URL tanpa param vs `?start=...` → teksnya berganti.
- [ ] Rentang tanpa pesanan → Rata-rata/Pesanan tampil **"Rp 0"**, bukan "Rp NaN".
- [ ] Sisipkan order dengan kolom `items` bernilai null / objek non-array → halaman tetap render, `itemsSold` tidak error.
- [ ] Rekam count-up → angka mulai dari 0 dan **melambat di akhir** (easing), bukan linear/instan. Gulir bolak-balik → angka **tidak reset** ke 0.
- [ ] Uji delta = 0 (**hijau "+0%"**), delta positif, delta negatif (**"-12%" merah tanpa plus ganda**).
- [ ] Resize 1200px → 500px → grid KPI jadi **2×2**, bukan satu kolom bertumpuk. Pada ≥1024px rasio panel **2:1** di kedua baris; di bawah itu urutan vertikal: chart → status&pembayaran → top menu → pesanan terbaru.
- [ ] Gulir sampai panel chart masuk viewport → batang tumbuh dari nol, **stagger 35ms** (14 batang ≈ 455ms), hari beromzet 0 tetap menyisakan **garis tipis 2%**. Batang paling kanan ("hari ini") jelas lebih pekat.
- [ ] Hover di area kosong **di atas** batang pendek → tooltip tetap muncul (hit-area = seluruh kolom setinggi 168px). Geser mouse cepat melintasi tooltip → tooltip **tidak berkedip** (`pointer-events: none`).
- [ ] 14 hari → **7 label X** berjarak sama, sejajar batang indeks genap, hanya token pertama tanggal.
- [ ] Footer "Maks: Rp": uji 950 → **950**, 12.500 → **13rb**, 1.500.000 → **1.5jt**, 12.000.000 → **12jt**; rentang kosong → **"Maks: Rp 1"**, bukan NaN/Infinity.
- [ ] Preset "Hari Ini" (1 batang) → batang tetap ramping **≤18px** di tengah kolomnya, tinggi chart tetap **168px**.
- [ ] Buka di HP, tap batang → **tidak ada tooltip** (baseline saat ini; kalau rebuild menambahkannya, catat sebagai perubahan perilaku yang disepakati).
- [ ] Donut status: angka tengah = jumlah pesanan tiga kategori itu; sisipkan order berstatus tak dikenal → **tidak menambah total dan tidak error**.
- [ ] Buat order **QRIS yang belum lunas** → harus masuk kategori **"Belum Bayar"**, BUKAN QRIS (urutan if/else penentu makna laporan).
- [ ] Ada garis pemisah horizontal antara dua donut, jarak 18px di bawah garis. Rekam animasi arc: segmen terisi berurutan searah jarum jam dari puncak, **ujung membulat**, stagger 120ms.
- [ ] Rentang tanpa pesanan → donut tetap tampak sebagai **cincin abu dengan angka 0** di tengah, legenda semua **0%** (bukan NaN%).
- [ ] Label pusat donut atas = **"PESANAN"**, donut bawah = **"TRANSAKSI"** (uppercase); total >999 pakai titik ribuan. Perkecil lebar panel → label legenda **ellipsis**, bukan wrap.
- [ ] Panel Menu Penyumbang Pendapatan: template kolom **20px / 1fr / auto** (nomor, nama, angka sejajar dengan baris data); zebra pada indeks 0/2/4; hanya angka "1" oranye; menu teratas bar **100%**; data semua-revenue-0 tidak menimbulkan bar NaN.
- [ ] Menu bernama 80 karakter → truncate satu baris, kolom "Qty · Pendapatan" **tetap di posisi sama**. Simbol **× (U+00D7)** dipakai; rupiah `Intl` id-ID tanpa desimal (`Rp 40.000`).
- [ ] Maksimal **6 baris**, urut menurut **pendapatan** (bukan qty); item tanpa `nama_menu` tampil **`—`**.
- [ ] Rentang tanggal di masa depan → panel menu menampilkan kalimat empty state persis, **tanpa baris header kolom** di atasnya.
- [ ] Sisipkan order berstatus `cancelled` → badge tampil **outline abu bertuliskan "cancelled"**, bukan crash/badge kosong. Aktifkan simulasi buta warna → status tetap terbaca dari teks (dot + label).
- [ ] Baris pesanan terbaru: uji umur 30 detik (**"baru saja"**), 45 menit (**"45 mnt lalu"** — singkatan `mnt`, bukan "menit"/"m"), 5 jam, 3 hari.
- [ ] Rentang kosong → bandingkan kedua panel: **"Belum ada pesanan."** vs **"Belum ada penjualan."** harus tetap **berbeda**.
- [ ] Rentang 30 hari di kafe sepi → chart punya **30 batang** (banyak bernilai 0). Buka `?start=2026-07-20&end=2026-07-01` (terbalik) → halaman tidak hang/kosong.
- [ ] Sisipkan order `2026-07-21T17:30:00Z` (= 00:30 WIB 22 Jul) → omzetnya muncul di batang **tanggal 22**, bukan 21. Label chart format **"21 Jul"** dua token.
- [ ] Buka `/dashboard/revenue` polos → hitung batang chart = **tepat 14** (default 14 hari terakhir).

### §4b. Ekspor (CSV & Cetak/PDF)

- [ ] Klik Ekspor → chevron **berputar mulus** ke atas; klik lagi → menu tertutup dan chevron kembali. Menu turun sedikit sambil memudar masuk ~200ms.
- [ ] Muat halaman dan klik Ekspor **secepat mungkin** → tidak ada error hydration di console.
- [ ] Buka menu Ekspor, gulir halaman perlahan → menu **tetap menempel** di bawah tombol (listener `capture: true`, jadi scroll di container dashboard ikut terdeteksi). Resize juga.
- [ ] Klik pada area teks "Buka di Excel / Sheets" → menu **tidak** tertutup sebelum aksi selesai.
- [ ] Bandingkan dua baris aksi (CSV vs Cetak/PDF): judul, deskripsi, dan warna ikon masing-masing sesuai baseline.
- [ ] Throttle jaringan, klik Unduh CSV → spinner **hanya** di baris itu; baris PDF **tidak** ikut disabled.
- [ ] Pilih rentang tanpa transaksi lalu klik Unduh CSV → muncul **"Tidak ada transaksi pada periode ini."**, menu **tidak tertutup**, **tidak ada** file `.csv` terunduh dan **tidak ada** dialog print muncul.
- [ ] Paksa server action melempar error → spinner berhenti (tombol bisa diklik lagi) **dan menu tetap terbuka**. Menu hanya tertutup saat berhasil.
- [ ] Panggil `getSalesExport` tanpa sesi → mengembalikan **error, bukan data**. Untuk kafe >2000 pesanan → hasil terpotong **2000 terbaru** (perilaku saat ini, tanpa peringatan ke user).
- [ ] Buka `?end=abc` lalu ekspor → tetap menghasilkan laporan (filter diabaikan), bukan crash. Uji `end=hari-ini` → transaksi jam 22:00 hari itu **ikut** terekspor.
- [ ] Ekspor order dengan `items` null dan order dengan `total` null → kedua baris tetap muncul dengan Item kosong dan Total 0, **tanpa "undefined"** di file.
- [ ] Buat pesanan dengan nomor meja **`=1+1`** lewat `POST /api/orders`, ekspor CSV, buka di Excel → sel berisi teks `'=1+1`, **bukan angka 2** (guard CSV injection).
- [ ] Buka CSV di Excel: nama kafe beraksen tampil benar (BOM bekerja), ada blok metadata dan **9 kolom persis**.
- [ ] Ekspor dari kafe "Senja Kopi" → nama file **`Laporan-Senja-Kopi-2026-07-27.csv`**; ekspor berulang tidak membocorkan memori (`URL.revokeObjectURL` dipanggil).
- [ ] Klik Cetak/PDF → dialog print menampilkan **pratinjau laporan** (bukan tangkapan halaman dashboard); batalkan, periksa DOM setelah ~2 detik → **tidak ada `<iframe>` tersisa**.
- [ ] Buat pesanan dengan nomor meja `<img src=x onerror=alert(1)>` dan cetak → tampil sebagai **teks mentah**, tidak tereksekusi.
- [ ] Di dialog print pilih **A4** → margin 14mm/12mm, kolom Total tidak terpotong ke baris baru, ringkasan item panjang membungkus dalam 240px. Bandingkan label header, tiga kartu ringkasan, tabel **8 kolom**, dan rasio "Sudah Lunas" dengan jumlah baris berstatus Lunas.
- [ ] Bandingkan jumlah kolom CSV vs PDF → **9 vs 8**. Perbedaan ini disengaja; pertahankan atau sepakati perubahannya secara eksplisit.
- [ ] Ekspor dengan hanya `?start=` → baris **"Periode: Sejak 1 Jul 2026"** (S kapital); format tanggal-waktu gaya id-ID 2 digit.
- [ ] Periksa berkas ekspor → **tidak ada kata Inggris** ("cash", "paid", "ready") yang bocor untuk nilai yang dikenal.
- [ ] Di halaman Pesanan tandai satu order tunai lunas, pindah ke Penjualan → segmen **"Belum Bayar" berkurang tanpa hard refresh**.
- [ ] Tekan Tab berurutan: Ekspor → item menu → tombol tanggal → preset → sel kalender → Batal → Terapkan → semuanya punya cincin oranye jelas. Catat baseline: kalender **tidak** punya navigasi panah keyboard dan dropdown **tidak** memindahkan/mengembalikan fokus.

---

## §5. `/dashboard/orders` — Pesanan

- [ ] Cek Network/SQL log: **satu** query Orders dengan filter `cafe_id` dan `limit 60`, urut menurun. Buat 61 pesanan → hanya 60 yang muncul, teratas = `created_at` terbaru.
- [ ] Throttle Slow 3G, klik menu Pesanan → skeleton **4 kartu ber-shimmer** dengan bentuk sama seperti kartu asli (tidak melompat saat data datang).
- [ ] Klik tab "Diproses" → hanya kartu `preparing` yang tampil. Ubah status pesanan lain lewat tab "Semua", kembali ke "Diproses" → filter **tidak kembali sendiri** ke "Semua".
- [ ] Dengan tab "Baru" aktif, tandai satu pesanan jadi Diproses → chip **"Baru" turun 1** dan **"Diproses" naik 1** seketika tanpa reload (penghitung dari seluruh data, bukan hasil filter).
- [ ] Sempitkan viewport ke 320px → chip tetap ukuran penuh, bisa digeser, **tanpa scrollbar terlihat**.
- [ ] Klik toggle alarm → ikon jadi lonceng berdering, warna teal, label **"Alarm Aktif"**, atribut `title` berubah sesuai state. Tekan F5 → tombol kembali menyala; cek Local Storage kunci **`3diner.orderAlerts` = `on`**.
- [ ] Nyalakan alarm → terdengar **chime dua nada naik (G5→C6)**. Buat pesanan dari menu pelanggan → bunyi yang sama terdengar. Picu `playChime` 10× → jumlah `AudioContext` di memori tetap **1**.
- [ ] Muat halaman baru (belum ada interaksi), nyalakan alarm, picu pesanan dari perangkat lain → **bunyi keluar** (priming audio di dalam gesture).
- [ ] Reset izin notifikasi situs, klik toggle → prompt muncul. **Tolak** izin, klik toggle lagi → **tidak ada prompt kedua** dan tombol tetap berfungsi.
- [ ] Beri izin, pindah ke aplikasi lain, buat pesanan **2 item × 3 qty** → notifikasi OS menampilkan **total qty 6** (bukan 2) dan rupiah format Indonesia; tag anti-tumpuk bekerja.
- [ ] Kirim event INSERT realtime **dua kali** dengan payload identik → **satu** kartu, **satu** toast (id = ID pesanan), chime berbunyi **sekali**.
- [ ] Matikan Wi-Fi → **satu** toast peringatan koneksi putus; nyalakan lagi → toast **berubah jadi sukses di tempat yang sama**, tidak menumpuk (id tetap).
- [ ] Buka dua browser dengan akun kafe berbeda; buat pesanan di kafe A → **hanya** dashboard kafe A yang bergerak. Nama channel di Supabase Realtime inspector = `orders-<uuid>`.
- [ ] Bayar pesanan QRIS lewat Midtrans dari perangkat lain → kartu berubah **Lunas tanpa refresh**, dan daftar item di kartu **tidak menjadi kosong** (merge parsial pada UPDATE).
- [ ] Hapus satu baris Orders lewat Supabase Studio → kartu **lenyap tanpa reload**.
- [ ] Buka Network → WS. Nyalakan/matikan alarm beberapa kali → **jumlah koneksi WebSocket tidak bertambah**, tapi pesanan baru setelahnya tetap membunyikan alarm (ref mirror).
- [ ] Buka Pesanan, pindah cepat ke halaman lain sambil jaringan mati → **tidak muncul** toast "Koneksi realtime terputus" di halaman lain; tidak ada warning React setState-after-unmount.
- [ ] Resize 375px → 1280px: kolom berubah tepat di **768px**, gap antar kartu **16px** konsisten. Hover kartu di desktop → naik 2px; simulasi sentuh → tidak nyangkut.
- [ ] Bandingkan judul kartu dengan kolom `id_order` di database → **identik penuh** (bukan potongan), plus nomor meja ("Meja 7").
- [ ] Klik ikon salin pada kartu → berubah **centang hijau ~1,5 detik** lalu kembali; tempel di editor → ID persis sama. Klik pada kartu kedua → centang **berpindah**, tidak muncul di dua kartu sekaligus.
- [ ] Pesanan baru → "baru saja". Ubah `created_at` ke 90 menit lalu → **"2 jam lalu"** (pembulatan, bukan pemotongan).
- [ ] Pesanan bervarian → baris kecil di bawah nama menu berbunyi **"Large · Less Sugar"**; subtotal = harga satuan × qty format Rp.
- [ ] Set kolom `items` satu baris Orders ke **null** → kartu tetap tampil (daftar kosong) dan **struk tetap bisa dibuka**, tanpa layar putih.
- [ ] Siapkan 3 pesanan (cash unpaid, qris unpaid, sudah paid) → label dan warna PaymentRow berbeda sesuai baseline; teks **"Lunas"/"Belum" wajib ada** (bukan warna saja).
- [ ] Pesanan **QRIS unpaid** → tidak ada tombol pelunasan, hanya teks **"Menunggu Midtrans"**. Pesanan **tunai** → tombol "Tandai Lunas" muncul dan berfungsi.
- [ ] Matikan jaringan lalu klik Tandai Lunas → pil tetap **"Belum"** + toast error; **tidak boleh** sempat terlihat "Lunas" lalu berbalik (bukan optimistis).
- [ ] Lunasi satu pesanan tunai lalu buka `/dashboard/revenue` → angka pendapatan **sudah ikut terbarui** (revalidate ganda).
- [ ] Kirim pesanan dengan catatan dua baris (`Tanpa es\nPedas sedikit`) → dua baris **tetap terpisah** di kartu.
- [ ] Klik "Mulai Proses" → warna tombol oranye→kuning dan teks jadi **"Tandai Siap"**; klik lagi → jadi teks **"Selesai" tanpa tombol**.
- [ ] Throttle Slow 3G lalu klik Mulai Proses → badge status berubah **instan** sementara spinner berputar; angka tab ikut berubah seketika (optimistic).
- [ ] Panggil `updateOrderStatus` dengan `id_order` milik kafe lain → **tidak ada** perubahan di database. Setelah update sukses, reload → status tetap sesuai (revalidate).
- [ ] Klik ikon printer (accessible name **"Preview & Cetak Struk"**) → modal muncul, dan dialog berada di dalam **`#dash-portal-root`**, bukan langsung di body.
- [ ] Buka struk untuk pesanan 1 item lalu 15 item → keduanya tampil penuh, **tidak ada scrollbar di dalam kertas**, yang menggulir adalah modal luar. Terlihat **perforasi garis putus-putus** di tepi atas dan bawah, latar kertas **krem**.
- [ ] Klik "Cetak Sekarang" → dialog print muncul **dan modal ikut tertutup dalam satu aksi**; halaman di belakang tidak berkedip/bergeser. Cetak 5×, periksa DOM setelah ~2 detik → **tidak ada iframe menumpuk**.
- [ ] Cetak ke PDF → lebar hasil **80mm**, semua baris muat 32 karakter tanpa terpotong, "MEJA 12" terbaca.
- [ ] Buat pesanan dengan catatan `<script>` / `<img src=x onerror="alert(1)">` lalu cetak → tampil sebagai teks, **tidak pernah lolos mentah**; sementara "Tanpa gula & es sedikit" tetap terbaca normal.
- [ ] Cetak struk untuk `created_at 2026-07-21T10:00:00Z` → tanggal **dd/mm/yyyy** dan jam mengikuti waktu lokal perangkat (**17:00 WIB**), bukan UTC.
- [ ] Buka modal dengan keyboard (Tab ke ikon printer, Enter), tutup dengan Esc, tekan Tab → fokus **lanjut dari kartu itu**, bukan lompat ke awal halaman.
- [ ] Pilih tab yang kosong → empty state lengkap dengan CTA **"Bagikan QR Menu"**; klik CTA → mendarat di Pengaturan **dan menggulir ke bagian QR menu**.
- [ ] Total 1250000 → **"Rp 1.250.000"** (titik, tanpa ",00"). Angka tab berubah 9 → 10 → lebar chip **tidak bergeser**.
- [ ] Tekan-tahan chip filter, tombol cetak, tombol Mulai Proses → ketiganya mengecil dengan tingkat berbeda (**0.95 / 0.96 / 0.97**). Emulasi mobile → tidak ada hover tertinggal.
- [ ] Picu ketiga jenis toast (pesanan baru, koneksi putus, gagal lunas) → semuanya di **kanan atas**, gelap, ikon sesuai jenis.

---

## §6. `/dashboard/menu` — Daftar menu

- [ ] Tambah menu baru (`sort_order` NULL), reload → menu baru muncul **paling bawah**, bukan di atas menu yang sudah punya `sort_order`.
- [ ] Nyalakan log query / ukur TTFB → query menu dan resep **overlap** (Promise.all), bukan berurutan; user tanpa kafe **tidak memicu query sama sekali**.
- [ ] Buat menu dengan 2 bahan, turunkan stok salah satunya di bawah `qty_per_menu` → badge jadi **"Stok kurang"**, bukan "Resep aktif" (aturan "low menang").
- [ ] Putuskan relasi inventory dari sebuah resep → halaman **tidak crash**, menu jatuh ke state `low`.
- [ ] Paksa error pada query menu dan pada query resep secara terpisah → **dua pesan berbeda dalam Bahasa Indonesia**, bukan satu "Something went wrong".
- [ ] Ketik pencarian → angka **"{n} item terdaftar"** di header **tidak berubah**; yang berubah hanya "{n} hasil" di toolbar tabel.
- [ ] Kafe tanpa menu → panel empty state dengan CTA sendiri (bukan tabel kosong). Kafe bermenu + pencarian tidak cocok → **"Tidak ada menu yang cocok…"**, BUKAN panel empty state.
- [ ] Lebar 375px → "Tambah Menu" di atas, kedua tombol full-width; 1024px → sejajar horizontal, lebar mengikuti isi.
- [ ] Ketik **nama kategori** (bukan nama menu) → baris tetap tersaring. Ketik dengan spasi di depan/belakang → hasil sama. Klik X → query kosong dan tombol X hilang.
- [ ] Klik header "Harga" tiga kali → **asc → desc → kembali ke urutan manual**, dan hint berubah jadi "Seret baris untuk atur urutan".
- [ ] Sort kolom "Resep" asc → **"Stok kurang" di paling atas** (bukan "Tanpa resep"). Sort "Nama" asc dengan menu `ayam` dan `Ayam` → dianggap setara (locale Indonesia).
- [ ] Sort kolom "Harga" → panah di **sisi kiri** label dan seluruh sel kolom **rata kanan**; kolom lain panah di kanan label.
- [ ] Seret baris ke-3 ke posisi pertama, reload → urutan baru **bertahan**. Seret dan lepas di tempat semula → **tidak ada** indikator "Menyimpan".
- [ ] Ketik pencarian lalu coba seret baris → **tidak bisa**; hover handle → tooltip menjelaskan **dua** syaratnya. Kosongkan pencarian dan reset sort → handle aktif lagi.
- [ ] Seret baris → indikator "Menyimpan" lalu "Tersimpan"; **"Tersimpan" hilang ~2 detik** kemudian dan tidak menempel selamanya.
- [ ] Lakukan tiga aksi bergantian (drag / sort / search) → ketiga kalimat bantuan toolbar muncul, termasuk instruksi "klik judul lagi untuk reset".
- [ ] Seret sebuah baris, lalu toggle status aktif menu lain (memicu revalidate + refresh) → urutan hasil drag **tidak melompat balik**.
- [ ] Muat di 375px dan Tab dari awal → fokus **tidak masuk** ke elemen tabel desktop. Resize ke 1200px **tanpa reload** → tabel muncul menggantikan kartu, dan hanya **satu** representasi ter-mount (bukan dua dengan salah satunya hidden).
- [ ] Buka di 1024px → panel boleh scroll horizontal di dalam dirinya (min-width 720px), tapi `<body>` **tidak** ikut scroll horizontal.
- [ ] Menu tanpa foto → kotak berisi **ikon Box**, bukan area kosong / broken image. Menu bernama sangat panjang → truncate + tooltip nama penuh saat hover.
- [ ] Menu tanpa kategori: desktop menampilkan **`-`**, mobile menampilkan **"Tanpa kategori"**. (Kalau rebuild menyeragamkan, itu perubahan perilaku yang harus disetujui.)
- [ ] Bandingkan harga 9.000 dan 450.000 → digit **sejajar vertikal** (tabular-nums), pemisah **titik**. Menu tanpa GLB → **em-dash polos**, bukan badge "Tanpa 3D".
- [ ] Aktifkan simulasi grayscale → ketiga status resep tetap terbaca dari teks; label persis **"Resep aktif" / "Stok kurang" / "Tanpa resep"**.
- [ ] Matikan toggle sebuah menu → seluruh barisnya **meredup ~55% seketika** tanpa reload. Tap baris di emulasi ponsel → latar **tidak** berubah permanen.
- [ ] Klik toggle dengan throttling → label jadi **"..."**, spinner di dalam bulatan thumb, tombol tidak bisa diklik lagi; setelah selesai label "Aktif"/"Habis" dan **minWidth menjaga posisi** elemen di kanannya.
- [ ] Hover tombol Edit di desktop → latar menerang; klik → navigasi **terasa instan** (Link + prefetch, bukan `router.push` manual).
- [ ] Ketik `zzzz` di pencarian → kalimat "tidak ada hasil" muncul (versi tabel dan versi kartu terpisah), header tetap menampilkan jumlah total item terdaftar.

---

## §7. Form menu — `/dashboard/menu/new` & `/dashboard/menu/[id]/edit`

- [ ] Paksa error pada query `Inventory_Items` di halaman Tambah Menu → **form TIDAK muncul** (bukan form dengan dropdown bahan kosong), dan kartu error tampil.
- [ ] Paksa error pada masing-masing query di Edit Menu (inventory / resep / grup varian) → judul kartu error **berbeda** sesuai penyebabnya. Urutan baris resep mengikuti `created_at`, bukan acak.
- [ ] Matikan semua pilihan dalam satu grup varian, reload halaman edit → **grup tetap ada**; buka halaman menu pelanggan → **grup hilang** (owner memuat pilihan nonaktif, pelanggan tidak).
- [ ] Buka form di 375px → kartu pratinjau ponsel berada **di ATAS** field "Nama Menu". Di desktop → di kanan dan **tetap menempel (sticky)** saat form di-scroll.
- [ ] Isi deskripsi panjang + banyak bahan sampai konten >560px → bisa di-scroll di dalam bingkai **tanpa scrollbar terlihat**; dynamic island tetap tergambar.
- [ ] Isi harga 50000 + diskon 20 → pratinjau menampilkan **Rp40.000 besar oranye + Rp50.000 dicoret** dan badge **-20%**. Isi 15 bahan dipisah koma → hanya **10 chip** tampil. Kosongkan nama → judul jadi **"Nama Hidangan"**.
- [ ] Tab dari field terakhir form → fokus **melompati kedua tombol CTA** di dalam bingkai ponsel.
- [ ] Submit form kosong → browser memblokir di field **"Nama Menu"**. Set harga ke 0 → kotak input tampak **kosong**, bukan berisi angka 0.
- [ ] Geser slider skala 3D ke ujung kiri/kanan → pembacaan **0.2x** dan **3.0x**, lebar teks **tidak menggeser** slider. Simpan lalu buka lagi → nilai kembali seperti disimpan.
- [ ] Matikan sakelar "Tampilkan di menu" lalu simpan; buka daftar menu → baris meredup dan toggle di tabel jadi **"Habis"**. Seluruh baris sakelar (termasuk teks label) dapat diklik.
- [ ] Pilih chip hari Sen dan Rab, simpan, buka lagi → **tepat dua chip** aktif (pemetaan 1..7). Kosongkan semua lalu simpan → `schedule_days` null/kosong dan hint menjelaskan artinya.
- [ ] Isi jam 08:00–15:00, simpan, buka lagi → nilai kembali. Pastikan input jam tetap memakai **`defaultValue`**, bukan `value` tanpa `onChange`.
- [ ] Buka tab Network saat mengunggah gambar 10MB → request unggah menuju **domain Supabase**, BUKAN origin aplikasi; path objek diawali **id kafe**.
- [ ] Seret file dari desktop ke zona unggah → border berubah oranye **sebelum** dilepas. Klik zona saat sedang mengunggah → dialog **tidak** terbuka.
- [ ] Unggah gambar → label **"Terunggah" hijau** lalu berubah **"Tersimpan"** setelah ~2 detik; nama file **tanpa angka timestamp** di depan. Klik X → pratinjau ponsel ikut kehilangan gambarnya.
- [ ] Unggah file A, klik X, lalu pilih file A **lagi** → unggahan berjalan (bukti `e.target.value` di-reset).
- [ ] Buka dialog file pada slot **"Model iOS"** → hanya `.usdz` yang dapat dipilih. Bandingkan ketiga hint slot media → teksnya berbeda dan menyebut platform yang tepat.
- [ ] Tambah semua bahan yang ada di RecipeEditor → tombol **"Tambah Bahan" disabled**. Pilih bahan yang sudah dipakai di baris lain → opsinya **disabled**. Kosongkan jumlah jadi 0 → banner merah muncul dan submit ditolak.
- [ ] Set stok sebuah bahan ke 0 lalu pilih bahan itu di resep → label **"Habis" merah** DAN tautan **"Perbarui stok"**. Set stok tepat = minimum → **"Menipis"**, bukan "Aman".
- [ ] Tambah grup varian sampai **10** → tombol disabled, opacity 50, cursor `not-allowed`. Grup baru langsung punya **satu baris pilihan kosong**, bukan grup kosong.
- [ ] Set grup ke "Pilih beberapa" dengan 3 pilihan, simpan, buka lagi → dropdown tetap **"Pilih beberapa"**. Set balik ke "Pilih satu" → `max_select` kembali **1**.
- [ ] Centang "Boleh dilewati tamu", simpan, buka halaman menu pelanggan → grup itu **bisa dilewati** tanpa memilih apa pun (`min_select` 0).
- [ ] Grup dengan **satu** pilihan → tombol hapus **tidak ada**, tapi kolom grid tetap sejajar. Isi selisih harga `2500.7` → tersimpan **2500**. Kurangi jumlah pilihan pada grup "Pilih beberapa" → `max_select` ikut turun.
- [ ] Isi selisih harga `-3000` → kalimat berbunyi **"Harga menu turun Rp3.000"** tanpa minus ganda.
- [ ] Buka menu yang variannya sudah punya bahan → accordion resep-per-varian **sudah terbuka**. Buat varian baru → accordion **tertutup**. Chevron **berputar** saat diklik, bukan berganti ikon.
- [ ] Buat dua pilihan bernama `Large` dan `large` dalam satu grup → **"Pilihan "large" muncul dua kali dalam satu grup."** Kosongkan nama grup → **"Setiap grup varian harus punya nama."** (validasi identik di klien dan server).
- [ ] Buat menu baru dengan resep yang sengaja invalid di server, klik Simpan (gagal), perbaiki, Simpan lagi → daftar menu berisi **satu** menu, bukan dua (pemulihan anti-duplikat, urutan menu → resep → varian).
- [ ] Klik Simpan dengan throttling lalu coba mengetik di input jumlah bahan / nama varian → **tidak bisa diketik** sampai proses selesai; label tombol kontekstual.
- [ ] Buka edit menu, klik **Hapus** → teks konfirmasi memuat **nama menu yang benar**. Klik Batal → tidak terhapus. Konfirmasi → kembali ke daftar dan menu hilang.
- [ ] Simpan menu tanpa deskripsi/kategori → kolom di DB **NULL**, sementara `model_3d_url` dan `redirect_link` **string kosong**. Hapus field `is_active` dari FormData lewat devtools lalu simpan → menu tersimpan **AKTIF**.
- [ ] Seret ulang urutan menu, lalu buka `/{slug}` di tab lain → urutan baru **sudah terlihat**. Ubah jadwal menu, buka `/dashboard/scheduler` → perubahannya sudah ada (peta `revalidatePath`).
- [ ] Panggil `setMenuAvailability` dengan `id_menu` milik kafe lain dari console → **tidak ada** perubahan data. Hapus cookie sesi lalu klik toggle → **kegagalan bersih**, bukan exception.
- [ ] Kirim resep dengan `inventory_item_id` milik kafe lain → pesan **"Bahan tidak ditemukan."**, bukan pesan Postgres mentah berbahasa Inggris.
- [ ] Tab melewati seluruh MenuForm → setiap input menampilkan **cincin fokus oranye** jelas. Bandingkan warna latar input di panel "Basics" vs "Varian & Add-on" → sesuai baseline.
- [ ] Jalankan NVDA/VoiceOver pada panel varian → setiap input dibacakan dengan **konteks grup dan nomor pilihan**, bukan hanya "edit text".

---

## §8. Fitur AI — Ekstraktor, Auto-Isi, Tripo 3D, Meteran Jatah

- [ ] Viewport 375px: tombol "Ekstrak Menu via AI" **selebar penuh**, posisi mengikuti `flex-col-reverse`; 1280px: dua tombol sejajar horizontal.
- [ ] Buka modal ekstraktor, inspect DOM → `.me-overlay` adalah **anak langsung `<body>`**, bukan di dalam container halaman; reload dengan JS lambat → tidak ada warning hydration.
- [ ] Set `body.style.overflow = 'scroll'` via console **sebelum** buka modal, buka modal (scroll roda mouse di luar → halaman diam), tutup → nilai kembali **`'scroll'`**, bukan `''`.
- [ ] Rekam via panel Animations → backdrop **180ms**, dialog **260ms** dengan easing overshoot lembut, bukan linear.
- [ ] Seret PNG dari Explorer ke dropzone → border berubah **sebelum** dilepas; lepaskan → langsung masuk stage processing.
- [ ] Drop `.txt` → **"Format tidak didukung. Gunakan JPG, PNG, WEBP, atau PDF."** Drop PDF 20MB → **"Ukuran file maksimal 15MB."**
- [ ] Pilih `menu.png`, tunggu error, klik dropzone dan pilih `menu.png` **lagi** → stage processing muncul lagi (input file di-reset).
- [ ] Throttle Slow 3G, unggah foto → persentase bergerak **melambat dan berhenti di 90**, lalu meloncat ke 100 sebelum daftar draft muncul. Garis scanner oranye menyapu **turun berulang** tanpa terlihat naik; nama file panjang **ellipsis**, tidak melebarkan kartu.
- [ ] Matikan jaringan lalu unggah → banner error muncul dan dropzone kembali (stage idle).
- [ ] Ekstrak ≥3 item; ubah harga satu item ke huruf → nilai jadi **0**. Hapus semua item → tombol **"Simpan & Publikasikan (0 Menu)" disabled** dan menekan Simpan menghasilkan **"Tidak ada item valid untuk disimpan."** tanpa memanggil server.
- [ ] Isi deskripsi berbeda di baris 1,2,3; hapus baris 2 → deskripsi baris 3 **tetap di baris 3** (uid stabil, bukan index sebagai key).
- [ ] Di stage review klik **"Ganti file"** → dropzone muncul tanpa dialog konfirmasi, dan progress bar **tidak menyisakan nilai lama** saat unggah berikutnya.
- [ ] Simpan 3 draft → tombol disabled + spinner selama request, lalu berpindah ke **stage done** dengan jumlah tersimpan yang benar. Salah satu draft bernama kosong → yang tampil **2, bukan 3**.
- [ ] Simpan saat sesi kedaluwarsa → banner **"Sesi tidak valid. Masuk ulang."** dan **tetap di stage review**.
- [ ] Kirim 101 draft → **"Maksimal 100 menu sekali simpan."** Ketik nama 200 karakter → tersimpan **terpotong 120**.
- [ ] Simpan 2 menu, tutup modal, navigasi ke halaman lain dan kembali ke `/dashboard/menu` → 2 menu baru **dan angka jatah AI sudah terbarui** tanpa hard reload.
- [ ] `curl -X POST /api/menu/extract` tanpa cookie → **401** dan **tidak ada** panggilan ke Gemini.
- [ ] Set `GEMINI_MODEL` ke nama tidak valid → **502** memuat pesan asli Gemini; kembalikan → ekstraksi jalan. Unggah foto menu berharga "Rp 45.000" → draft menampilkan **45000**.
- [ ] Catat sisa jatah, unggah foto buram/kosong sehingga **0 item terdeteksi** → pesan "Tidak ada item menu terdeteksi. Coba foto yang lebih jelas." dan setelah reload **sisa jatah SAMA** seperti sebelum unggah (refund bekerja).
- [ ] Kosongkan nama menu → tombol **Auto-Isi disabled** dengan tooltip "Isi nama menu dulu". Isi "Nasi Goreng" → klik → **keempat field terisi**. Matikan `GEMINI_API_KEY`/jaringan → teks merah + ikon AlertCircle **menggantikan** kalimat hint "Ketik nama menu, lalu biarkan AI mengisi detail di bawah otomatis."
- [ ] Isi manual deskripsi lalu klik Auto-Isi untuk menu yang responsnya tidak berisi `calories` → **kilatan oranye hanya pada field yang benar-benar berubah**, padam ~1,1 detik.
- [ ] `POST /api/menu/generate-details` dengan `{ name: "x".repeat(121) }` → **400 "Nama menu terlalu panjang."**; tanpa cookie → **401** tanpa memanggil Gemini.
- [ ] Buka `/dashboard/menu/new` → kartu teal Tripo berada persis **di antara "Foto Menu" dan "Model 3D (.glb)"**.
- [ ] Buka modal upload Tripo: klik area gelap → tutup; klik di dalam kartu → tidak tutup. Pilih PNG ber-alpha → **pola kotak-kotak terlihat menembus**; pilih PNG opaque → pola tertutup penuh. File picker → **JPG tidak muncul** di daftar default. Pilih PNG 250KB → tampil **"nama.png · 244 KB"**.
- [ ] Pilih 10 file berbeda berturut-turut → jumlah blob aktif (chrome://blob-internals) **tidak menumpuk** (revoke ganda).
- [ ] Throttle jaringan, klik Generate 3D, lalu klik X berkali-kali → modal **tetap terbuka** sampai upload selesai, lalu **menutup sendiri**; progress bar teal muncul di kartu dan form lain **tetap bisa diketik** selama generate berjalan.
- [ ] DevTools Network saat Generate → request ke `*.supabase.co/storage/...` **langsung**, BUKAN POST besar ke domain aplikasi; path objek diawali id cafe.
- [ ] Filter Network `status` → request polling muncul **~setiap 3,5 detik**, tidak pernah dua bersamaan. Navigasi keluar di tengah proses → **polling berhenti**.
- [ ] Progress bar Tripo: langsung terisi **≥4%** meski Tripo melapor 0, **tidak pernah 100%** sebelum fase done, bergerak halus. Label melewati **kelima teks proses berurutan** lalu berhenti di **"Model 3D siap"** dengan ikon centang.
- [ ] Tunggu ~40–60 detik → kotak ikon berubah jadi **render model** (`rendered_image`); Network menunjukkan request gambar **langsung ke domain Tripo**, bukan lewat `/_next/image`.
- [ ] Setelah selesai, buka URL GLB di field "Model 3D (.glb)" → domainnya **supabase.co**, bukan tripo3d.ai; ukuran GLB **jauh di bawah 14MB**. Nama file GLB mengandung nama menu.
- [ ] Selama fase "Konversi USDZ": field GLB **sudah terisi**; setelah selesai field "Model iOS (.usdz)" ikut terisi (<2 menit), objek Supabase di folder `/usdz/` dengan contentType `model/vnd.usdz+zip`. Simpan form di tengah konversi → menu tetap punya GLB.
- [ ] Blokir `/api/tripo/convert` lewat request blocking, jalankan generate → label akhir tetap **"Model 3D siap"**, GLB terisi, dan muncul teks merah **"Model 3D siap, tapi USDZ (AR iPhone) gagal: …"** (degradasi anggun).
- [ ] Generate 3D lalu klik X pada kartu GLB → field kosong dan nilai hasil AI **tidak muncul kembali** pada render berikutnya (sinkronisasi `injectedUrl` saat render, tanpa `useEffect`).
- [ ] Tambahkan BOM di depan `TRIPO_API_KEY` → generate **tetap berhasil**. Set key kosong → **502 dengan pesan konfigurasi**.
- [ ] `curl` keempat endpoint `/api/tripo/*` tanpa cookie → semuanya **401**. `POST /api/tripo/generate` dengan `{ image_url: "file:///etc/passwd" }` → **400 dan tidak ada credit terpakai**.
- [ ] Catat sisa jatah, jalankan satu generate penuh sampai USDZ jadi, reload → sisa jatah berkurang **tepat 1, bukan 2** (konversi USDZ tidak menagih credit).
- [ ] Buka `/dashboard/menu` → meteran muncul persis **di antara header dan tabel menu**. Matikan RPC `get_ai_credit_status` (atau pakai cafe tak dikenal) → meteran **hilang total**, bukan menampilkan 0/0, dan halaman tetap render.
- [ ] Set `ai_credits_used = quota-1` → warna **kuning**; `= quota` → **merah**; `status_lunas=false` dengan `used=0` → **tetap merah**. Set `status_lunas=false` DAN `used=quota` → yang tampil **kalimat langganan**, bukan kalimat kuota.
- [ ] Paksa `getCreditStatus` mengembalikan `periodStart: ""` → label berbunyi **"bulan ini"**, bukan "undefined NaN".
- [ ] Jalankan axe/Lighthouse atau NVDA di `/dashboard/menu` → elemen **`role="progressbar"`** terbaca dengan nilai numerik benar.
- [ ] Set `ai_credits_period_start` ke bulan lalu dan `used = quota`, lalu klik Auto-Isi → **BERHASIL** (reset otomatis saat klaim); meteran setelah reload menunjukkan `used = 1`.
- [ ] Set `ai_credits_period_start` ke bulan lalu dengan `used = quota`, reload `/dashboard/menu` **tanpa** memakai fitur AI → meteran menunjukkan **sisa penuh** dan label bulan berjalan (reset virtual).

---

## §9. `/dashboard/inventory`

- [ ] Throttle Slow 3G, klik menu Inventory → skeleton berbentuk **4 kartu + 2 panel** (bukan spinner polos); inspect root skeleton → **`aria-busy="true"`** dan **`aria-label="Memuat inventory"`**; shimmer bergerak, lalu berhenti saat `prefers-reduced-motion` aktif.
- [ ] Lihat waterfall jaringan/log Supabase → query `items` dan `movements` **start hampir bersamaan**, bukan berurutan.
- [ ] Tambah bahan "Zucchini" lalu "Apel", refresh → **Apel di atas Zucchini** (A–Z).
- [ ] Lakukan **13** penyesuaian stok berturut-turut, refresh → panel Mutasi menampilkan **tepat 12** entri terbaru, teratas = yang terakhir dilakukan, lengkap dengan nama + satuan bahan hasil join.
- [ ] Ubah sementara nama tabel di query movements jadi salah → muncul **"Terjadi kendala saat memuat riwayat mutasi."** bukan crash/halaman kosong. Paksa **dua** query gagal → pesan gabungan persis **"Terjadi kendala saat memuat data bahan dan riwayat mutasi."**
- [ ] Dengan error dipaksa, bandingkan `/dashboard` (varian **embedded**, tanpa judul besar "Inventory") vs `/dashboard/inventory` (varian **standalone**, dengan judul).
- [ ] Buat bahan stok 5 minimum 5 → kartu **"Stok Menipis" bertambah 1**, bukan "Aman". Set stok 0 → **pindah ke "Stok Habis"** dan keluar dari "Menipis".
- [ ] Bandingkan hex warna dan ikon keempat kartu KPI satu per satu; kartu **"Nilai Stok" menampilkan "Rp" kecil** sebelum angka.
- [ ] Muat halaman → keempat angka KPI **menghitung naik dari 0**; scroll turun-naik **tidak memicu ulang**. Isi inventory >1 juta → tampil **"Rp 1.250.000"** dengan titik, bukan koma/tanpa pemisah, lebar digit tetap.
- [ ] Buka `/dashboard` dan `/dashboard/inventory` berdampingan → ukuran judul, jarak bawah header, dan keberadaan tombol berbeda persis seperti baseline. Tombol **"Buka fokus inventory" hanya ada di `/dashboard`** dan mengarah ke `/dashboard/inventory`.
- [ ] Set satu bahan stok 0, buka `/dashboard` → panel gradient oranye **"Bahan perlu dicek hari ini"** muncul di bawah kartu KPI. Set semua bahan aman → panel **lenyap sepenuhnya** (bukan panel kosong).
- [ ] Siapkan satu bahan stok 0 dan satu bahan stok 1 minimum 5 → **warna teks sisa stok kedua kartu berbeda**.
- [ ] Muat ulang halaman → tabel muncul **sedikit lebih dulu** dari panel mutasi (`dash-d1`/`dash-d2`/`dash-d5`). Aktifkan reduced motion → keduanya langsung terlihat, **tanpa elemen yang tetap transparan**.
- [ ] Tambah bahan baru → toast hijau muncul **kanan atas dan tetap di sana sampai diklik X** (tidak auto-dismiss, kecuali memang disepakati berubah).
- [ ] Inspect DOM → div `sr-only` dengan `aria-live` **selalu ada** (bukan dirender kondisional) dan isinya berubah setelah simpan.
- [ ] Tambah bahan baru **tanpa F5** → baris baru muncul di tabel dan angka "Total Bahan" bertambah (`router.refresh()`).
- [ ] Keyboard saja: Tab ke tombol **Ubah** pada baris tertentu → Enter → Escape. Fokus **kembali persis ke tombol Ubah baris itu**, bukan lompat ke awal halaman.
- [ ] Buka modal **Atur Stok** lalu langsung ketik angka tanpa klik apa pun → angka masuk ke field **Jumlah**. (Modal bahan autofokus ke field **Nama**.)
- [ ] Buka modal, inspect DOM → konten dialog di dalam **`#dash-portal-root`**; latar modal mengikuti **`var(--dash-raised)`**, bukan putih.
- [ ] Arahkan screen reader ke tombol X kedua modal → label **berbeda** ("formulir bahan" vs "pengaturan stok"), bukan sekadar "Close".
- [ ] Buka modal, tekan Tab berulang melebihi jumlah field → fokus **tidak keluar** ke tabel di belakang. Tekan **Escape** → tertutup; klik overlay → tertutup; halaman bisa discroll lagi.
- [ ] Perkecil lebar sampai mobile → tombol **"Tambah Bahan" tetap utuh satu baris dan tidak menyusut**, teks judul yang mengalah.
- [ ] Kafe tanpa bahan → ada **dua** tombol "Tambah Bahan" (header + tengah empty state) dan **keduanya berfungsi**.
- [ ] Perkecil jendela, Tab ke tabel, tekan panah kanan/kiri → tabel bergeser **±240px** dan **halaman tidak ikut scroll**; hint scroll horizontal terlihat.
- [ ] Cocokkan **enam** judul kolom persis, termasuk spasi pada **"Harga / Unit"**; pastikan `min-w-[760px]` masih ada.
- [ ] Buat bahan bernama 60 karakter dan catatan 100 karakter → keduanya **ellipsis** dan hover masing-masing memunculkan tooltip teks penuh.
- [ ] Buat bahan **1500 gram** → tabel menampilkan **"1,500 gram"** (koma). Isi harga 5000 → kolom Harga / Unit menampilkan **"Rp 5.000"** (titik ribuan, tanpa ",00").
- [ ] Bahan stok 5 minimum 5 → badge **"Menipis"** (bukan "Aman"); ketiga varian badge punya dot + label.
- [ ] Inspect `aria-label` tombol pada baris "Tomat" → berbunyi **"Atur stok Tomat"**, bukan hanya "Atur stok".
- [ ] Hover baris di desktop → menyala; emulasi sentuh, tap baris → latar **tidak tetap menyala**. Baris terakhir **tidak** punya garis bawah ganda dengan tepi kartu.
- [ ] Kafe baru tanpa penyesuaian → panel kanan menampilkan **empty state**, bukan kotak kosong. Perkecil di bawah breakpoint `lg` → panel **pindah ke bawah tabel** dengan lebar penuh.
- [ ] Lakukan satu **tambah**, satu **kurang**, satu **set** → ketiganya punya **label dan ikon berbeda**. Lakukan "Set jumlah persis" ke nilai **sama dengan stok sekarang** → entri muncul **tanpa tanda + atau -, berwarna hijau**.
- [ ] Inspect elemen `<time>` → `dateTime` berisi **ISO penuh**, teksnya format pendek Indonesia; bulan singkat **Jul / Agu / Des**, bukan Jul/Aug/Dec.
- [ ] Penyesuaian stok dengan catatan sangat panjang → di panel mutasi catatan **terpotong tepat di baris kedua**, tidak memanjangkan kartu.
- [ ] Hapus sebuah `Inventory_Items` di Supabase yang punya riwayat mutasi, muat halaman → entri menampilkan **"Bahan dihapus"**, bukan baris kosong/error render.
- [ ] Buka modal Tambah Bahan di ponsel/emulator → field **Stok memunculkan keyboard desimal**, field **Harga keyboard numerik**. Dropdown satuan berisi **tepat 7 pilihan** urut: gram, kg, ml, liter, pcs, pack, botol.
- [ ] Throttle Slow 3G, klik Simpan → tombol menampilkan **spinner + label "Menyimpan"**, dan klik kedua **tidak membuat data ganda**.
- [ ] Ubah value option satuan jadi nilai asing lewat DOM lalu submit → **kotak merah muncul di dalam modal** dan isian form **tidak ter-reset**.
- [ ] Simpan bahan bernama `" Kopi "` (dengan spasi) → toast berbunyi **"Kopi berhasil ditambahkan ke inventory."** tanpa spasi menggantung.
- [ ] Modal Atur Stok: pilih **"Set jumlah persis"**, ketik 0, submit → **diterima**. Pilih **"Tambah stok"**, ketik 0, submit → **browser menolak** (min = 0.001). Sama untuk mode "Kurangi stok".
- [ ] Buka Atur Stok untuk bahan bersatuan liter dengan stok 2.5 → deskripsi berbunyi **"Nama. Stok sekarang 2.5 liter."** dan placeholder field jumlah **"Jumlah (liter)"**.
- [ ] Bandingkan kedua modal → **Atur Stok punya baris tombol Batal + Simpan**; modal Tambah/Ubah Bahan hanya punya satu tombol "Simpan Bahan" rata kiri.
- [ ] Panggil `updateInventoryItem` dengan ID milik kafe lain → query update **tetap ter-scope `cafe_id`**, tidak ada baris berubah.
- [ ] Tambah bahan baru di `/dashboard/inventory`, lalu navigasi ke `/dashboard/menu` dan buka form menu → bahan baru **sudah tersedia** di dropdown RecipeEditor **tanpa hard refresh**.
- [ ] Dengan 2 bahan di inventory, klik "Tambah Bahan" di RecipeEditor tiga kali → tombol **mati setelah klik kedua** (tanpa baris duplikat). Tambah dua baris dengan bahan berbeda, buka dropdown baris pertama → bahan milik baris kedua **disabled**, bahan baris sendiri tetap bisa dipilih.
- [ ] Kosongkan field jumlah pada satu baris resep lalu simpan menu → **"Jumlah setiap bahan harus lebih dari 0."** dan penyimpanan dibatalkan. Resep **kosong sepenuhnya** harus dianggap **valid**.
- [ ] Pilih bahan bersatuan "liter" → teks **"liter" muncul di dalam kanan field jumlah**; klik tepat pada tulisan itu → kursor masuk ke input.
- [ ] Tambah dua baris resep, hapus yang pertama → baris kedua **tetap ada dengan jumlahnya utuh**.
- [ ] Buat satu bahan berstatus habis lalu buka form menu → di baris bahan itu muncul konteks **"Stok 0 gram" / "Minimum 500 gram" / "Habis"** dan link **"Perbarui stok"** ke `/dashboard/inventory` (hanya pada baris itu).
- [ ] Throttle jaringan, klik Simpan pada form menu → selama spinner **tidak satu pun** kontrol di blok Resep Inventory bisa diubah.
- [ ] Klik-tahan tombol "Tambah Bahan" → mengecil. Hover ikon Ubah pada baris tabel → latar muncul. Emulasi sentuh → hover **tidak tersangkut** setelah tap.
- [ ] Tab melewati semua field modal Tambah Bahan → setiap field (termasuk **select Satuan** dan **textarea Catatan**) menampilkan **border oranye saat fokus**, bukan outline biru default browser.
- [ ] Buka accessibility tree / screen reader → muncul region bernama **"Inventory"**, **"Daftar Bahan"**, **"Mutasi Terbaru"**, bukan tiga region tanpa nama.

---

## §10. `/dashboard/settings` — Pengaturan & QR Smart Menu

- [ ] Isi greeting + URL ulasan Google, reload → nilai **muncul kembali** di input (query Settings memakai `select("*")`; kalau kolom hilang dari select, field kosong tiap reload).
- [ ] Throttle Slow 3G, klik menu Pengaturan → skeleton proporsional dengan halaman jadi, **tanpa layout shift besar**.
- [ ] Cek input **Tautan Smart Menu** di produksi → **tidak ada slash ganda atau protokol dobel** (prioritas env + normalisasi).
- [ ] Klik satu kali di input tautan → **seluruh teks tersorot otomatis** (read-only + select-on-focus).
- [ ] Klik **Salin**: umpan balik muncul ~**2,2 detik**. Uji juga di halaman http atau dengan izin clipboard diblokir → **pesan fallback muncul**, bukan diam saja; pesannya `aria-live`.
- [ ] Klik **"Buka Menu"** → tab baru ke URL **yang sama persis** dengan isi input.
- [ ] Tab ke pemicu **"Sesuaikan Tampilan QR"**, tekan Enter → panel terbuka, **chevron berputar**, `aria-expanded` berubah.
- [ ] Ubah setiap kontrol kustomisasi → pratinjau berubah **instan**; buka Network → **tidak ada request** saat mengubah opsi (SVG di-memo).
- [ ] Centang **logo** → jumlah modul QR bertambah (level koreksi error adaptif). Unduh PNG berlogo dan **scan dengan ponsel dari 30cm** → tetap terbaca (backing putih + logo 20% matriks).
- [ ] Pilih style **Bingkai 3Diner**, unduh PNG **dan** SVG → keduanya memuat garis tepi navy dan teks **"3DINER SMART MENU"** pada posisi sama.
- [ ] Pilih warna **Gelap Pekat**, unduh SVG, buka di editor teks → atribut `fill` path bernilai **`#10151D`**.
- [ ] Hapus centang **nama kafe** dan **teks scan** → tinggi SVG **menyusut** dan caption hilang **dari PNG juga**.
- [ ] Unduh PNG dengan semua opsi aktif → lebar berkas **≥ ~2048px**, posisi caption **identik** dengan SVG hasil unduhan; quiet zone 4 modul (lebar = size + 2×quiet zone).
- [ ] Unduh SVG berlogo, buka di editor teks → `href` diawali **`data:image/png;base64,`**, bukan `/brand/...`; buka berkas **offline** → logo tetap tampil.
- [ ] Ganti nama kafe jadi mengandung `<script>`, unduh SVG → keluar sebagai **`&lt;script&gt;`** (escaping XML pada seluruh teks SVG).
- [ ] Inspect SVG QR → ada **`role="img"`** dengan nama seperti **"QR menu digital Senja Kopi"**.
- [ ] Unduh berkas di **Safari/iOS dan Chrome** → berkas tersimpan **utuh, tidak 0 byte** (anchor sementara + revoke tertunda 2 detik).
- [ ] Uji slug berisi **spasi/tanda kurung** → nama berkas hasil unduhan **tersanitasi**.
- [ ] Klik cepat dua kali tombol unduh → **hanya satu berkas**; **kedua** tombol unduh nonaktif selama proses.
- [ ] Picu jalur gagal ekspor (mis. blokir canvas) → **pesan merah muncul dan tidak menggeser tata letak**; pesan sukses/gagal keduanya `aria-live`.
- [ ] Cari string **"3x3 cm"** di UI (petunjuk cetak QR) → masih ada setelah rebuild.
- [ ] Ukur `minHeight` setiap kontrol QR (style/DevTools) → **≥44px** semuanya.
- [ ] Render dengan `slug` null / `menuUrl` kosong → **state kosong QR** muncul, bukan matriks rusak.
- [ ] Muat **`/dashboard/settings#qr-menu`** → halaman **tergulir langsung** ke panel QR.
- [ ] Kosongkan field **greeting** → baris greeting **hilang** dari pratinjau. Kosongkan **nama** → pratinjau menampilkan fallback **"Nama Kafe"**.
- [ ] Hapus logo lewat tombol X pada FileUpload → pratinjau jatuh ke **mark 3Diner**, bukan kotak putih kosong. Hapus sampul → **fallback gradien**, bukan area kosong.
- [ ] Ganti foto sampul → pergantian gambar **melembut ~300ms**; judul pratinjau juga bertransisi.
- [ ] Lebar **390px** → mockup ponsel **di atas** form; **1440px** → di kanan. Scroll halaman ke bawah pada layar lebar → mockup **tetap terlihat (sticky)**; isi konten panjang lalu scroll di dalam mockup → **tanpa scrollbar terlihat**.
- [ ] Simpan sekali dan hitung → label **"Tersimpan" kembali jadi "Simpan Perubahan" setelah ~2,5 detik**. Tekan **Enter** di field nama juga menyimpan.
- [ ] Hapus atribut `required` lewat DevTools, kosongkan nama kafe, simpan → banner **"Nama kafe wajib diisi."** Isi nama dengan **spasi saja** → server tetap **menolak**.
- [ ] Kosongkan URL ulasan, simpan, reload → field **tetap kosong** (dinormalisasi ke null) dan tombol ulasan **hilang** dari menu pelanggan.
- [ ] Hapus cookie sesi di tab lain, lalu tekan Simpan → **"Sesi tidak valid. Masuk ulang."**, bukan sukses palsu.
- [ ] Unggah logo dan pantau Network → request **PUT ke domain Supabase**, bukan `/api` milik aplikasi.
- [ ] Pilih satu berkas, hapus dengan X, lalu pilih **berkas yang sama** lagi → unggahan tetap berjalan (reset `e.target.value`). Seret berkas ke zona → border berubah sebelum dilepas.
- [ ] Unggah berkas → teks hijau **"Terunggah"** berubah **"Tersimpan"** setelah ~2 detik.

---

## §11. `/dashboard/scheduler` — Jadwal & Diskon

- [ ] Throttle Slow 3G → skeleton Scheduler proporsional (`max-w-3xl`). Rekam pemuatan dengan **>8 menu** → kartu ke-1..8 muncul bertahap, kartu ke-9 dst **muncul bersamaan** (delay dibatasi `7 × 0.055s`).
- [ ] Aktifkan **Reduce Motion**, muat Scheduler → semua kartu langsung terlihat penuh, **tidak ada yang tetap transparan**.
- [ ] Tambah menu bernama "Aaa" dan "Zzz" → urutan daftar **alfabetis**, bukan urutan pembuatan.
- [ ] Akun kafe **tanpa menu** → state kosong dengan **CTA tambah menu**, bukan halaman kosong.
- [ ] Simpan menu dengan **hanya jam mulai** (tanpa hari), reload → baris kembali dalam mode **"Jadwal"**, bukan "Selalu".
- [ ] Matikan sakelar menu → `aria-pressed="false"` di inspector, knop beranimasi, badge status berubah **"Tersembunyi"**, dan **seluruh badan kartu meredup**.
- [ ] Dengan menu nonaktif, coba klik chip hari dan input jam → **tidak ada reaksi**; nyalakan lagi → kontrol bisa dipakai.
- [ ] Klik **Selalu** lalu Simpan → di database `schedule_days` / `schedule_start` / `schedule_end` menjadi **NULL**.
- [ ] Pilih **semua hari** → tombol berubah jadi **"Hapus semua"**; simpan dan cek `schedule_days` berisi **`1,2,3,4,5,6,7`**. Cek 7 chip punya label singkat tapi `aria-label` penuh.
- [ ] Isi jam **22:00–02:00** lalu simpan → uji perilaku lintas tengah malam: pada pukul 23:00 **dan** 01:00 menu harus **tampil**.
- [ ] Ubah zona waktu OS ke **UTC** lalu buka halaman Jadwal → badge "Tampil/Tersembunyi" tetap mengikuti **waktu Jakarta**, bukan waktu perangkat.
- [ ] Pilih mode **Jadwal** tanpa mengisi apa pun → muncul pesan penjelas dan badge tetap **"Tampil"**.
- [ ] Tempelkan **`250`** ke kolom diskon → nilai langsung jadi **100** dan teks bantuan menampilkan 100%.
- [ ] Atur jadwal **hanya hari ini dengan jam yang sudah lewat** → badge langsung berubah **"Tersembunyi" tanpa menyimpan** (dihitung dari state belum-disimpan).
- [ ] Buka halaman → **tidak ada** tombol simpan di kartu mana pun. Ubah satu chip hari → tombol muncul **hanya di kartu itu**. Simpan → label berganti dan **footer hilang setelah ~2,5 detik**.
- [ ] Simpan satu baris lalu buka `/dashboard/menu` → status aktif/diskon menu **sudah ikut terbarui** (revalidate ganda), dan UI tidak sempat memblokir saat menyimpan (`useTransition`).
- [ ] Putuskan jaringan lalu tekan Simpan → error muncul **di kartu itu saja**, bukan toast global tanpa konteks.
- [ ] Set diskon **100** pada satu menu → harga di halaman pelanggan **tetap harga asli** (batas `pct < 100`). Set **33%** pada harga 25000 → hasil **16750** (bulat).
- [ ] Klik teks label **"Mulai"** → fokus lompat ke input jam **pada baris yang sama**.

---

## §12. `/dashboard/announcements` — Pengumuman

- [ ] Akun tanpa pengumuman → form kosong (warna default oranye, jenis **Info**, sakelar mati), bukan error (`maybeSingle`).
- [ ] Pilih jenis **"Penting"** → ikon segitiga peringatan **ikut muncul di banner pratinjau**. Cek pemilih jenis berupa **4 kolom berikon**.
- [ ] Klik template **"Libur"** → pesan, jenis **"Penting"**, dan warna **#B91C1C** berubah **bersamaan**, dan pratinjau ikut berubah. Uji kelima template.
- [ ] Tempel **300 karakter** → hanya **120 pertama** masuk; penghitung berwarna **amber di 105+**.
- [ ] Pilih warna kuning terang lewat picker → tanda centang/teks pratinjau berubah jadi **navy gelap**, bukan putih. Set `#FFFF00` lalu `#000080` → teks berganti **navy lalu putih** otomatis (luminance WCAG). Cek 5 preset + picker kustom + tampilan hex.
- [ ] Nyalakan sakelar "Tampilkan ke pelanggan" → **`aria-checked="true"`** (`role="switch"`) dan pratinjau berganti dari **"Banner nonaktif"** ke banner berwarna.
- [ ] Simpan, lalu bandingkan pratinjau dengan halaman **`/[slug]`** sungguhan → **warna, ikon, dan tombol X sama**.
- [ ] Simpan pertama kali (insert), lalu ubah pesan dan simpan lagi (update) → jumlah baris di tabel `Announcements` **tetap satu** (id ikut terkirim di FormData).
- [ ] Simpan, lalu **segera** klik sebuah template → label tombol langsung kembali dari **"Tersimpan"** ke normal.

---

## §13. Lintas rute — sistem desain, motion, aksesibilitas

- [ ] Bandingkan **head band** DashboardPanel di Analitik, Penjualan, Inventory, Pengaturan, Jadwal, Pengumuman → tinggi, huruf kapital, dan padding **identik**; radius 16px konsisten; tabel di dalam panel terpotong mengikuti radius. Judul kartu di Jadwal tetap **huruf normal**, bukan KAPITAL. Tombol di head band **kapital normal**, bukan ALL CAPS.
- [ ] Buka `/dashboard/orders` di 375px → elemen **DashboardToolbar** menumpuk vertikal dengan gap (tidak berdesakan); di desktop terpisah kiri-kanan dengan garis pemisah di bawahnya.
- [ ] Kumpulkan seluruh **StatusBadge** dari `/dashboard/revenue`, `/dashboard/orders`, `/dashboard/menu`, `/dashboard/scheduler` → cocokkan label dan warna dengan tabel META (12 status). Badge **"Belum Bayar"**, **"Tanpa resep"**, dan **"Nonaktif"** harus bergaya **outline** (transparan berbingkai), bukan berlatar. Setiap badge selalu punya **dot + label**, tidak pernah warna saja.
- [ ] Picu satu **ConfirmAction** (aksi destruktif): Tab berulang → fokus **tidak keluar** ke halaman di belakang; Escape → fokus **kembali ke tombol pemicu**; tombol konfirmasi destruktif **merah `#EF4444`**, bukan oranye; dialog **zoom-in halus dari 95%**.
- [ ] Kosongkan filter pesanan sampai tanpa hasil, dan filter jadwal kosong → keduanya menampilkan blok terpusat dengan **judul + hint + CTA**, bukan area kosong melompong (tiga state standar Empty/Error/Skeleton).
- [ ] Klik teks **label** sebuah field di `/dashboard/settings` → kursor masuk ke input. Fokus input → **ring oranye 3px**, bukan outline biru default browser (`outline-none` menghapusnya, jadi ring ini satu-satunya indikator fokus).
- [ ] Tab ke tombol **Batal** di ConfirmAction → ring 3px `--ring`. Tekan-tahan tombol → **bergeser turun 1px**. Tombol yang membuka popover → **TIDAK** bergeser (dijaga `:not([aria-haspopup])`).
- [ ] Jalankan `npx shadcn diff` atau bandingkan dengan registry → **5 primitive** (dialog, sheet, tooltip, popover, dropdown/collapsible sesuai baseline) punya tambahan prop **`container`**; buka setiap jenis overlay di dashboard → semuanya di dalam `#dash-portal-root`.
- [ ] Grep pemakaian **Collapsible**; di mana pun dipakai, trigger tetap punya `aria-expanded` dan konten benar-benar dilepas/disembunyikan (bukan `height: 0` yang masih fokusable).
- [ ] Aktifkan **Reduce Motion** lalu muat kedelapan rute → header halaman langsung terlihat penuh, ikon nav pending tidak berdenyut, shimmer diam, count-up tidak jalan, **dan tidak ada satu pun elemen yang tetap tak terlihat**.
- [ ] Emulasi perangkat sentuh, tap kartu/baris di setiap rute → **tidak ada hover state yang menempel** (hover dibatasi `@media (hover: hover)`).
- [ ] Bandingkan format angka di seluruh dashboard → rupiah **`Rp 1.250.000`** (titik, tanpa desimal), bulan Indonesia (**Agu**, bukan Aug), tabular-nums di semua metrik.
- [ ] Deploy ke **produksi** (PWA mati di dev), muat dashboard, aktifkan **Offline** di DevTools, navigasi antar rute → tetap merender dari cache; matikan offline → halaman reload sendiri.

---

## §14. Suite tes otomatis (jalankan setelah rebuild)

- [ ] `cd C:\Kerja\3Diner\App && npx vitest run` — seluruh suite hijau.
- [ ] `npx vitest run tests/ai-credits.test.ts tests/menu-ai-route-auth.test.ts` — 10 test hijau (gerbang sesi AI, klaim/refund credit, fail-closed 503, biaya per jenis pekerjaan).
- [ ] `npx vitest run tests/inventory.test.ts tests/inventory-dashboard.test.ts tests/dashboard-inventory-actions.test.ts` — `formatQty` (`200 ml` / `1.5 kg` / `2.25 liter`), `inventoryStatus`, prioritas bahan kritis, label mutasi, aturan kuantitas Atur Stok, penolakan `-1 / NaN / -0.001 / Infinity / -20 / 'not-a-number'`, dan bukti RPC atomik (tanpa `.update()` dan tanpa insert manual ke `Inventory_Movements`).
- [ ] `npx vitest run tests/recipe-editor.test.ts` — seleksi baris, konteks stok (`Stok 0 gram` / `Minimum 500 gram` / `Habis`), link `/dashboard/inventory`, validasi kuantitas termasuk kasus resep kosong = valid.
- [ ] `npx vitest run tests/qr-smart-menu.test.ts` — helper `site-url`, sanitasi nama berkas, quiet zone, escaping XML, metadata `role="img"`, state kosong, kunci ekspor ganda, `minHeight` ≥44px.
- [ ] `npx vitest run tests/orders-sonner.test.tsx` — toast dedupe id pesanan, toast realtime disconnect id tetap, kartu sebagai `<section>`, StatusBadge, dialog struk di dalam `#dash-portal-root`, pemulihan fokus.
- [ ] `npx vitest run tests/receipt-html-escaping.test.ts tests/sales-report-html-escaping.test.ts tests/sales-csv-injection.test.ts` — payload `<script>`, `<img onerror>`, dan `=cmd|' /C calc'!A0` semuanya tertahan.
- [ ] `npx vitest run tests/menu-options.test.ts tests/menu-form-save.test.ts tests/menu-table-inventory.test.ts tests/menu-page-inventory-errors.test.ts` — varian, urutan simpan berlapis, comparator sorting, kartu error inventory.

---

## §15. Keputusan produk yang wajib dikonfirmasi sebelum rebuild dinyatakan selesai

Ini bukan bug. Ini perilaku baseline atau kode menganggur yang **harus diputuskan secara sadar** — kalau hilang tanpa keputusan, itu tetap dihitung fitur hilang.

- [ ] Field **`desc`** pada 8 item navigasi: saat ini tidak dirender di sidebar desktop tapi tampil di sheet mobile. Tampilkan di mana saja, atau buang? Kalau ditampilkan, teksnya harus **persis 8 string yang ada**.
- [ ] **`quietHint`** di Analitik: dihitung tapi tidak pernah ditampilkan. Tampilkan atau buang?
- [ ] Komponen chart lama **`Counter`, `AnimatedBar`, `DailyChart`**: jalankan pencarian impor di seluruh `src/`. Kalau nihil, konfirmasi ke pemilik produk apakah dibuang atau dipertahankan sebagai cadangan.
- [ ] Label kategori kosong di `/dashboard/menu`: desktop **`-`** vs mobile **"Tanpa kategori"**. Diseragamkan atau dipertahankan?
- [ ] Bar chart Penjualan **tanpa dukungan sentuh** (tap tidak memunculkan tooltip). Menambahkannya = perubahan perilaku yang harus disepakati.
- [ ] Dropdown tanggal **tidak mengikuti scroll/resize** (berbeda dari menu Ekspor yang mengikuti). Diperbaiki atau dipertahankan?
- [ ] Kalender **tidak punya navigasi panah keyboard**, dan dropdown tanggal **tidak memindahkan/mengembalikan fokus** saat dibuka-ditutup. Diperbaiki atau dicatat sebagai utang a11y?
- [ ] Ekspor **CSV 9 kolom vs PDF 8 kolom** — perbedaan disengaja. Pertahankan atau samakan?
- [ ] Ekspor terpotong **2000 baris terbaru tanpa peringatan ke user**. Tambahkan peringatan atau pertahankan?
- [ ] Toast Inventory **tidak auto-dismiss** (tetap sampai diklik X), berbeda dari toast Pesanan. Diseragamkan atau dipertahankan?
- [ ] Default kuota AI per tier langganan: migrasi memakai klausa `where ai_credits_quota = 5` sehingga **nilai manual tidak tertimpa**. Pastikan aturan ini masih berlaku setelah rebuild.

---

**Tanda tangan verifikasi**

| | |
| --- | --- |
| Diverifikasi oleh | |
| Tanggal | |
| Commit / branch | |
| Jumlah kotak gagal | |
| Nomor kotak yang gagal | |
