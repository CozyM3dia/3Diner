/** StatCard = alias DashboardMetric (system layer).
 *  Dipertahankan sampai semua call site pindah; dihapus di hardening pass. */
export { default, type DashboardMetricProps as StatCardProps } from "./system/DashboardMetric";
